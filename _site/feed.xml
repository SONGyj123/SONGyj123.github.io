<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://songyj123.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://songyj123.github.io/" rel="alternate" type="text/html" /><updated>2025-04-22T22:40:23+08:00</updated><id>https://songyj123.github.io/feed.xml</id><title type="html">SONGyj123</title><subtitle>description
</subtitle><author><name>Songyj</name></author><entry><title type="html">Rpi_4b</title><link href="https://songyj123.github.io/rpi-4b.html" rel="alternate" type="text/html" title="Rpi_4b" /><published>2025-04-19T00:00:00+08:00</published><updated>2025-04-19T00:00:00+08:00</updated><id>https://songyj123.github.io/RPI_4B</id><content type="html" xml:base="https://songyj123.github.io/rpi-4b.html"><![CDATA[<h1 id="rpi-4b">RPI 4B</h1>

<h2 id="0x00-boot-config">0x00 Boot config</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Stage</th>
      <th style="text-align: center">Filename</th>
      <th style="text-align: left">Description</th>
      <th style="text-align: left">Source</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">FSBL</td>
      <td style="text-align: center">-</td>
      <td style="text-align: left">Mounts SD and loads SSBL</td>
      <td style="text-align: left">ROM</td>
    </tr>
    <tr>
      <td style="text-align: left">GPU firmware</td>
      <td style="text-align: center">start4.elf</td>
      <td style="text-align: left">Loads CPU bootloader and boots CPU</td>
      <td style="text-align: left">https://github.com/raspberrypi/firmware/tree/master/boot/start4.elf</td>
    </tr>
    <tr>
      <td style="text-align: left">Additional GPU firmware</td>
      <td style="text-align: center">fixup4.dat</td>
      <td style="text-align: left">Fixes memory locations used in start4.elf</td>
      <td style="text-align: left">https://github.com/raspberrypi/firmware/tree/master/boot/fixup4.dat</td>
    </tr>
    <tr>
      <td style="text-align: left">Usually the Linux kernel, but could also be U-Boot</td>
      <td style="text-align: center">u-boot.bin</td>
      <td style="text-align: left">U-Boot binary</td>
      <td style="text-align: left">Compiled using the instructions above</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: center">config.txt</td>
      <td style="text-align: left">U-Boot parameters</td>
      <td style="text-align: left">Add <code class="language-plaintext highlighter-rouge">arm_64bit=1</code> and <code class="language-plaintext highlighter-rouge">kernel=u-boot.bin</code> to the bottom of <code class="language-plaintext highlighter-rouge">config.txt</code><br />还需要指定<code class="language-plaintext highlighter-rouge">enable_uart=1</code>以及<code class="language-plaintext highlighter-rouge">uart_2ndstage=1</code></td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: center">uboot.env</td>
      <td style="text-align: left">U-Boot saved environment</td>
      <td style="text-align: left">Generated by U-Boot (default environment) bootcmd copied to bootcmd_orig bootcmd and bootdelay removed. This file will not exist when you first setup your SD card.</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: center">bcm2711-rpi-4-b.dtb</td>
      <td style="text-align: left">RPi4 device tree blob</td>
      <td style="text-align: left">https://github.com/raspberrypi/firmware/tree/master/boot/bcm2711-rpi-4-b.dtb</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: center">overlays/*</td>
      <td style="text-align: left">Device tree overlays</td>
      <td style="text-align: left">https://github.com/raspberrypi/firmware/tree/master/boot/overlays</td>
    </tr>
  </tbody>
</table>

<h3 id="partition-the-sd-card">Partition the SD card</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># fdisk操作SD卡 根据fdisk提示信息来操作就行了</span>
<span class="c"># /boot / 两个就行</span>
<span class="c"># 所有的启动文件放在boot里面</span>
<span class="nb">sudo </span>fdisk /dev/sdb
</code></pre></div></div>

<h2 id="0x01-gpio-pinout">0x01 GPIO Pinout</h2>

<p>[]: https://pinout.xyz/pinout/pin8_gpio14/	“RPI 40 GPIO pinout”</p>

<h2 id="0x02-uboot--kernel">0x02 uboot &amp; kernel</h2>

<h3 id="编译源码">编译源码</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git://git.denx.de/u-boot.git
<span class="nb">cd </span>u-boot
make <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-linux-gnu- rpi_4_defconfig
make <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-linux-gnu-
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="nt">--depth</span><span class="o">=</span>1 <span class="nt">-b</span> rpi-6.12.y https://github.com/raspberrypi/linux.git
<span class="nb">cd </span>linux
make <span class="nv">ARCH</span><span class="o">=</span>arm64 <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-rpi4-linux-gnu- bcm2711_defconfig
make <span class="nv">ARCH</span><span class="o">=</span>arm64 <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-rpi4-linux-gnu- <span class="nt">-j12</span>
</code></pre></div></div>

<h3 id="boot设置">Boot设置</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># boot kernel from uboot
tftpboot ${kernel_addr_r} Image
tftpboot ${fdt_addr_r} bcm2711-rpi-4-b.dtb
booti ${kernel_addr_r} - ${fdt_addr_r}
setenv bootargs 8250.nr_uarts=1 console=ttyS0,115200 root=/dev/mmcblk0p2 rootwait rw fsck.repair=yes 
</code></pre></div></div>

<p>可以把三个load和启动命令做成脚本自动执行：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># 把命令写入boot.cmd，并且在PC上做成scr脚本
mkimage -A arm64 -O linux -T script -C none -d boot.cmd boot.scr

# 在uboot中用bootcmd来执行脚本
setenv bootcmd "load mmc 0:1 ${scriptaddr} boot.scr; source ${scriptaddr}"
saveenv
</code></pre></div></div>

<h4 id="问题">问题</h4>

<p><strong>1. 启动kenel后停留在<code class="language-plaintext highlighter-rouge">starting kernel...</code></strong></p>

<p>连接HDMI屏幕发现屏幕有打印，应该是串口没被正确设置。在bootargs中添加<code class="language-plaintext highlighter-rouge">8250.nr_uarts=1</code></p>

<p><strong>2. 报错<code class="language-plaintext highlighter-rouge">systemd-remount-fs.service fails to remount rootfs as read-write</code></strong></p>

<p>串口配置好后，发现启动没有进入控制台，检查后发现remount失败了。因为rootfs是从做好的img镜像文件中直接复制的，所以UUID和自己的SD分区UUID不匹配。需要修改/etc/fstab中的配置匹配SD分区</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>blkid
/dev/sdb1: <span class="nv">LABEL_FATBOOT</span><span class="o">=</span><span class="s2">"boot"</span> <span class="nv">LABEL</span><span class="o">=</span><span class="s2">"boot"</span> <span class="nv">UUID</span><span class="o">=</span><span class="s2">"9489-2CD5"</span> <span class="nv">BLOCK_SIZE</span><span class="o">=</span><span class="s2">"512"</span> <span class="nv">TYPE</span><span class="o">=</span><span class="s2">"vfat"</span> <span class="nv">PARTUUID</span><span class="o">=</span><span class="s2">"2ab1dd98-12c4-4f14-8cc4-5182555a9c24"</span>
/dev/sdb2: <span class="nv">LABEL</span><span class="o">=</span><span class="s2">"root"</span> <span class="nv">UUID</span><span class="o">=</span><span class="s2">"9263d76f-db89-4e1b-95c8-9cce75656338"</span> <span class="nv">BLOCK_SIZE</span><span class="o">=</span><span class="s2">"4096"</span> <span class="nv">TYPE</span><span class="o">=</span><span class="s2">"ext4"</span> <span class="nv">PARTUUID</span><span class="o">=</span><span class="s2">"6958800b-7d1c-4941-bdea-5a1ad942258b"</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> /etc/fstab
proc            /proc           proc    defaults          0       0
<span class="nv">PARTUUID</span><span class="o">=</span>2ab1dd98-12c4-4f14-8cc4-5182555a9c24  /boot  vfat    defaults          0       2
<span class="nv">PARTUUID</span><span class="o">=</span>6958800b-7d1c-4941-bdea-5a1ad942258b  /               ext4    defaults,noatime  0       1
</code></pre></div></div>

<p><strong>3. 用户名密码错误</strong></p>

<p>修改bootargs直接进入bash，在bash中修改并固化</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 进入到bash可以修改密码</span>
setenv bootargs 8250.nr_uarts<span class="o">=</span>1 <span class="nv">console</span><span class="o">=</span>ttyS0,115200 <span class="nv">root</span><span class="o">=</span>/dev/mmcblk0p2 rootwait rw <span class="nv">init</span><span class="o">=</span>/bin/sh

<span class="nv">$ </span>passwd pi <span class="c"># pi代表用户名</span>
<span class="nv">$ </span><span class="nb">sync</span>
<span class="nv">$ </span><span class="nb">exec</span> /sbin/init <span class="c"># 继续启动</span>
</code></pre></div></div>

<h2 id="0x00-reference">0x00 Reference</h2>

<p>[]: https://docs.sel4.systems/Hardware/Rpi4.html	“rpi4b boot preparation”
[]: https://hechao.li/posts/Boot-Raspberry-Pi-4-Using-uboot-and-Initramfs/#72-option-2-boot-with-a-permanent-rootfs-directly
[]: https://lancesimms.com/RaspberryPi/HackingRaspberryPi4WithYocto_Part3.html</p>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[RPI 4B]]></summary></entry><entry><title type="html">Linux_driver</title><link href="https://songyj123.github.io/linux-driver.html" rel="alternate" type="text/html" title="Linux_driver" /><published>2025-04-05T00:00:00+08:00</published><updated>2025-04-05T00:00:00+08:00</updated><id>https://songyj123.github.io/Linux_Driver</id><content type="html" xml:base="https://songyj123.github.io/linux-driver.html"><![CDATA[<h1 id="linux-driver">Linux Driver</h1>

<h2 id="0x00-env-setup">0x00 ENV Setup</h2>

<h3 id="共享网络-可以ssh不能上网的情况">共享网络 （可以ssh不能上网的情况）</h3>

<p>ifconfig命令查看wifi网卡和有线网卡，需要將流量从wifi网卡转发到有线网卡，如下所示wifi卡（wlo1），有线（eno1）</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>ifconfig
docker0: <span class="nv">flags</span><span class="o">=</span>4099&lt;UP,BROADCAST,MULTICAST&gt;  mtu 1500
        inet 172.17.0.1  netmask 255.255.0.0  broadcast 172.17.255.255
        ether 02:42:46:24:2c:35  txqueuelen 0  <span class="o">(</span>Ethernet<span class="o">)</span>
        RX packets 0  bytes 0 <span class="o">(</span>0.0 B<span class="o">)</span>
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 0  bytes 0 <span class="o">(</span>0.0 B<span class="o">)</span>
        TX errors 0  dropped 155 overruns 0  carrier 0  collisions 0

eno1: <span class="nv">flags</span><span class="o">=</span>4163&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu 1500
        inet 10.42.0.1  netmask 255.255.255.0  broadcast 10.42.0.255
        inet6 fe80::d8ea:b4fd:d1b0:1445  prefixlen 64  scopeid 0x20&lt;<span class="nb">link</span><span class="o">&gt;</span>
        ether 04:0e:3c:04:37:f0  txqueuelen 1000  <span class="o">(</span>Ethernet<span class="o">)</span>
        RX packets 19935  bytes 12312967 <span class="o">(</span>12.3 MB<span class="o">)</span>
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 14446  bytes 2897490 <span class="o">(</span>2.8 MB<span class="o">)</span>
        TX errors 0  dropped 18 overruns 0  carrier 0  collisions 0

enx0826ae35f08d: <span class="nv">flags</span><span class="o">=</span>4099&lt;UP,BROADCAST,MULTICAST&gt;  mtu 1500
        ether 08:26:ae:35:f0:8d  txqueuelen 1000  <span class="o">(</span>Ethernet<span class="o">)</span>
        RX packets 0  bytes 0 <span class="o">(</span>0.0 B<span class="o">)</span>
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 0  bytes 0 <span class="o">(</span>0.0 B<span class="o">)</span>
        TX errors 0  dropped 2 overruns 0  carrier 0  collisions 0

lo: <span class="nv">flags</span><span class="o">=</span>73&lt;UP,LOOPBACK,RUNNING&gt;  mtu 65536
        inet 127.0.0.1  netmask 255.0.0.0
        inet6 ::1  prefixlen 128  scopeid 0x10&lt;host&gt;
        loop  txqueuelen 1000  <span class="o">(</span>Local Loopback<span class="o">)</span>
        RX packets 1089779  bytes 4159784997 <span class="o">(</span>4.1 GB<span class="o">)</span>
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 1089779  bytes 4159784997 <span class="o">(</span>4.1 GB<span class="o">)</span>
        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0

wlo1: <span class="nv">flags</span><span class="o">=</span>4163&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu 1500
        inet 192.168.31.182  netmask 255.255.255.0  broadcast 192.168.31.255
        inet6 fe80::5ded:4ab:d586:b60c  prefixlen 64  scopeid 0x20&lt;<span class="nb">link</span><span class="o">&gt;</span>
        ether 50:e0:85:e8:58:0f  txqueuelen 1000  <span class="o">(</span>Ethernet<span class="o">)</span>
        RX packets 3582838  bytes 4974553253 <span class="o">(</span>4.9 GB<span class="o">)</span>
        RX errors 0  dropped 6  overruns 0  frame 0
        TX packets 788343  bytes 101990756 <span class="o">(</span>101.9 MB<span class="o">)</span>
        TX errors 0  dropped 34 overruns 0  carrier 0  collisions 0

</code></pre></div></div>

<p>输入以下命令进行转发配置：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>iptables <span class="nt">-t</span> nat <span class="nt">-A</span> POSTROUTING <span class="nt">-o</span> wlo1 <span class="nt">-j</span> MASQUERADE
<span class="nv">$ </span><span class="nb">sudo </span>iptables <span class="nt">-A</span> FORWARD <span class="nt">-i</span> eno1 <span class="nt">-o</span> wlo1 <span class="nt">-j</span> ACCEPT
<span class="nv">$ </span><span class="nb">sudo </span>iptables <span class="nt">-A</span> FORWARD <span class="nt">-i</span> wlo1 <span class="nt">-o</span> eno1 <span class="nt">-m</span> state <span class="nt">--state</span> RELATED,ESTABLISHED <span class="nt">-j</span> ACCEPT
<span class="nv">$ </span><span class="nb">sudo </span>netfilter-persistent save
</code></pre></div></div>

<p>随后在单板端创建文件/etc/netplan/50-cloud-init.yaml，内容如下</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">network</span><span class="pi">:</span>
    <span class="na">version</span><span class="pi">:</span> <span class="m">2</span>
    <span class="na">ethernets</span><span class="pi">:</span>
        <span class="na">eth0</span><span class="pi">:</span>
            <span class="na">addresses</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">10.42.0.218/24</span><span class="pi">]</span>  <span class="c1"># 与PC同子网</span>
            <span class="na">routes</span><span class="pi">:</span>
              <span class="pi">-</span> <span class="na">to</span><span class="pi">:</span> <span class="s">0.0.0.0/0</span>
                <span class="na">via</span><span class="pi">:</span> <span class="s">10.42.0.1</span>  <span class="c1"># PC的有线IP</span>
            <span class="na">nameservers</span><span class="pi">:</span>
                <span class="na">addresses</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">8.8.8.8</span><span class="pi">,</span> <span class="nv">1.1.1.1</span><span class="pi">]</span>  <span class="c1"># 手动指定DNS</span>
</code></pre></div></div>

<p>使能刚才的配置并确认已经转发到对应端口并尝试ping外网</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>netplan apply
<span class="nv">$ </span>ip route show | <span class="nb">grep </span>default
default via 10.42.0.1 dev enP4p65s0 proto dhcp metric 100
<span class="nv">$ </span>ping baidu.com
ING baidu.com <span class="o">(</span>110.242.68.66<span class="o">)</span> 56<span class="o">(</span>84<span class="o">)</span> bytes of data.
64 bytes from baidu.com <span class="o">(</span>110.242.68.66<span class="o">)</span>: <span class="nv">icmp_seq</span><span class="o">=</span>1 <span class="nv">ttl</span><span class="o">=</span>49 <span class="nb">time</span><span class="o">=</span>84.8 ms
64 bytes from baidu.com <span class="o">(</span>110.242.68.66<span class="o">)</span>: <span class="nv">icmp_seq</span><span class="o">=</span>3 <span class="nv">ttl</span><span class="o">=</span>49 <span class="nb">time</span><span class="o">=</span>101 ms
</code></pre></div></div>

<h3 id="tftp启动内核">tftp启动内核</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>setenv ipaddr 10.42.0.218
setenv netmask 255.255.255.0
setenv gatewayip 10.42.0.1
setenv serverip 10.42.0.1
setenv bootargs <span class="nv">root</span><span class="o">=</span>/dev/nfs rw <span class="nv">nfsroot</span><span class="o">=</span><span class="k">${</span><span class="nv">serverip</span><span class="k">}</span>:/home/songyj/embedded/nfs,v3,tcp <span class="nv">ip</span><span class="o">=</span><span class="k">${</span><span class="nv">ipaddr</span><span class="k">}</span>::<span class="k">${</span><span class="nv">serverip</span><span class="k">}</span>:255.255.255.0::eth0:off <span class="nv">console</span><span class="o">=</span>ttyS0,1500000n8
pci enum
tftpboot <span class="k">${</span><span class="nv">kernel_addr_r</span><span class="k">}</span> Image
tftpboot <span class="k">${</span><span class="nv">fdt_addr_r</span><span class="k">}</span> rk3588-rock-5b.dtb
booti <span class="k">${</span><span class="nv">kernel_addr_r</span><span class="k">}</span> - <span class="k">${</span><span class="nv">fdt_addr_r</span><span class="k">}</span>
</code></pre></div></div>

<h3 id="如何编译驱动">如何编译驱动？</h3>

<p>在PC上编译驱动需要内核源码以及交叉编译工具链，驱动文件可以在内核外随便找地方存放，编译之前需要先设置环境变量：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">ARCH</span><span class="o">=</span>arm64
<span class="nb">export </span><span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-linux-gnu-
</code></pre></div></div>

<p>在Makefile中指定内核路径。make -C表示到KDIR这个路径执行make，随后將生成的.ko复制到板上。</p>

<div class="language-makefile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">KDIR</span> <span class="o">?=</span> /home/songyj/embedded/rk5b/kernel
<span class="nv">PWD</span> <span class="o">:=</span> <span class="nf">$(</span><span class="nb">shell</span> <span class="nb">pwd</span><span class="nf">)</span>

<span class="nv">obj-m</span> <span class="o">+=</span> gpio.o

<span class="nl">all</span><span class="o">:</span>
        <span class="err">make</span> <span class="err">-C</span> <span class="err">${KDIR}</span> <span class="nv">M</span><span class="o">=</span><span class="nv">$(PWD)</span> modules

<span class="nl">clean</span><span class="o">:</span>
        <span class="err">make</span> <span class="err">-C</span> <span class="err">${KDIR}</span> <span class="nv">M</span><span class="o">=</span><span class="nv">$(PWD)</span> clean
</code></pre></div></div>

<h2 id="0x01-device-tree">0x01 Device Tree</h2>

<h3 id="dt-basics">DT basics</h3>

<p><img src="/home/songyj/note/pics/dts_description.png" alt="" /></p>

<p>冒号之前的是Lable，相当于节点的别名，如果后续需要修改节点的的property可以通过引用别名来追加修改或者新建property。</p>

<p><img src="/home/songyj/note/pics/dts_label_add_property.png" alt="" /></p>

<p>看一个例子，下面是截取rock5B的RGB LED的设备树节点。结合原理图可以确认user-led2是控制蓝色LED的引脚，RK_PB7和GPIO_ACTIVE_HIGH代表引脚编号以及电平设置，这两个宏定义在dt-binding相关的文档。这些格式都是内核文档中已经规定好的，在<strong>Documentation/devicetree/bindings</strong>中搜索具体的规则。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// arch/arm64/boot/dts/rockchip/rk3588-rock-5b.dts</span>
<span class="o">/</span><span class="p">{</span>
	<span class="p">...</span>
	
	<span class="n">gpio</span><span class="o">-</span><span class="n">leds</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"gpio-leds"</span><span class="p">;</span>
        <span class="n">pinctrl</span><span class="o">-</span><span class="n">names</span> <span class="o">=</span> <span class="s">"default"</span><span class="p">;</span>

        <span class="n">user</span><span class="o">-</span><span class="n">led2</span> <span class="p">{</span>
            <span class="n">gpios</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">gpio0</span> <span class="n">RK_PB7</span> <span class="n">GPIO_ACTIVE_HIGH</span><span class="o">&gt;</span><span class="p">;</span>
            <span class="n">linux</span><span class="p">,</span><span class="k">default</span><span class="o">-</span><span class="n">trigger</span> <span class="o">=</span> <span class="s">"heartbeat"</span><span class="p">;</span>
            <span class="k">default</span><span class="o">-</span><span class="n">state</span> <span class="o">=</span> <span class="s">"on"</span><span class="p">;</span>
    	<span class="p">};</span>
	<span class="p">};</span>
	
	<span class="p">...</span>
<span class="p">}</span>

<span class="c1">// include/dt-bindings/pinctrl/rockchip.h</span>
<span class="cp">#define RK_PB7		15
</span>
<span class="c1">// include/dt-bindings/gpio/gpio.h</span>
<span class="cp">#define GPIO_ACTIVE_HIGH 0
</span></code></pre></div></div>

<p><img src="/home/songyj/note/pics/rk5b_rgb_led_sch.png" alt="" /></p>

<h3 id="cells">cells</h3>

<p>DeveiceTree中有个比较重要的概念<strong>cells</strong>，<strong>cells</strong>表示property相关的32bits的数字。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x50027000</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0x4</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0x500273f0</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0x10</span><span class="o">&gt;</span><span class="p">;</span>	<span class="c1">// 4 entriy 4 cell</span>
<span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x50027000</span> <span class="mh">0x4</span> <span class="mh">0x500273f0</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0x10</span><span class="o">&gt;</span><span class="p">;</span>			<span class="c1">// 2 entriy 4 cell</span>
<span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x50027000</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0x4</span> <span class="mh">0x500273f0</span> <span class="mh">0x10</span><span class="o">&gt;</span><span class="p">;</span>			<span class="c1">// 2 entriy 4 cell</span>
<span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x50027000</span> <span class="mh">0x4</span> <span class="mh">0x500273f0</span> <span class="mh">0x10</span><span class="o">&gt;</span><span class="p">;</span>				<span class="c1">// 1 entriy 4 cell</span>
</code></pre></div></div>

<p>cells如何规定怎么填写property值，以下面设备树为例：在spi3节点中使用到之前定义的三个外设节点intc，rcc以及dmamux1，他们的cells与后面spi3使用时正好对应（注意类似<em>&amp;rcc</em>和<em>&amp;dmamux1</em>不是cell，因为他们不是和property相关的值，不计入cells数量）</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 定义三个外设</span>
<span class="n">intc</span><span class="o">:</span> <span class="n">interrupt</span><span class="o">-</span><span class="n">controller</span><span class="err">@</span><span class="n">a0021000</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"arm,cortex-a7-gic"</span><span class="p">;</span>
    <span class="cp">#interrupt-cells = &lt;3&gt;;
</span>    <span class="n">interrupt</span><span class="o">-</span><span class="n">controller</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0xa0021000</span> <span class="mh">0x1000</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;</span><span class="mh">0xa0022000</span> <span class="mh">0x2000</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
<span class="n">rcc</span><span class="o">:</span> <span class="n">rcc</span><span class="err">@</span><span class="mi">50000000</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"st,stm32mp1-rcc"</span><span class="p">,</span> <span class="s">"syscon"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x50000000</span> <span class="mh">0x1000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="cp">#clock-cells = &lt;1&gt;;
</span>    <span class="cp">#reset-cells = &lt;1&gt;;
</span><span class="p">};</span>
<span class="n">dmamux1</span><span class="o">:</span> <span class="n">dma</span><span class="o">-</span><span class="n">router</span><span class="err">@</span><span class="mi">48002000</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"st,stm32h7-dmamux"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x48002000</span> <span class="mh">0x1c</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="cp">#dma-cells = &lt;3&gt;;
</span>    <span class="n">clocks</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">rcc</span> <span class="n">DMAMUX</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">resets</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">rcc</span> <span class="n">DMAMUX_R</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>

<span class="c1">// spi3需要配置这三个外设</span>
<span class="n">spi3</span><span class="o">:</span> <span class="n">spi</span><span class="err">@</span><span class="mi">4000</span><span class="n">c000</span> <span class="p">{</span>
    <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="n">GIC_SPI</span> <span class="mi">51</span> <span class="n">IRQ_TYPE_LEVEL_HIGH</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">clocks</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">rcc</span> <span class="n">SPI3_K</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">resets</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">rcc</span> <span class="n">SPI3_R</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">dmas</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">dmamux1</span> <span class="mi">61</span> <span class="mh">0x400</span> <span class="mh">0x05</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;&amp;</span><span class="n">dmamux1</span> <span class="mi">62</span> <span class="mh">0x400</span> <span class="mh">0x05</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<h3 id="dtb使用">DTB使用</h3>

<ul>
  <li>设备树编译命令</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dtc -@ <span class="nt">-I</span> dts <span class="nt">-O</span> dtb <span class="nt">-o</span> ./gpio.dtbo ./gpio.dts
</code></pre></div></div>

<ul>
  <li>使能自己的dtbo</li>
</ul>

<p><strong>方法1：/boot/extlinux/extlinux.conf</strong></p>

<p>这个配置文件是uboot的启动参数，l0是默认启动参数，l0r是recovery启动参数，添加<em>fdtoverlays /lib/firmware/5.10.0-1012-rockchip/device-tree/rockchip/gpio.dtbo</em>指定要加载的dtbo文件。设置好路径之后重启设备，在<em>/sys/firmware/devicetree/base</em>中检查有没有对应的节点。</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">## /boot/extlinux/extlinux.conf</span>
<span class="c">##</span>
<span class="c">## IMPORTANT WARNING</span>
<span class="c">##</span>
<span class="c">## The configuration of this file is generated automatically.</span>
<span class="c">## Do not edit this file manually, use: u-boot-update</span>

default l0
menu title U-Boot menu
prompt 1
<span class="nb">timeout </span>20

label l0
        menu label Ubuntu 22.04.5 LTS 5.10.0-1012-rockchip
        linux /boot/vmlinuz-5.10.0-1012-rockchip
        initrd /boot/initrd.img-5.10.0-1012-rockchip
        fdtdir /lib/firmware/5.10.0-1012-rockchip/device-tree/
        fdtoverlays /lib/firmware/5.10.0-1012-rockchip/device-tree/rockchip/overlay/gpio.dtbo

        append <span class="nv">root</span><span class="o">=</span><span class="nv">UUID</span><span class="o">=</span>cbf6ee10-c4b9-49e9-b246-f17a818913e1 rootwait rw <span class="nv">console</span><span class="o">=</span>ttyS2,1500000 <span class="nv">console</span><span class="o">=</span>tty1 <span class="nv">cgroup_enable</span><span class="o">=</span>cpuset <span class="nv">cgroup_memory</span><span class="o">=</span>1 <span class="nv">cgroup_enable</span><span class="o">=</span>memory quiet splash plymouth.ignore-serial-consoles

label l0r
        menu label Ubuntu 22.04.5 LTS 5.10.0-1012-rockchip <span class="o">(</span>rescue target<span class="o">)</span>
        linux /boot/vmlinuz-5.10.0-1012-rockchip
        initrd /boot/initrd.img-5.10.0-1012-rockchip
        fdtdir /lib/firmware/5.10.0-1012-rockchip/device-tree/
        append <span class="nv">root</span><span class="o">=</span><span class="nv">UUID</span><span class="o">=</span>cbf6ee10-c4b9-49e9-b246-f17a818913e1 rootwait rw <span class="nv">console</span><span class="o">=</span>ttyS2,1500000 <span class="nv">console</span><span class="o">=</span>tty1 <span class="nv">cgroup_enable</span><span class="o">=</span>cpuset <span class="nv">cgroup_memory</span><span class="o">=</span>1 <span class="nv">cgroup_enable</span><span class="o">=</span>memory splash plymouth.ignore-serial-consoles single
</code></pre></div></div>

<p><strong>dtbo有时候很难给原设备树节点覆盖上</strong></p>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[Linux Driver]]></summary></entry><entry><title type="html">Cortex_m_note</title><link href="https://songyj123.github.io/cortex-m-note.html" rel="alternate" type="text/html" title="Cortex_m_note" /><published>2025-03-31T00:00:00+08:00</published><updated>2025-03-31T00:00:00+08:00</updated><id>https://songyj123.github.io/Cortex_M_note</id><content type="html" xml:base="https://songyj123.github.io/cortex-m-note.html"><![CDATA[<h1 id="cortex-m系列笔记">Cortex M系列笔记</h1>

<h2 id="1-architecture">1. Architecture</h2>

<h3 id="1-寄存器">1. 寄存器</h3>
<style>
  .highlight-row {
    background-color: yellow; /* 设置行的背景色 */
  }
  .highlight-cell {
    background-color: yellow; /* 设置单元格的背景色 */
  }
</style>

<table>
  <thead>
    <tr>
      <th>寄存器</th>
      <th>功能</th>
      <th>备注</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>r0~r12</td>
      <td>通用</td>
      <td></td>
    </tr>
    <tr>
      <td style="color: red;" class="highlight-cell">MSP PSP（r13）</td>
      <td class="highlight-cell">栈指针寄存器</td>
      <td class="highlight-cell">默认MSP 支持OS使用</td>
    </tr>
    <tr>
      <td style="color: red;" class="highlight-cell">LR（r14）</td>
      <td class="highlight-cell">返回地址寄存器</td>
      <td class="highlight-cell">函数返回 异常返回都是更新LR</td>
    </tr>
    <tr>
      <td style="color: red;" class="highlight-cell">PC（r15）</td>
      <td class="highlight-cell">PC</td>
      <td class="highlight-cell">读取返回当前指令地址+4（pipleline）</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>寄存器</th>
      <th>功能</th>
      <th>备注</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>APSR</td>
      <td>Application PSR</td>
      <td></td>
    </tr>
    <tr>
      <td>EPSR</td>
      <td>Execution PSR</td>
      <td></td>
    </tr>
    <tr>
      <td>IPSR</td>
      <td>Intertupt PSR</td>
      <td>IPSR[8:0] 9位表示全部512个异常</td>
    </tr>
    <tr>
      <td>CONTROL</td>
      <td>控制寄存器</td>
      <td></td>
    </tr>
  </tbody>
</table>

<p><strong>cm4</strong>就是<strong>cm3</strong>加上一个<strong>DSP指令</strong>再加<strong>FPU</strong></p>

<p><strong>cm3</strong>有两种模式<strong>Thread模式</strong>和<strong>Handler模式</strong></p>

<h3 id="2函数调用规则aapcs">2.函数调用规则AAPCS</h3>

<p><img src="https://i.imgur.com/JTLXmqo.png" alt="" /></p>

<h2 id="2异常和中断">2.异常和中断</h2>

<h3 id="1中断向量表">1.中断向量表</h3>

<style>
  .highlight-row {
    background-color: yellow; /* 设置行的背景色 */
  }
  .highlight-cell {
    background-color: lightblue; /* 设置单元格的背景色 */
  }
</style>

<table>
  <tr>
    <th class="highlight-cell">VTOR</th>
    <th>Exception Number</th>
    <th>Armv6-M &lt;Priority&gt;</th>
    <th>Armv7-M</th>
    <th>Armv8-M</th>
  </tr>
  <tr>
    <td>0x0000</td>
    <td>0</td>
    <td style="color: green;">MSP</td>
    <td style="color: green;">MSP</td>
    <td style="color: green;">MSP</td>
  </tr>
  <tr>
    <td>0x0004</td>
    <td>1</td>
    <td>Reset&lt;-3&gt;</td>
    <td>Reset&lt;-3&gt;</td>
    <td>Reset&lt;-4&gt;</td>
  </tr>
  <tr>
    <td>0x0008</td>
    <td>2</td>
    <td>NMl&lt;-2;&gt;</td>
    <td>NMI&lt;-2&gt;</td>
    <td>NMl&lt;-2&gt;</td>
  </tr>
  <tr>
    <td>0x0012</td>
    <td>3</td>
    <td>HardFault&lt;-1&gt;</td>
    <td>HardFault&lt;-1&gt;</td>
    <td style="color: red;">HardFault&lt;-1&gt;<br />Secure HF(NS=0)&lt;-1&gt;<br />Secure HF(NS=1)&lt;-3&gt;</td>
  </tr>
  <tr>
    <td>0x0016</td>
    <td>4</td>
    <td></td>
    <td style="color: red;">MemManage</td>
    <td>MemManage</td>
  </tr>
  <tr>
    <td>0x0020</td>
    <td>5</td>
    <td></td>
    <td style="color: red;">BusFault</td>
    <td>BusFault</td>
  </tr>
  <tr>
    <td>0x0024</td>
    <td>6</td>
    <td></td>
    <td style="color: red;">UsageFault</td>
    <td>UsageFault</td>
  </tr>
  <tr>
    <td>0x0028</td>
    <td>7</td>
    <td></td>
    <td></td>
    <td style="color: red;">SecureFault</td>
  </tr>
  <tr>
    <td>0x0032</td>
    <td>8</td>
    <td></td>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td>0x0036</td>
    <td>9</td>
    <td></td>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td>0x0040</td>
    <td>10</td>
    <td></td>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td>0x0044</td>
    <td>11</td>
    <td>SVCall &lt;configurable&gt;</td>
    <td>SVCall</td>
    <td>SVCall</td>
  </tr>
  <tr>
    <td>0x0048</td>
    <td>12</td>
    <td></td>
    <td style="color: red;">DebugMonitor</td>
    <td>DebugMonitor</td>
  </tr>
  <tr>
    <td>0x0052</td>
    <td>13</td>
    <td></td>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td>0x0056</td>
    <td>14</td>
    <td>PendSV</td>
    <td>PendSV</td>
    <td>PendSV</td>
  </tr>
  <tr>
    <td>0x0060</td>
    <td>15</td>
    <td>SysTick, optional</td>
    <td style="color: red;">SysTick</td>
    <td>SysTick</td>
  </tr>
  <tr>
    <td>0x0064</td>
    <td>16</td>
    <td>External Interrupt(0)</td>
    <td>External Interrupt(0)</td>
    <td>External Interrupt(0)</td>
  </tr>
  <tr>
    <td>0x0068</td>
    <td>17</td>
    <td>External Interrupt(1)</td>
    <td>External Interrupt(1)</td>
    <td>External Interrupt(1)</td>
  </tr>
  <tr>
    <td>0x0072</td>
    <td>18</td>
    <td>External Interrupt(2)</td>
    <td>External Interrupt(2)</td>
    <td>External Interrupt(2)</td>
  </tr>
  <tr>
    <td>...</td>
    <td>...</td>
    <td>...</td>
    <td>...</td>
    <td>...</td>
  </tr>
  <tr>
    <td>0x2040</td>
    <td>510</td>
    <td>External Interrupt(494)</td>
    <td>External Interrupt(494)</td>
    <td>External Interrupt(494)</td>
  </tr>
  <tr>
    <td>0x2044</td>
    <td>511</td>
    <td>External Interrupt(495)</td>
    <td>External Interrupt(495)</td>
    <td>External Interrupt(495)</td>
  </tr>
</table>
<p>前16个是异常，后面(512-16)个是中断，各芯片厂自己的外设就是设置在后面的(512-16)，比如串口什么的。</p>

<h3 id="2异常处理流程">2.异常处理流程</h3>

<p>cm3在异常处理中的一大特色就是可以直接使用c语言来编写exception_handler(),在cortex-A或者是riscv中，异常的返回都需要使用特殊的指令<strong>eret sret mret</strong></p>

<p>不同与其他架构例如cortex-A，m3会使用硬件将栈帧(stack frame)压栈而不需要手动压栈，这也是为什么我们在cm相关的库文件中没有看见过压栈操作的原因。cm3硬件压栈只保存八个寄存器，剩余的callee保存的，所以编译器在编译c代码时会根据这个procedure协议生成保存剩余寄存器的汇编代码。</p>

<ul>
  <li><strong><em>cm3栈帧结构：</em></strong></li>
</ul>

<p>cm3的栈帧可以设置8byte对齐也就是他说的<strong>double word align</strong>，可以注意到cm3的栈帧结构就是8byte：</p>

<p><img src="https://i.imgur.com/eN2VLrW.png" alt="" /></p>

<p>注意下图的<strong>padding</strong>，栈从高地址向低地址生长，这个padding是在栈帧之前的，因为在入栈时栈可能并没有8byte对齐。</p>

<p><img src="https://i.imgur.com/c8Fl4X1.png" alt="" /></p>

<ul>
  <li><strong><em>cm3硬件压栈顺序：</em></strong></li>
</ul>

<pre><code class="language-c=">//和栈帧的位置并不相同
PC -&gt; PSR -&gt; R0 -&gt; R1 -&gt; R2 -&gt; R3 -&gt; R12 -&gt; LR
</code></pre>

<p>压栈顺序和栈帧的顺序是不一样的，首先会压入PC和PSR，这样做可以更快开始<strong>vector fetch</strong>，<strong>cm3是哈佛结构</strong>，数据总线和指令总线是分开的这样一来压栈和取值的操作可以并行进行</p>

<p>MSP和PSP的切换是通过设置<strong>CONTROL</strong>寄存器的<strong>bit1</strong>来完成的(0使用MSP 1使用PSP)，值得一提的是<strong>中断嵌套时的压栈必须使用MSP</strong></p>

<h2 id="3-cortex-m3-启动">3. cortex-m3 启动</h2>

<p>###</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">BOOT0</th>
      <th style="text-align: center">BOOT1</th>
      <th style="text-align: center">启动介质</th>
      <th style="text-align: center">Column 3</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">x</td>
      <td style="text-align: center">0</td>
      <td style="text-align: center">SRAM</td>
      <td style="text-align: center">0x2000 0000</td>
    </tr>
    <tr>
      <td style="text-align: center">0</td>
      <td style="text-align: center">1</td>
      <td style="text-align: center">Flash</td>
      <td style="text-align: center">0x0800 0000</td>
    </tr>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: center">1</td>
      <td style="text-align: center">System ROM</td>
      <td style="text-align: center">0x1FFF B000</td>
    </tr>
  </tbody>
</table>

<h3 id="flash">Flash</h3>

<p>mcu内置的Flash一般是nor flash可以任意读写，所以stm32就使用的这类Flash。
nand flash是块读写，不能任意读写。这类芯片需要将所有拷贝到RAM。</p>

<p><a href="https://www.51zxw.net/TechArticleDetails.aspx?zid=471&amp;id=2196">存储区别</a></p>

<h3 id="1-从flash启动流程">1 从flash启动流程</h3>

<p><em>前提：</em>
硬件可以将<strong>FLASH</strong>以及<strong>System ROM</strong>映射到<strong>0x00000000</strong>开始的一段空间上，所以在设置了<strong>boot pin</strong>脚电平之后，<strong>FLASH</strong>或者<strong>SYS ROM</strong>当中的前一小段数据就可以从<strong>0x00000000</strong>访问了。</p>

<ol>
  <li>
    <p>上电之后硬件复位会自动进行如下操作：
从<strong>0x00000000</strong>读取数据写入<strong>MSP</strong>(main stack pointer)
在<strong>0x00000000</strong>存放<strong>stack</strong>地址
从<strong>0x00000004</strong>读取数据写入<strong>PC</strong>(program counter)
在<strong>0x00000004</strong>存放<strong>Reset_Handler()</strong> 的入口地址</p>
  </li>
  <li>
    <p><strong>Reset_Handler()</strong> 做的事情是：</p>
    <ol>
      <li>copy data from flash to sram</li>
      <li>initialize .data .bss in RAM</li>
      <li>bring up main()</li>
    </ol>
  </li>
</ol>

<p><strong>为什么要将数据拷贝到RAM中去呢？</strong></p>

<p>虽然单片机上的flash也可以执行和读写程序，但是SRAM比起flash更快读写更flexible，所以更倾向将需要大量改动的数据存储在SRAM中，如果SRAM足够大，我们甚至想把代码也copy到SRAM中去执行就像A架构的芯片使用DDR一样。</p>

<p><strong>为什么拷贝的数据只包含.data？</strong></p>

<p>.bss不需要copy只需要空出来赋值0，因为这里面都是没有赋值的变量，.bss段甚至不再bin文件中，这也是.bss段存在的意义，减小bin文件的大小，减少需要搬移的数据。
.rodata和.text不会改动所以不用拷贝</p>

<p><strong>链接的时候我们不就已经确定了程序的加载位置吗？如果改变了数据段的位置，程序又怎么能正确的找到重定位之后的数据段呢？</strong></p>

<p>linkerscript中每一个输出段都有两个地址：<strong>LMA</strong>(load memory addr)和<strong>VMA</strong>(virtual memory addr)，下图中<strong>RAM</strong>是<strong>VMA</strong>，flash是<strong>LMA</strong>。</p>

<pre><code class="language-ld=">/* 
程序在执行的时候知道data段是存放在RAM当中的所以链接的地址都是RAM的
但是ld加载器把data段初始值是放在FLASH当中的，所以startup需要copy data from FLASH to RAM
*/
    .data  : AT ( _sidata )
    {
	    . = ALIGN(4);
        /* This is used by the startup in order to initialize the .data secion */
        _sdata = . ;
        
        *(.data)
        *(.data.*)

	    . = ALIGN(4);
	    /* This is used by the startup in order to initialize the .data secion */
   	 _edata = . ;
    } &gt;RAM
</code></pre>

<details>
  <summary>完整的代码加载到片上flash数据加载到sram的ld脚本</summary>

```ld=
//需要将数据段copy到sram
ENTRY(Reset_Handler)

/* Highest address of the user mode stack */
_estack = 0x20005000;    /* end of 20K RAM */

/* Generate a link error if heap and stack don't fit into RAM */
_Min_Heap_Size = 0;      /* required amount of heap  */
_Min_Stack_Size = 0x100; /* required amount of stack */

/* Specify the memory areas */
MEMORY
{
  FLASH (rx)      : ORIGIN = 0x08000000, LENGTH = 128K
  RAM (xrw)       : ORIGIN = 0x20000000, LENGTH = 20K
  MEMORY_B1 (rx)  : ORIGIN = 0x60000000, LENGTH = 0K
}

SECTIONS
{
  /* The startup code goes first into FLASH */
  .isr_vector :
  {
    . = ALIGN(4);
    KEEP(*(.isr_vector)) /* Startup code */
    . = ALIGN(4);
  } &gt;FLASH

  /* The program code and other data goes into FLASH */
  .text :
  {
    . = ALIGN(4);
    *(.text)           /* .text sections (code) */
    *(.text*)          /* .text* sections (code) */
    *(.rodata)         /* .rodata sections (constants, strings, etc.) */
    *(.rodata*)        /* .rodata* sections (constants, strings, etc.) */
    *(.glue_7)         /* glue arm to thumb code */
    *(.glue_7t)        /* glue thumb to arm code */

    KEEP (*(.init))
    KEEP (*(.fini))

    . = ALIGN(4);
    _etext = .;        /* define a global symbols at end of code */
  } &gt;FLASH

  /* used by the startup to initialize data */
  _sidata = .;

  /* Initialized data sections goes into RAM, load LMA copy after code */
  .data : AT ( _sidata )
  {
    . = ALIGN(4);
    _sdata = .;        /* create a global symbol at data start */
    *(.data)           /* .data sections */
    *(.data*)          /* .data* sections */

    . = ALIGN(4);
    _edata = .;        /* define a global symbol at data end */
  } &gt;RAM

  /* Uninitialized data section */
  . = ALIGN(4);
  .bss :
  {
    /* This is used by the startup in order to initialize the .bss secion */
    _sbss = .;         /* define a global symbol at bss start */
    __bss_start__ = _sbss;
    *(.bss)
    *(.bss*)
    *(COMMON)

    . = ALIGN(4);
    _ebss = .;         /* define a global symbol at bss end */
    __bss_end__ = _ebss;
  } &gt;RAM

  PROVIDE ( end = _ebss );
  PROVIDE ( _end = _ebss );

  /* User_heap_stack section, used to check that there is enough RAM left */
  ._user_heap_stack :
  {
    . = ALIGN(4);
    . = . + _Min_Heap_Size;
    . = . + _Min_Stack_Size;
    . = ALIGN(4);
  } &gt;RAM
}
```
</details>

<h3 id="2-从外部flash启动流程">2. 从外部flash启动流程</h3>

<p>即使需要从外部flash启动最开始也是在内部flash开始的：</p>

<ol>
  <li>在内部flash初始化中断向量表，初始化外部flash（例如spi flash需要初始化spi读写）</li>
  <li>重定位中断向量表到sram</li>
  <li>从外部flash拷贝代码到sram</li>
</ol>

<h3 id="3-中断向量表重定位">3. 中断向量表重定位</h3>

<p>单片机程序做的第一件事情就是设置中断向量表，因为有NMI（non Maskable interrupt）可能会在上电之后马上发生。cm3和cm4的中断向量表中存放的是各个中断处理函数的起始地址</p>

<p>cm3和cm4提供vtor（vector table offset reg）reset value是0写vtor之前需要先将向量表搬移到指定位置</p>
<pre><code class="language-c=">#define HW32_REG(ADDRESS) (*((volatile unsigned long *)(ADDRESS)))
#define VTOR_NEW_ADDR 0x20000000

for (i=0;i&lt;48;i++)
{    
    // Assume maximum number of exception is 48
    // Copy each vector table entry from flash to SRAM
    HW32_REG((VTOR_NEW_ADDR + (i&lt;&lt;2))) = HW32_REG((i&lt;&lt;2));
}

__DMB(); // Data Memory Barrier to ensure write to memory is completed

SCB-&gt;VTOR = VTOR_NEW_ADDR; // Set VTOR to the new vector table location

__DSB(); // Data Synchronization Barrier to ensure all subsequence instructions use the new configuation
</code></pre>

<p>注意即使是单核心的cpu也要考虑内存屏障，因为cm3cm4都有自己的icache dcache，而且是乱序执行（out of order execution）的</p>

<p>__DMB();确保没有乱序
__DSB();确保缓存刷新了</p>

<h4 id="为什么需要中断向量表重定位我感觉应该第二种情况居多">为什么需要中断向量表重定位？我感觉应该第二种情况居多</h4>

<p><img src="https://i.imgur.com/x6KGL4u.png" alt="" /></p>

<p><a href="https://stackoverflow.com/questions/70633100/linker-copying-data-from-flash-memory-to-ram">data copy to sram</a></p>

<h2 id="4-os-support-feature">4. OS support feature</h2>

<h3 id="1svc异常">1.SVC异常</h3>

<p>类似于riscv中的ecall指令，svc指令也可以作为引起syscall</p>

<pre><code class="language-asm=">SVC #0x3 ; Call SVC function 3
</code></pre>

<ul>
  <li>
    <h4 id="为什么不使用普通的软件中断">为什么不使用普通的软件中断？</h4>
    <blockquote>
      <p>Although it is possible to trigger an interrupt using software by writing to NVIC (e.g., Software Trigger Interrupt Register, NVIC-&gt;STIR), the behavior is a bit different: Interrupts are imprecise. It means that a number of instructions could be executed after setting the pending status but before the interrupt actually takes place. On the other hand, SVC is precise. The SVC handler must execute after the SVC instruction, except when another higher-priority exception arrives at the same time.</p>
    </blockquote>
  </li>
  <li>
    <h4 id="为什么使用pendsvc作为context-swtch的中断">为什么使用PendSVC作为context swtch的中断？</h4>
  </li>
</ul>

<p>和svc中断不一样的是PendSV不是准确的，PendSV有最低的优先级，这一点保证他在所有的中断执行完之后才会执行，否则中断的执行可能会被context swtch打断并延迟执行。</p>

<p><img src="https://i.imgur.com/vApw1u1.png" alt="" /></p>

<p>使用PendSV来完成context swtch的例子：由下图可见，sysytick虽然打断了中断的执行但是他只是置位PendSV之后立即返回中断继续执行，中断执行完毕之后才开始context swtch最大程度保证实时性</p>

<p><img src="https://i.imgur.com/bCvpJgl.png" alt="" /></p>

<p><strong>PendSV</strong>还被用来实现中断的上半段和下半段，上半段是<strong>time critical</strong>十分紧要的操作在<strong>time critical</strong>的操作完成之后只要置位<strong>PendSV</strong>将下半段不是<strong>time critical</strong>就交给<strong>PendSV</strong>来完成</p>

<p><img src="https://i.imgur.com/24LMFqV.png" alt="" /></p>

<h2 id="5-baremetal示例">5. Baremetal示例</h2>

<p>Linux环境下baremetal编程需要以下工具链，openocd充当调试器上位机程序</p>

<ul>
  <li>软件： arm-none-eabi-gcc，openocd，gdb-multiarch</li>
  <li>硬件： CMSIS-DAP调试器，Air32F103</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 项目文件结构</span>
├── linkscript.ld
├── main.c
├── Makefile
├── openocd.cfg
└── startup.c
</code></pre></div></div>

<p>用c语言完成启动文件的编写，我感觉主要有几点需要注意。</p>

<ol>
  <li>作为简单的验证程序，其实不用將中断向量表完整写出，但是至少包含SP地址以及Reset_Handler</li>
  <li>如果选择从link脚本中读取SP的值，C语言语法的使用</li>
  <li>link脚本中注意SRAM和Flash的实际大小，注意代码运行地址和放置地址</li>
  <li>link脚本中使用ALIGN()对齐,段结束的symbol应该在ALIGN()后</li>
</ol>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// statrup.c</span>
<span class="k">extern</span> <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">_estack</span><span class="p">;</span>
<span class="k">extern</span> <span class="kt">int</span> <span class="nf">main</span><span class="p">();</span>

<span class="kt">void</span> <span class="nf">Reset_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">NMI_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">Hardfault_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">Memfault_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">Busfault_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">Usagefault_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">SVCall_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">DebugMon_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">PendSV_Handler</span><span class="p">();</span>
<span class="kt">void</span> <span class="nf">Systick_Handler</span><span class="p">();</span>

<span class="n">__attribute__</span> <span class="p">((</span><span class="n">section</span><span class="p">(</span><span class="s">".vectors"</span><span class="p">),</span> <span class="n">used</span><span class="p">))</span>
<span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">vector_table</span><span class="p">[]</span> <span class="o">=</span> 
<span class="p">{</span> 
	<span class="p">(</span><span class="kt">unsigned</span> <span class="kt">int</span><span class="p">)</span><span class="o">&amp;</span><span class="n">_estack</span><span class="p">,</span>
	<span class="n">Reset_Handler</span><span class="p">,</span>
	<span class="n">NMI_Handler</span><span class="p">,</span>
	<span class="n">Hardfault_Handler</span><span class="p">,</span>
	<span class="n">Memfault_Handler</span><span class="p">,</span>
	<span class="n">Busfault_Handler</span><span class="p">,</span>
	<span class="n">Usagefault_Handler</span><span class="p">,</span>
	<span class="mi">0</span><span class="p">,</span>
	<span class="mi">0</span><span class="p">,</span>
	<span class="mi">0</span><span class="p">,</span>
	<span class="mi">0</span><span class="p">,</span>
	<span class="n">SVCall_Handler</span><span class="p">,</span>
	<span class="n">DebugMon_Handler</span><span class="p">,</span>
	<span class="mi">0</span><span class="p">,</span>
	<span class="n">PendSV_Handler</span><span class="p">,</span>
	<span class="n">Systick_Handler</span><span class="p">,</span>
<span class="p">};</span>

<span class="kt">void</span> <span class="nf">Reset_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span> <span class="n">i</span><span class="o">&lt;=</span><span class="mi">5</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span>
	<span class="p">{</span>
		<span class="p">;</span>
	<span class="p">}</span>
	<span class="n">main</span><span class="p">();</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">NMI_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">Hardfault_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">Memfault_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">Busfault_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">Usagefault_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">SVCall_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">DebugMon_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">PendSV_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">void</span> <span class="nf">Systick_Handler</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>

<span class="c1">// main.c</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
        <span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* linkscript.ld */</span>
<span class="n">ENTRY</span><span class="p">(</span><span class="n">Reset_Handler</span><span class="p">)</span>

<span class="n">MEMORY</span> <span class="p">{</span>
  <span class="n">flash</span><span class="p">(</span><span class="n">rx</span><span class="p">)</span>  <span class="o">:</span> <span class="n">ORIGIN</span> <span class="o">=</span> <span class="mh">0x08000000</span><span class="p">,</span> <span class="n">LENGTH</span> <span class="o">=</span> <span class="mi">128</span><span class="n">K</span>
  <span class="n">sram</span><span class="p">(</span><span class="n">rwx</span><span class="p">)</span> <span class="o">:</span> <span class="n">ORIGIN</span> <span class="o">=</span> <span class="mh">0x20000000</span><span class="p">,</span> <span class="n">LENGTH</span> <span class="o">=</span> <span class="mi">32</span><span class="n">K</span>  <span class="cm">/* remaining 64k in a separate address space */</span>
<span class="p">}</span>
<span class="n">_estack</span>     <span class="o">=</span> <span class="n">ORIGIN</span><span class="p">(</span><span class="n">sram</span><span class="p">)</span> <span class="o">+</span> <span class="n">LENGTH</span><span class="p">(</span><span class="n">sram</span><span class="p">);</span>    <span class="cm">/* stack points to end of SRAM */</span>

<span class="n">SECTIONS</span> <span class="p">{</span>
  <span class="p">.</span> <span class="o">=</span> <span class="mh">0x08000000</span><span class="p">;</span>
  <span class="p">.</span><span class="n">vectors</span> <span class="o">:</span>  
  <span class="p">{</span> 
	  <span class="n">KEEP</span><span class="p">(</span><span class="o">*</span><span class="p">(.</span><span class="n">vectors</span><span class="p">))</span> 
  <span class="p">}</span> <span class="o">&gt;</span> <span class="n">flash</span>
  <span class="p">.</span><span class="n">text</span> <span class="o">:</span> 
  <span class="p">{</span> 
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
	  <span class="o">*</span><span class="p">(.</span><span class="n">text</span><span class="p">)</span> 
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
  <span class="p">}</span> <span class="o">&gt;</span> <span class="n">flash</span>
  <span class="p">.</span><span class="n">rodata</span> <span class="o">:</span> 
  <span class="p">{</span> 
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
	  <span class="o">*</span><span class="p">(.</span><span class="n">rodata</span><span class="p">)</span> 
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
  <span class="p">}</span> <span class="o">&gt;</span> <span class="n">flash</span>

  <span class="p">.</span><span class="n">data</span> <span class="o">:</span>
  <span class="p">{</span>
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
	  <span class="n">_sdata</span> <span class="o">=</span> <span class="p">.;</span>   <span class="cm">/* .data section start */</span>
	  <span class="o">*</span><span class="p">(.</span><span class="n">data</span><span class="p">)</span>
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
	  <span class="n">_edata</span> <span class="o">=</span> <span class="p">.;</span>  <span class="cm">/* .data section end */</span>
  <span class="p">}</span> <span class="o">&gt;</span> <span class="n">sram</span> <span class="n">AT</span> <span class="o">&gt;</span> <span class="n">flash</span>

  <span class="p">.</span><span class="n">bss</span> <span class="o">:</span> <span class="p">{</span>
      <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
      <span class="n">_sbss</span> <span class="o">=</span> <span class="p">.;</span>              <span class="cm">/* .bss section start */</span>
      <span class="o">*</span><span class="p">(.</span><span class="n">bss</span><span class="p">)</span>
      <span class="o">*</span><span class="p">(</span><span class="n">COMMON</span><span class="p">)</span>
	  <span class="p">.</span> <span class="o">=</span> <span class="n">ALIGN</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
	  <span class="n">_ebss</span> <span class="o">=</span> <span class="p">.;</span>              <span class="cm">/* .bss section end */</span>
  <span class="p">}</span> <span class="o">&gt;</span> <span class="n">sram</span>
<span class="p">}</span>
</code></pre></div></div>

<p>使用Makefile来自动化编译：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>all: firmware.bin

startup.o: startup.c
        arm-none-eabi-gcc -c -mcpu=cortex-m3 -g startup.c -o startup.o

main.o: main.c
        arm-none-eabi-gcc -c -mcpu=cortex-m3 -g main.c -o main.o

firmware.elf: startup.o main.o
        arm-none-eabi-ld -T linkscript.ld -o firmware.elf startup.o main.o

firmware.bin: firmware.elf
        arm-none-eabi-objcopy -O binary firmware.elf firmware.bin

dbg: firmware.bin
        qemu-system-arm -S -M stm32vldiscovery -kernel firmware.bin -gdb tcp::3333 -nographic -monitor telnet:127.0.0.1:1234,server,nowait

clean:
        rm -rf firmware.* startup.o main.o
</code></pre></div></div>

<p>在实际上板测试之前可以使用QEMU来进行仿真，如果仿真通不过就别上板了。（注意qemu支持的SRAM和Flash大小应该和实际使用的板子有区别，需要修改link脚本）</p>

<p>上板调试，gdb的一些默认重复的命令可以提前写入.gdbinit文件中，之后启动gdb可以自动执行。</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 在窗口1打开openocd</span>
<span class="c"># openocd会自动读取该文件夹中的openocd.cfg</span>
<span class="c"># openocd.cfg里面主要指定了调试器和硬件的配置文件</span>
<span class="nv">$ </span><span class="nb">cat</span> ./openocd.cfg 
<span class="nb">source</span> <span class="o">[</span>find interface/cmsis-dap.cfg]
<span class="nb">source</span> <span class="o">[</span>find target/stm32f1x.cfg]

<span class="c"># 可以从输出中看到已经和板子识别上，并且在3333端口监听gdb链接</span>
<span class="nv">$ </span>openocd
Open On-Chip Debugger 0.12.0
Licensed under GNU GPL v2
For bug reports, <span class="nb">read
	</span>http://openocd.org/doc/doxygen/bugs.html
Info : auto-selecting first available session transport <span class="s2">"swd"</span><span class="nb">.</span> To override use <span class="s1">'transport select &lt;transport&gt;'</span><span class="nb">.</span>
Info : Listening on port 6666 <span class="k">for </span>tcl connections
Info : Listening on port 4444 <span class="k">for </span>telnet connections
Info : Using CMSIS-DAPv2 interface with VID:PID<span class="o">=</span>0x0d28:0x0204, <span class="nv">serial</span><span class="o">=</span>000000800671ff525154887767033308a5a5a5a597969908
Info : CMSIS-DAP: SWD supported
Info : CMSIS-DAP: Atomic commands supported
Info : CMSIS-DAP: FW Version <span class="o">=</span> 2.2.3R
Info : CMSIS-DAP: Serial# <span class="o">=</span> C36E08E4C284
Info : CMSIS-DAP: Interface Initialised <span class="o">(</span>SWD<span class="o">)</span>
Info : SWCLK/TCK <span class="o">=</span> 1 SWDIO/TMS <span class="o">=</span> 1 TDI <span class="o">=</span> 0 TDO <span class="o">=</span> 0 nTRST <span class="o">=</span> 0 nRESET <span class="o">=</span> 1
Info : CMSIS-DAP: Interface ready
Info : clock speed 1000000 kHz
Info : SWD DPIDR 0x2ba01477
Info : <span class="o">[</span>stm32f1x.cpu] Cortex-M3 r2p0 processor detected
Info : <span class="o">[</span>stm32f1x.cpu] target has 6 breakpoints, 4 watchpoints
Info : starting gdb server <span class="k">for </span>stm32f1x.cpu on 3333
Info : Listening on port 3333 <span class="k">for </span>gdb connections

<span class="c"># 在窗口2打开gdb-multiarch并读入elf文件</span>
<span class="c"># 在gdb中输入 target remote localhost:3333</span>
<span class="c"># 输入 monitor reset init 进行复位</span>
<span class="nv">$ </span>gdb-multiarch ./firmware.elf 
GNU gdb <span class="o">(</span>Ubuntu 15.0.50.20240403-0ubuntu1<span class="o">)</span> 15.0.50.20240403-git
Copyright <span class="o">(</span>C<span class="o">)</span> 2024 Free Software Foundation, Inc.
License GPLv3+: GNU GPL version 3 or later &lt;http://gnu.org/licenses/gpl.html&gt;
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.
Type <span class="s2">"show copying"</span> and <span class="s2">"show warranty"</span> <span class="k">for </span>details.
This GDB was configured as <span class="s2">"x86_64-linux-gnu"</span><span class="nb">.</span>
Type <span class="s2">"show configuration"</span> <span class="k">for </span>configuration details.
For bug reporting instructions, please see:
&lt;https://www.gnu.org/software/gdb/bugs/&gt;.
Find the GDB manual and other documentation resources online at:
    &lt;http://www.gnu.org/software/gdb/documentation/&gt;.

For <span class="nb">help</span>, <span class="nb">type</span> <span class="s2">"help"</span><span class="nb">.</span>
Type <span class="s2">"apropos word"</span> to search <span class="k">for </span>commands related to <span class="s2">"word"</span>...
Reading symbols from ./firmware.elf...
<span class="o">(</span>gdb<span class="o">)</span> target remote localhost:3333
Remote debugging using localhost:3333
main <span class="o">()</span> at main.c:3
3		<span class="k">while</span><span class="o">(</span>1<span class="o">)</span><span class="p">;</span>
<span class="o">(</span>gdb<span class="o">)</span> monitor reset init
<span class="o">[</span>stm32f1x.cpu] halted due to debug-request, current mode: Thread 
xPSR: 0x01000000 pc: 0x0800012c msp: 0x20008000
<span class="o">(</span>gdb<span class="o">)</span> si
0x0800012e <span class="k">in </span>Reset_Handler <span class="o">()</span> at startup.c:96
</code></pre></div></div>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[Cortex M系列笔记]]></summary></entry><entry><title type="html">Linux_graphic_stack</title><link href="https://songyj123.github.io/linux-graphic-stack.html" rel="alternate" type="text/html" title="Linux_graphic_stack" /><published>2025-01-01T00:00:00+08:00</published><updated>2025-01-01T00:00:00+08:00</updated><id>https://songyj123.github.io/Linux_graphic_stack</id><content type="html" xml:base="https://songyj123.github.io/linux-graphic-stack.html"><![CDATA[<p>[TOC]</p>

<h1 id="linux---graphic-stack">Linux - Graphic stack</h1>

<p><img src="https://i-blog.csdnimg.cn/blog_migrate/b8530def6ef20500472895d5ff25f45b.png" alt="img" /></p>

<p>一个具有显存的虚拟GPU和ARMv8 CPU，有一个DMA可以在RAM和GPU显存之间来回搬运数据</p>

<h2 id="0x00-env-setup">0x00 ENV setup</h2>

<h3 id="1-编译kernel">1. 编译kernel</h3>

<p>随便选择一版Linux源码</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 使用/arch/arm64/中的defconfig编译内核</span>
<span class="c"># 这是一个aarch64通用的defconfig</span>
<span class="nb">export </span><span class="nv">ARCH</span><span class="o">=</span>arm64
<span class="nb">export </span><span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-linux-gnu-
make defconfig
make <span class="nt">-j12</span>

<span class="c"># 如果没有export</span>
<span class="c"># 需要两次命令中都有 ARCH=arm64 CROSS_COMPILE=aarch64-buildroot-linux-gnu-</span>
<span class="c"># make ARCH=arm64 defconfig CROSS_COMPILE=aarch64-buildroot-linux-gnu-后直接make -j12会需要重复选择各种config</span>
make <span class="nv">ARCH</span><span class="o">=</span>arm64 defconfig <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-buildroot-linux-gnu-
make <span class="nv">ARCH</span><span class="o">=</span>arm64 <span class="nv">CROSS_COMPILE</span><span class="o">=</span>aarch64-buildroot-linux-gnu- <span class="nt">-j12</span>
</code></pre></div></div>

<h3 id="2-编译qemu-system-aarch64">2. 编译qemu-system-aarch64</h3>

<p>随便选择一版QEMU源码，尽量將./configure –target-list=aarch64-softmmu之后输出的各种支持都装上主要是virtio 图形 网络的一些支持包</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./configure <span class="nt">--target-list</span><span class="o">=</span>aarch64-softmmu
<span class="nb">cd</span> ./build
make <span class="nt">-j12</span> qemu-system-aarch64
</code></pre></div></div>

<p>需要用到virtio相关</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qemu-system-aarch64 <span class="se">\</span>
    <span class="nt">-M</span> virt <span class="se">\</span>
    <span class="nt">-cpu</span> cortex-a53 <span class="se">\</span>
    <span class="nt">-kernel</span> /home/songyj/embedded/buildroot-2024.08.2/output/images/Image <span class="se">\</span>
    <span class="nt">-drive</span> <span class="nv">file</span><span class="o">=</span>/home/songyj/embedded/buildroot-2024.08.2/output/images/rootfs.ext4,format<span class="o">=</span>raw <span class="se">\</span>
    <span class="nt">-append</span> <span class="s2">"root=/dev/vda nokaslr ip=dhcp"</span> <span class="se">\</span>
    <span class="nt">-device</span> virtio-gpu-gl <span class="se">\</span>
    <span class="nt">-display</span> gtk,gl<span class="o">=</span>core <span class="se">\</span>
    <span class="nt">-device</span> virtio-net-pci,netdev<span class="o">=</span>net0 <span class="se">\</span>
    <span class="nt">-serial</span> mon:stdio <span class="se">\ </span><span class="c"># 串口输出重定向到当前的终端</span>
    <span class="c"># gdb调试时加入下面的switch 会停止在kernel加载的地方</span>
    <span class="c"># -s -S</span>
</code></pre></div></div>

<h3 id="3-buildroot准备根文件系统">3. buildroot准备根文件系统</h3>

<h3 id="4-环境验证">4. 环境验证</h3>

<h3 id="5-命令">5. 命令</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># qemu上linux获取交叉编译的应用程序</span>
tftp 10.0.2.2 <span class="nt">-c</span> get drm_test

<span class="c"># gdb调试kernel panic</span>
gdb-multiarch vmlinux
<span class="c"># 在gdb内部使用</span>
target remote localhost：1234
b 错误函数
</code></pre></div></div>

<h3 id="6-问题">6. 问题</h3>

<ul>
  <li><strong><em>nvim clangd返回-32001 Invalid AST</em></strong></li>
</ul>

<p>在linux项目顶层添加.clangd文件，内容如下：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CompileFlags:
  Remove: <span class="o">[</span><span class="nt">-march</span><span class="o">=</span><span class="k">*</span>, <span class="nt">-mabi</span><span class="o">=</span><span class="k">*</span><span class="o">]</span>
</code></pre></div></div>

<p>参见https://github.com/clangd/clangd/issues/1582</p>

<ul>
  <li><strong><em>nvim clangd突然无法跳转 并显示一大堆错误</em></strong></li>
</ul>

<p>应该正确使用clangd生成脚本，compile_commands.json生成在源码根目录</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># /home/songyj/embedded/linux
python3 ./scripts/clang-tools/gen_compile_commands.py
</code></pre></div></div>

<p>随后打开文件，让clangd更新条目</p>

<p>参见https://blog.csdn.net/Roger_Spencer/article/details/135325340</p>

<ul>
  <li><strong><em>QEMU串口终端不能滚动</em></strong></li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 將串口输出重定向到当前的终端</span>
<span class="nt">-serial</span> mon:stdio 
</code></pre></div></div>

<p>https://fadeevab.com/how-to-setup-qemu-output-to-console-and-automate-using-shell-script/</p>

<h2 id="0x01-drm">0x01 DRM</h2>

<blockquote>
  <p>The scope of DRM has been expanded over the years to cover more functionality previously handled by user-space programs, such as framebuffer managing and <a href="https://en.wikipedia.org/wiki/Mode_setting">mode setting</a>, memory-sharing objects and memory synchronization.</p>
</blockquote>

<p>DRM最开始是为应用程序使用GPU而设计的子系统，但是随着DRM子系统的发展，加入了越来越多的模块比如：KMS和GEM</p>

<ul>
  <li><strong>KMS（kernel mode-setting）</strong> a combination of <a href="https://en.wikipedia.org/wiki/Screen_resolution">screen resolution</a>, <a href="https://en.wikipedia.org/wiki/Color_depth">color depth</a> and <a href="https://en.wikipedia.org/wiki/Refresh_rate">refresh rate</a> 曾今在user space完成设置，现在在kernel space解决多进程并发等问题。</li>
</ul>

<blockquote>
  <p>To avoid breaking backwards compatibility of the DRM API, Kernel Mode-Setting is provided as an additional <em>driver feature</em> of certain DRM drivers.</p>
</blockquote>

<p>为了兼容之前的DRM驱动，维持一致性，KMS作为一个属性被嵌入到DRM driver中，如果一个DRM driver支持KMS操作，需要置位<strong>DRIVER_MODESET</strong>，这些支持KMS的DRM驱动被叫做KMS driver和legacy的DRM驱动区别开，KMS驱动的支持程度非常高，哪怕这个驱动中没有实现DRM操作只有KMS操作（比如在不支持3D rendering的硬件上）</p>

<p><img src="https://i-blog.csdnimg.cn/blog_migrate/c0131052a585d4c497f612384d98ead4.png" alt="img" /></p>

<p><strong>KMS</strong>所管理的display pipeline：</p>

<ol>
  <li>
    <p><strong>FrameBuffer</strong> 存储图像数据的内存区域，它是显存的一部分，包含了需要显示到屏幕上的像素数据。</p>
  </li>
  <li>
    <p><strong>Planes</strong> 不是任何硬件，它表示一个memory object containing a buffer，这个buffer随后会被送入CRTC</p>
  </li>
  <li>
    <p><strong>CRTC</strong> 控制屏幕输出的硬件模块，它决定了图像的显示方式（包括位置、大小、刷新率等）。</p>
  </li>
  <li>
    <p><strong>Connector</strong> 显示器或屏幕的接口，负责处理显示输出信号的连接。（HDMI、DP、VGA）</p>
  </li>
  <li>
    <p><strong>Encoder</strong> 将图像信号从 GPU 输出到显示器（通过 Connector）的硬件模块，将来自 CRTC 的图像数据转换为符合显示器要求的信号格</p>

    <p>式。</p>
  </li>
</ol>

<table>
  <thead>
    <tr>
      <th style="text-align: center">KMS display pipeline</th>
      <th style="text-align: center">KMS Output Pipeline</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><img src="https://www.kernel.org/doc/html/latest/_images/DOT-dade12aa9127c64406e41cdf8d7f80694c134db2.svg" alt="KMS Display Pipeline" /></td>
      <td style="text-align: center"><img src="https://www.kernel.org/doc/html/latest/_images/DOT-6445c75fc4859992454fd377127d4d309e82f09a.svg" alt="KMS Output Pipeline" /></td>
    </tr>
  </tbody>
</table>

<ul>
  <li><strong>GEM（Graphics Execution Manager）</strong></li>
</ul>

<p>在/dev/dri中，为什么有两种形式的节点？他们的区别？</p>

<blockquote>
  <p>Each GPU detected by DRM is referred to as a <em>DRM device</em>, and a device file <code class="language-plaintext highlighter-rouge">/dev/dri/card*X*</code> (where <em>X</em> is a sequential number) is created to interface with it. User-space programs that want to talk to the GPU must <a href="https://en.wikipedia.org/wiki/Open_(system_call)">open</a> this file and use <a href="https://en.wikipedia.org/wiki/Ioctl">ioctl</a> calls to communicate with DRM. Different ioctls correspond to different functions of the DRM <a href="https://en.wikipedia.org/wiki/Application_programming_interface">API</a>.</p>
</blockquote>

<ul>
  <li>/dev/dri/cardX  这种节点被叫做<strong>primary node</strong>，最开始被用来完成both privileged (modesetting, other display control) and non-privileged (rendering, <a href="https://en.wikipedia.org/wiki/GPGPU">GPGPU</a> compute) operations</li>
  <li>/dev/dri/renderD128 这种节点的出现为了解决单一primary node的不便（there must always be a running graphics server (the X Server, a Wayland compositor, …) acting as DRM-Master of a DRM device so that other user space programs can be granted the use of the device, even in cases not involving any graphics display like GPGPU computations），將non-privileged的操作独立出来，因此操作/dev/dri/renderD128只能使用a subset of DRM api</li>
</ul>

<p>https://en.wikipedia.org/wiki/Direct_Rendering_Manager</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 部分重要成员</span>
<span class="k">struct</span> <span class="n">drm_device</span> <span class="p">{</span>
    <span class="k">struct</span> <span class="n">device</span> <span class="o">*</span><span class="n">dev</span><span class="p">;</span>
    
    <span class="k">const</span> <span class="k">struct</span> <span class="n">drm_driver</span> <span class="o">*</span><span class="n">driver</span><span class="p">;</span>			<span class="c1">// driver</span>
	<span class="k">struct</span> <span class="n">drm_minor</span> <span class="o">*</span><span class="n">primary</span><span class="p">,</span> <span class="o">*</span><span class="n">render</span><span class="p">,</span> <span class="o">*</span><span class="n">accel</span><span class="p">;</span> <span class="c1">// 分别对应三种不同的功能 accel对应的应该是GPGPU</span>
	<span class="n">u32</span> <span class="n">driver_features</span><span class="p">;</span> 						<span class="c1">// driver支持的功能 DRIVER_MODESET DRIVER_GEM等</span>
	
    <span class="k">struct</span> <span class="n">drm_mode_config</span> <span class="n">mode_config</span><span class="p">;</span>			<span class="c1">// 管理了kms中的几乎所有硬件节点 property func</span>
												<span class="c1">// crtc encoder connector plane都被链表管理起来供驱动选择</span>
    											<span class="c1">// 包括drm_mode_config_funcs：fb_create atomic_commit等</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="1-libdrm">1. libdrm</h3>

<p>使用libdrm提供的接口可以完成喜爱那是设置，下面先看一个simple_drm.c程序，程序使用drm接口配置屏幕显示全白：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stddef.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;errno.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;fcntl.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdint.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;sys/mman.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;time.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;unistd.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;xf86drm.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;xf86drmMode.h&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="n">buffer_object</span> <span class="p">{</span>
	<span class="kt">uint32_t</span> <span class="n">width</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">height</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">pitch</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">handle</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">size</span><span class="p">;</span>
	<span class="kt">uint8_t</span> <span class="o">*</span><span class="n">vaddr</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">fb_id</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="n">buffer_object</span> <span class="n">buf</span><span class="p">;</span>

<span class="k">static</span> <span class="kt">int</span> <span class="nf">modeset_create_fb</span><span class="p">(</span><span class="kt">int</span> <span class="n">fd</span><span class="p">,</span> <span class="k">struct</span> <span class="n">buffer_object</span> <span class="o">*</span><span class="n">bo</span><span class="p">)</span>
<span class="p">{</span>
	<span class="k">struct</span> <span class="n">drm_mode_create_dumb</span> <span class="n">create</span> <span class="o">=</span> <span class="p">{};</span>
 	<span class="k">struct</span> <span class="n">drm_mode_map_dumb</span> <span class="n">map</span> <span class="o">=</span> <span class="p">{};</span>

	<span class="cm">/* create a dumb-buffer, the pixel format is XRGB888 */</span>
	<span class="n">create</span><span class="p">.</span><span class="n">width</span> <span class="o">=</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">width</span><span class="p">;</span>
	<span class="n">create</span><span class="p">.</span><span class="n">height</span> <span class="o">=</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">height</span><span class="p">;</span>
	<span class="n">create</span><span class="p">.</span><span class="n">bpp</span> <span class="o">=</span> <span class="mi">32</span><span class="p">;</span>
	<span class="n">drmIoctl</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">DRM_IOCTL_MODE_CREATE_DUMB</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">create</span><span class="p">);</span>

	<span class="cm">/* bind the dumb-buffer to an FB object */</span>
	<span class="n">bo</span><span class="o">-&gt;</span><span class="n">pitch</span> <span class="o">=</span> <span class="n">create</span><span class="p">.</span><span class="n">pitch</span><span class="p">;</span>
	<span class="n">bo</span><span class="o">-&gt;</span><span class="n">size</span> <span class="o">=</span> <span class="n">create</span><span class="p">.</span><span class="n">size</span><span class="p">;</span>
	<span class="n">bo</span><span class="o">-&gt;</span><span class="n">handle</span> <span class="o">=</span> <span class="n">create</span><span class="p">.</span><span class="n">handle</span><span class="p">;</span>
	<span class="n">drmModeAddFB</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">width</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">height</span><span class="p">,</span> <span class="mi">24</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">pitch</span><span class="p">,</span>
			   <span class="n">bo</span><span class="o">-&gt;</span><span class="n">handle</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">bo</span><span class="o">-&gt;</span><span class="n">fb_id</span><span class="p">);</span>

	<span class="cm">/* map the dumb-buffer to userspace */</span>
	<span class="n">map</span><span class="p">.</span><span class="n">handle</span> <span class="o">=</span> <span class="n">create</span><span class="p">.</span><span class="n">handle</span><span class="p">;</span>
	<span class="n">drmIoctl</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">DRM_IOCTL_MODE_MAP_DUMB</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">map</span><span class="p">);</span>

	<span class="n">bo</span><span class="o">-&gt;</span><span class="n">vaddr</span> <span class="o">=</span> <span class="n">mmap</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">create</span><span class="p">.</span><span class="n">size</span><span class="p">,</span> <span class="n">PROT_READ</span> <span class="o">|</span> <span class="n">PROT_WRITE</span><span class="p">,</span>
			<span class="n">MAP_SHARED</span><span class="p">,</span> <span class="n">fd</span><span class="p">,</span> <span class="n">map</span><span class="p">.</span><span class="n">offset</span><span class="p">);</span>

	<span class="cm">/* initialize the dumb-buffer with white-color */</span>
	<span class="n">memset</span><span class="p">(</span><span class="n">bo</span><span class="o">-&gt;</span><span class="n">vaddr</span><span class="p">,</span> <span class="mh">0xff</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">size</span><span class="p">);</span>

	<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">modeset_destroy_fb</span><span class="p">(</span><span class="kt">int</span> <span class="n">fd</span><span class="p">,</span> <span class="k">struct</span> <span class="n">buffer_object</span> <span class="o">*</span><span class="n">bo</span><span class="p">)</span>
<span class="p">{</span>
	<span class="k">struct</span> <span class="n">drm_mode_destroy_dumb</span> <span class="n">destroy</span> <span class="o">=</span> <span class="p">{};</span>

	<span class="n">drmModeRmFB</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">fb_id</span><span class="p">);</span>

	<span class="n">munmap</span><span class="p">(</span><span class="n">bo</span><span class="o">-&gt;</span><span class="n">vaddr</span><span class="p">,</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">size</span><span class="p">);</span>

	<span class="n">destroy</span><span class="p">.</span><span class="n">handle</span> <span class="o">=</span> <span class="n">bo</span><span class="o">-&gt;</span><span class="n">handle</span><span class="p">;</span>
	<span class="n">drmIoctl</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">DRM_IOCTL_MODE_DESTROY_DUMB</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">destroy</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span><span class="p">,</span> <span class="kt">char</span> <span class="o">**</span><span class="n">argv</span><span class="p">)</span>
<span class="p">{</span>
	<span class="kt">int</span> <span class="n">fd</span><span class="p">;</span>
	<span class="n">drmModeConnector</span> <span class="o">*</span><span class="n">conn</span><span class="p">;</span>
	<span class="n">drmModeRes</span> <span class="o">*</span><span class="n">res</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">conn_id</span><span class="p">;</span>
	<span class="kt">uint32_t</span> <span class="n">crtc_id</span><span class="p">;</span>

	<span class="n">fd</span> <span class="o">=</span> <span class="n">open</span><span class="p">(</span><span class="s">"/dev/dri/card0"</span><span class="p">,</span> <span class="n">O_RDWR</span> <span class="o">|</span> <span class="n">O_CLOEXEC</span><span class="p">);</span>

	<span class="n">res</span> <span class="o">=</span> <span class="n">drmModeGetResources</span><span class="p">(</span><span class="n">fd</span><span class="p">);</span>
	<span class="n">crtc_id</span> <span class="o">=</span> <span class="n">res</span><span class="o">-&gt;</span><span class="n">crtcs</span><span class="p">[</span><span class="mi">0</span><span class="p">];</span>
	<span class="n">conn_id</span> <span class="o">=</span> <span class="n">res</span><span class="o">-&gt;</span><span class="n">connectors</span><span class="p">[</span><span class="mi">0</span><span class="p">];</span>

	<span class="n">conn</span> <span class="o">=</span> <span class="n">drmModeGetConnector</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">conn_id</span><span class="p">);</span>
	<span class="n">buf</span><span class="p">.</span><span class="n">width</span> <span class="o">=</span> <span class="n">conn</span><span class="o">-&gt;</span><span class="n">modes</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">hdisplay</span><span class="p">;</span>
	<span class="n">buf</span><span class="p">.</span><span class="n">height</span> <span class="o">=</span> <span class="n">conn</span><span class="o">-&gt;</span><span class="n">modes</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">vdisplay</span><span class="p">;</span>

	<span class="n">modeset_create_fb</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">buf</span><span class="p">);</span>

	<span class="n">drmModeSetCrtc</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="n">crtc_id</span><span class="p">,</span> <span class="n">buf</span><span class="p">.</span><span class="n">fb_id</span><span class="p">,</span>
			<span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">conn_id</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">conn</span><span class="o">-&gt;</span><span class="n">modes</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>

	<span class="n">getchar</span><span class="p">();</span>

	<span class="n">modeset_destroy_fb</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">buf</span><span class="p">);</span>

	<span class="n">drmModeFreeConnector</span><span class="p">(</span><span class="n">conn</span><span class="p">);</span>
	<span class="n">drmModeFreeResources</span><span class="p">(</span><span class="n">res</span><span class="p">);</span>

	<span class="n">close</span><span class="p">(</span><span class="n">fd</span><span class="p">);</span>

	<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong><em>为什么將节点换成/dev/dri/renderD128之后segfault？</em></strong></p>

<h4 id="legacy接口">legacy接口</h4>

<ul>
  <li><strong>drmModeSetCrtc</strong></li>
  <li><strong>drmModePageFlip</strong> （只会在vsync的时候进行切换framebuffer，避免画面撕裂）</li>
  <li><strong>drmModeSetPlane</strong>（只显示framebuffer的部分，之前都是全部显示）使用前需要先调用drmModeSetCrtc打通链路</li>
</ul>

<p><img src="https://i-blog.csdnimg.cn/blog_migrate/c1f6c3fdeef9d3571a45e46580c7379b.png" alt="img" /></p>

<h4 id="atomic接口">atomic接口</h4>

<p>atomic关键词如何理解？</p>

<p>所有的property配置完成后进行commit，commit如果失败，所有的配置恢复到commit之前</p>

<p>atomic接口引入property，这些property的引入简化驱动层IOCTL函数</p>

<p><img src="https://i-blog.csdnimg.cn/blog_migrate/2df1c788392fd8240a9637afad6f1d6b.png" alt="img" /></p>

<p><strong><em>为什么在简单的 DRM 应用程序中不需要显式配置 encoder？</em></strong></p>

<h3 id="2-drm-driver">2. DRM driver</h3>

<p><img src="file:///home/songyj/Pictures/Screenshots/Screenshot%20from%202024-12-15%2009-10-39.png" alt="" /></p>

<p>DRM驱动其实不是一整个驱动而是图形栈各硬件驱动都被插入到DRM框架中，比如有kms驱动，gem驱动以及GPU驱动，其中KMS驱动中会包含有操作硬件的部分，这些硬件操作会被嵌入相应的drm模块中，</p>

<p>render和display是区别开的</p>

<p>libdrm中提供的接口是DRM driver暴露出来的，每一个DRM驱动的核心都是<strong>struct drm_driver</strong>。对于Linux驱动而言，一般都是一个结构体，随后通过register函数往总线或者驱动框架注册，DRM驱动也是一样：</p>

<ol>
  <li><a href="https://www.kernel.org/doc/html/v4.11/gpu/drm-internals.html#c.drm_dev_alloc"><code class="language-plaintext highlighter-rouge">drm_dev_alloc</code></a> 为<strong>struct drm_driver</strong>分配内存</li>
  <li>填充<strong>struct drm_driver</strong>各成员</li>
  <li><a href="https://www.kernel.org/doc/html/v4.11/gpu/drm-internals.html#c.drm_dev_register"><code class="language-plaintext highlighter-rouge">drm_dev_register</code></a> 注册<strong>struct drm_driver</strong>到DRM驱动框架</li>
</ol>

<p><strong>struct drm_driver</strong>中的fops提供.unlocked_ioctl=drm_ioctl，使用drm core中的函数来处理用户空间的ioctl请求。</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>struct drm_driver
{
	一系列成员函数指针;
	
	.fops; // 这里面包含ioctl ioctl的函数可能最后还是调用到上面的成员函数 比如create_dumb
}

// This creates a new dumb buffer in the driver's backing storage manager (GEM, TTM or something else entirely) and // returns the resulting buffer handle. This handle can then be wrapped up into a framebuffer modeset object.
// 比如create_dumb接受ioctl传递的buffer参数（width height colorformat）凭这些参数计算buffer大小并申请
// framebuffer是buffer handle+metadata
</code></pre></div></div>

<p>drm_atomic_helper_commit_planes</p>

<p>初始化display pipeline的顺序很重要：connector-&gt;encoder-&gt;plane-&gt;crtc</p>

<p>drmModeSetCrtc ioctl的调用路径：</p>

<ol>
  <li>drm_mode_setcrtc</li>
  <li>drm_atomic_helper_set_config</li>
  <li>drm_atomic_commit</li>
  <li>drm_atomic_helper_commit</li>
  <li>drm_atomic_helper_commit_tail</li>
  <li>drm_atomic_helper_commit_planes</li>
  <li>plane.private-&gt;atomic_update</li>
</ol>

<p>drm_atomic_helper_check_modset</p>

<p>KMS</p>

<p>DRM driver必须调用<a href="https://www.kernel.org/doc/html/v4.11/gpu/drm-kms.html#c.drm_mode_config_init"><code class="language-plaintext highlighter-rouge">drm_mode_config_init</code></a></p>

<p>对<strong>struct drm_driver</strong>中的<strong><em>driver_feature</em></strong>进行置位，告诉DRM驱动框架当前驱动的能力，常见的标志位如下：</p>

<ul>
  <li><strong>DRIVER_MODESET</strong> 支持modesetting操作</li>
  <li><strong>DRIVER_GEM</strong> 支持内存分配</li>
  <li><strong>DRIVER_ATOMIC</strong> 支持atomic操作</li>
  <li><strong>DRIVER_RENDER</strong> 支持渲染</li>
</ul>

<p>调用fb_create的ioctl是<strong>DRM_IOCTL_MODE_ADDFB2</strong>，fb_create將gem obj handle和fb绑定，没有绑定handle的fb没有任何实际的显示内容</p>

<h4 id="综合实现分析exynos为例">综合实现分析（exynos为例）</h4>

<p>完整的KMS驱动中间含有很多sub drivers不是以前那种简单的一个.c的char driver所能相比的，这种驱动被叫做aggregate driver。不过依然可以找到一条主线来分析它：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>├── exynos5433_drm_decon.c
├── exynos5433_drm_decon.o
├── exynos7_drm_decon.c
├── exynos7_drm_decon.o
├── exynos_dp.c
├── exynos_drm_crtc.c
├── exynos_drm_crtc.h
├── exynos_drm_crtc.o
├── exynos_drm_dma.c
├── exynos_drm_dma.o
├── exynos_drm_dpi.c
├── exynos_drm_drv.c
├── exynos_drm_drv.h
├── exynos_drm_drv.o
├── exynos_drm_dsi.c
├── exynos_drm_dsi.o
├── exynos_drm_fb.c
├── exynos_drm_fbdev.c
├── exynos_drm_fbdev.h
├── exynos_drm_fbdev.o
├── exynos_drm_fb.h
├── exynos_drm_fb.o
├── exynos_drm_fimc.c
├── exynos_drm_fimd.c
├── exynos_drm_g2d.c
├── exynos_drm_g2d.h
├── exynos_drm_gem.c
├── exynos_drm_gem.h
├── exynos_drm_gem.o
├── exynos_drm_gsc.c
├── exynos_drm_ipp.c
├── exynos_drm_ipp.h
├── exynosdrm.ko
├── exynos_drm_mic.c
├── exynos_drm_mic.o
├── exynosdrm.mod
├── exynosdrm.mod.c
├── exynosdrm.mod.o
├── exynosdrm.o
├── exynos_drm_plane.c
├── exynos_drm_plane.h
├── exynos_drm_plane.o
├── exynos_drm_rotator.c
├── exynos_drm_scaler.c
├── exynos_drm_vidi.c
├── exynos_drm_vidi.h
├── exynos_hdmi.c
├── exynos_hdmi.o
├── exynos_mixer.c
├── Kconfig
├── Makefile
├── modules.order
├── regs-decon5433.h
├── regs-decon7.h
├── regs-fimc.h
├── regs-gsc.h
├── regs-hdmi.h
├── regs-mixer.h
├── regs-rotator.h
├── regs-scaler.h
└── regs-vp.h
</code></pre></div></div>

<p>从exynos_drm_drv.c开始看起</p>

<p>exynos_drm_init()作为入口函数被module_init宏设置到<strong>内核入口</strong></p>

<p>exynos_drm_register_devices()</p>

<p>exynos_drm_register_drivers()</p>

<h4 id="drm解析分发ioctl">DRM解析分发ioctl</h4>

<p>用户程序接口的名字和ioctl的名字不一定一致</p>

<p>atomic ioctl由一个ioctl统一分发</p>

<p>GEM是内存分配机制，dma-buf是内存共享机制，GEM和dma-buf结合使用。</p>

<h4 id="dma-buf">dma-buf</h4>

<p>dma-buf在内核空间和用户空间中的表显形式不一样，在用户空间中是一个<strong>fd</strong>，在内核中是<strong>struct dma_buf</strong></p>

<blockquote>
  <p>struct dma_buf - shared buffer object</p>

  <p>This represents a shared buffer, created by calling dma_buf_export(). The userspace representation is a normal file descriptor, which can be created by calling dma_buf_fd().</p>

  <p>Shared dma buffers are reference counted using dma_buf_put() and get_dma_buf().</p>

  <p>Device DMA access is handled by the separate &amp;struct dma_buf_attachment.</p>
</blockquote>

<ul>
  <li>No standardised memory allocation: It’s up to the exporter;</li>
  <li>An arbitrary buffer can be wrapped into a DMA buffer;</li>
  <li>An fd can be returned to the userspace to reference this buffer;</li>
  <li>The fd can be passed to another process;</li>
  <li>The fd can be mmapped and accessed by the CPU;</li>
  <li>The fd can be imported by any driver supporting the user role</li>
</ul>

<p>场景：</p>

<p>GPU 和显示控制器共享帧缓冲，GPU 使用 GEM 分配显存来存储绘制的图形帧。</p>

<p>GPU 驱动通过 <code class="language-plaintext highlighter-rouge">dma_buf_export</code> 将 GEM 对象导出为 dma-buf。</p>

<p>显示控制器驱动通过 <code class="language-plaintext highlighter-rouge">dma_buf_attach</code> 访问该缓冲区，并将内容直接显示。</p>

<p>下面列出一对用于在dma-buf和gem handle之间转换的ioctl</p>

<ul>
  <li>drm_prime_fd_to_handle</li>
  <li>drm_prime_handle_to_fd</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// 查看dma heap节点
ls /dev/dma_heap

struct dma_heap_allocation_data heap_data = {
    .len = 1048576, // 1meg
    .fd_flags = O_RDWR | O_CLOEXEC,
};

fd = open(“/dev/dma_heap/system”, O_RDONLY | O_CLOEXEC);
if (fd &lt; 0) return fd;
ret = ioctl(fd, DMA_HEAP_IOCTL_ALLOC, &amp;heap_data)
if (ret) return ret;
return heap_data.fd;
</code></pre></div></div>

<p><a href="https://www.youtube.com/watch?v=UsEVoWD_o0c">dma-buf</a></p>

<h4 id="helper-function">helper function</h4>

<h5 id="helper-function如何使用">helper function如何使用？</h5>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 定义某个helper function具体的操作</span>
<span class="k">static</span> <span class="kt">void</span> <span class="nf">exynos_plane_atomic_update</span><span class="p">(</span><span class="k">struct</span> <span class="n">drm_plane</span> <span class="o">*</span><span class="n">plane</span><span class="p">,</span> <span class="k">struct</span> <span class="n">drm_atomic_state</span> <span class="o">*</span><span class="n">state</span><span class="p">)</span>
<span class="p">{</span>
	<span class="k">struct</span> <span class="n">drm_plane_state</span> <span class="o">*</span><span class="n">new_state</span> <span class="o">=</span> <span class="n">drm_atomic_get_new_plane_state</span><span class="p">(</span><span class="n">state</span><span class="p">,</span>
								           <span class="n">plane</span><span class="p">);</span>
	<span class="k">struct</span> <span class="n">exynos_drm_crtc</span> <span class="o">*</span><span class="n">exynos_crtc</span> <span class="o">=</span> <span class="n">to_exynos_crtc</span><span class="p">(</span><span class="n">new_state</span><span class="o">-&gt;</span><span class="n">crtc</span><span class="p">);</span>
	<span class="k">struct</span> <span class="n">exynos_drm_plane</span> <span class="o">*</span><span class="n">exynos_plane</span> <span class="o">=</span> <span class="n">to_exynos_plane</span><span class="p">(</span><span class="n">plane</span><span class="p">);</span>

	<span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">new_state</span><span class="o">-&gt;</span><span class="n">crtc</span><span class="p">)</span>
		<span class="k">return</span><span class="p">;</span>

	<span class="k">if</span> <span class="p">(</span><span class="n">exynos_crtc</span><span class="o">-&gt;</span><span class="n">ops</span><span class="o">-&gt;</span><span class="n">update_plane</span><span class="p">)</span>
		<span class="n">exynos_crtc</span><span class="o">-&gt;</span><span class="n">ops</span><span class="o">-&gt;</span><span class="n">update_plane</span><span class="p">(</span><span class="n">exynos_crtc</span><span class="p">,</span> <span class="n">exynos_plane</span><span class="p">);</span> <span class="c1">// 硬件操作</span>
<span class="p">}</span>

<span class="c1">// 定义某个硬件helper function集合</span>
<span class="k">static</span> <span class="k">const</span> <span class="k">struct</span> <span class="n">drm_plane_helper_funcs</span> <span class="n">plane_helper_funcs</span> <span class="o">=</span> <span class="p">{</span>
	<span class="p">.</span><span class="n">atomic_check</span> <span class="o">=</span> <span class="n">exynos_plane_atomic_check</span><span class="p">,</span>
	<span class="p">.</span><span class="n">atomic_update</span> <span class="o">=</span> <span class="n">exynos_plane_atomic_update</span><span class="p">,</span>
	<span class="p">.</span><span class="n">atomic_disable</span> <span class="o">=</span> <span class="n">exynos_plane_atomic_disable</span><span class="p">,</span>
<span class="p">};</span>

<span class="c1">// 注册helper function到drm_plane</span>
<span class="c1">// 实际plane-&gt;helper_private = funcs;</span>
<span class="n">drm_plane_helper_add</span><span class="p">(</span><span class="o">&amp;</span><span class="n">exynos_plane</span><span class="o">-&gt;</span><span class="n">base</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">plane_helper_funcs</span><span class="p">);</span>
</code></pre></div></div>

<h4 id="scatterlist">scatterlist</h4>

<p>散列表的结构体如下，其中scatterlist可以被分成三种：一般，铰链，结束，这三种类型由page_link的最低两位决定</p>

<p>offset和length分别表示内存块在page上的偏移位置和内存长度</p>

<p>dma_address表示PA物理地址</p>

<p><strong>每一个scatterlist可以看作一个内存块，每一个sg_table则是一组链表，其中链表的每一个元素都是一个scaatterlist数组</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>struct scatterlist {
	unsigned long	page_link;
	unsigned int	offset;
	unsigned int	length;
	dma_addr_t	dma_address;
#ifdef CONFIG_NEED_SG_DMA_LENGTH
	unsigned int	dma_length;
#endif
#ifdef CONFIG_NEED_SG_DMA_FLAGS
	unsigned int    dma_flags;
#endif
};

struct sg_table {
	struct scatterlist *sgl;	/* the list */
	unsigned int nents;		/* number of mapped entries */
	unsigned int orig_nents;	/* original size of list */
};
</code></pre></div></div>

<p>散列表遍历</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#define for_each_sg(sglist, sg, nr, __i)	\
	for (__i = 0, sg = (sglist); __i &lt; (nr); __i++, sg = sg_next(sg))
	
struct scatterlist *sg_next(struct scatterlist *sg)
{
	if (sg_is_last(sg))
		return NULL;

	sg++;
	if (unlikely(sg_is_chain(sg)))
		sg = sg_chain_ptr(sg);

	return sg;
}
EXPORT_SYMBOL(sg_next);

</code></pre></div></div>

<h4 id="问题">问题</h4>

<p><strong>是否硬件相关的操作都要放在helper函数中？</strong></p>

<p>https://www.kernel.org/doc/html/v4.11/gpu/index.html</p>

<p><strong>fake_gpu 0000:00:01.0: BAR 0 [mem 0x00000000-0x0000001f]: not claimed; can’t enable device</strong></p>

<h2 id="0x02-qemu---fake-gpu-device">0x02 QEMU - fake GPU device</h2>

<p>在QEMU中引入一个新的虚拟GPU挂载到PCI总线</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"qemu/osdep.h"</span><span class="cp">
#include</span> <span class="cpf">"qom/object.h"</span><span class="cp">
#include</span> <span class="cpf">"ui/console.h"</span><span class="cp">
#include</span> <span class="cpf">"hw/pci/pci_device.h"</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">Fake_GPU_State</span> <span class="n">Fake_GPU_State</span><span class="p">;</span>

<span class="cp">#define TYPE_FAKE_GPU "fake_gpu"
</span><span class="n">OBJECT_DECLARE_SIMPLE_TYPE</span><span class="p">(</span><span class="n">Fake_GPU_State</span><span class="p">,</span> <span class="n">FAKE_GPU</span><span class="p">)</span>

<span class="cp">#define REG_NUM     8
#define REG_SIZE    4
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">Fake_GPU_State</span>
<span class="p">{</span>
    <span class="n">PCIDevice</span> <span class="n">parent_obj</span><span class="p">;</span>

    <span class="n">MemoryRegion</span> <span class="n">bar</span><span class="p">;</span>
    <span class="kt">uint32_t</span> <span class="n">regs</span><span class="p">[</span><span class="n">REG_NUM</span><span class="p">];</span>
<span class="p">}</span><span class="n">Fake_GPU_State</span><span class="p">;</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_write</span><span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="n">opaque</span><span class="p">,</span> <span class="n">hwaddr</span> <span class="n">addr</span><span class="p">,</span> <span class="kt">uint64_t</span> <span class="n">value</span><span class="p">,</span> <span class="kt">unsigned</span> <span class="n">size</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">Fake_GPU_State</span> <span class="o">*</span><span class="n">s</span> <span class="o">=</span> <span class="n">FAKE_GPU</span><span class="p">(</span><span class="n">opaque</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"wr</span><span class="se">\r\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">s</span><span class="o">-&gt;</span><span class="n">reg0</span> <span class="o">=</span> <span class="n">value</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">uint64_t</span> <span class="nf">fake_gpu_read</span><span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="n">opaque</span><span class="p">,</span> <span class="n">hwaddr</span> <span class="n">addr</span><span class="p">,</span> <span class="kt">unsigned</span> <span class="n">size</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">Fake_GPU_State</span> <span class="o">*</span><span class="n">s</span> <span class="o">=</span> <span class="n">FAKE_GPU</span><span class="p">(</span><span class="n">opaque</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"rd</span><span class="se">\r\n</span><span class="s">"</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">s</span><span class="o">-&gt;</span><span class="n">reg0</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="k">const</span> <span class="n">MemoryRegionOps</span> <span class="n">fake_gpu_ops</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">.</span><span class="n">write</span> <span class="o">=</span> <span class="n">fake_gpu_write</span><span class="p">,</span>
    <span class="p">.</span><span class="n">read</span> <span class="o">=</span> <span class="n">fake_gpu_read</span><span class="p">,</span>
<span class="p">};</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_realize</span><span class="p">(</span><span class="n">PCIDevice</span> <span class="o">*</span><span class="n">dev</span><span class="p">,</span> <span class="n">Error</span> <span class="o">**</span><span class="n">errp</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">Fake_GPU_State</span> <span class="o">*</span><span class="n">s</span> <span class="o">=</span> <span class="n">FAKE_GPU</span><span class="p">(</span><span class="n">dev</span><span class="p">);</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"realize start</span><span class="se">\r\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">memory_region_init_io</span><span class="p">(</span><span class="o">&amp;</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">bar</span><span class="p">,</span> <span class="n">OBJECT</span><span class="p">(</span><span class="n">s</span><span class="p">),</span> <span class="o">&amp;</span><span class="n">fake_gpu_ops</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="s">"fake_gpu"</span><span class="p">,</span> <span class="mi">32</span><span class="p">);</span>
    <span class="n">pci_register_bar</span><span class="p">(</span><span class="n">dev</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">PCI_BASE_ADDRESS_SPACE_MEMORY</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">bar</span><span class="p">);</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_exit</span><span class="p">(</span><span class="n">PCIDevice</span> <span class="o">*</span><span class="n">dev</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">return</span> <span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_class_init</span><span class="p">(</span><span class="n">ObjectClass</span> <span class="o">*</span><span class="n">klass</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">data</span><span class="p">)</span>
<span class="p">{</span>
    <span class="c1">//DeviceClass *dc = DEVICE_CLASS(klass);</span>
    <span class="n">PCIDeviceClass</span> <span class="o">*</span><span class="n">k</span> <span class="o">=</span> <span class="n">PCI_DEVICE_CLASS</span><span class="p">(</span><span class="n">klass</span><span class="p">);</span>

    <span class="n">k</span><span class="o">-&gt;</span><span class="n">realize</span> <span class="o">=</span> <span class="n">fake_gpu_realize</span><span class="p">;</span>
    <span class="n">k</span><span class="o">-&gt;</span><span class="n">exit</span> <span class="o">=</span> <span class="n">fake_gpu_exit</span><span class="p">;</span>
    <span class="n">k</span><span class="o">-&gt;</span><span class="n">vendor_id</span> <span class="o">=</span> <span class="mh">0x1234</span><span class="p">;</span>
    <span class="n">k</span><span class="o">-&gt;</span><span class="n">device_id</span> <span class="o">=</span> <span class="mh">0xbeef</span><span class="p">;</span>
    <span class="n">k</span><span class="o">-&gt;</span><span class="n">class_id</span> <span class="o">=</span> <span class="n">PCI_CLASS_OTHERS</span><span class="p">;</span> <span class="c1">// 一定要有否则报错 BAR0 not claimed</span>
<span class="p">}</span>

<span class="k">static</span> <span class="k">const</span> <span class="n">TypeInfo</span> <span class="n">fake_gpu_info</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">TYPE_FAKE_GPU</span><span class="p">,</span>
    <span class="p">.</span><span class="n">parent</span> <span class="o">=</span> <span class="n">TYPE_PCI_DEVICE</span><span class="p">,</span>
    <span class="p">.</span><span class="n">instance_size</span> <span class="o">=</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">Fake_GPU_State</span><span class="p">),</span>
    <span class="p">.</span><span class="n">class_init</span>    <span class="o">=</span> <span class="n">fake_gpu_class_init</span><span class="p">,</span>
    <span class="p">.</span><span class="n">interfaces</span> <span class="o">=</span> <span class="p">(</span><span class="n">InterfaceInfo</span><span class="p">[])</span> <span class="p">{</span>
        <span class="p">{</span> <span class="n">INTERFACE_CONVENTIONAL_PCI_DEVICE</span> <span class="p">},</span>
        <span class="p">{</span> <span class="p">},</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_device_register_types</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">type_register_static</span><span class="p">(</span><span class="o">&amp;</span><span class="n">fake_gpu_info</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">type_init</span><span class="p">(</span><span class="n">fake_gpu_device_register_types</span><span class="p">);</span>

</code></pre></div></div>

<p>QEMU启动相关配置：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qemu-system-aarch64 <span class="se">\</span>
    <span class="nt">-M</span> virt <span class="se">\</span>
    <span class="nt">-cpu</span> cortex-a53 <span class="se">\</span>
    <span class="nt">-kernel</span> /home/songyj/embedded/buildroot-2024.08.2/output/images/Image <span class="se">\</span>
    <span class="nt">-drive</span> <span class="nv">file</span><span class="o">=</span>/home/songyj/embedded/buildroot-2024.08.2/output/images/rootfs.ext4,format<span class="o">=</span>raw <span class="se">\</span>
    <span class="nt">-append</span> <span class="s2">"root=/dev/vda nokaslr ip=dhcp"</span> <span class="se">\</span>
    <span class="nt">-device</span> virtio-gpu-gl <span class="se">\</span>
    <span class="nt">-display</span> gtk,gl<span class="o">=</span>core <span class="se">\</span>
    <span class="nt">-device</span> virtio-net-pci,netdev<span class="o">=</span>net0 <span class="se">\</span>
    <span class="nt">-netdev</span> user,id<span class="o">=</span>net0,tftp<span class="o">=</span>/home/songyj/embedded/tftp_qemu <span class="se">\</span>
    <span class="nt">-device</span> fake_gpu
</code></pre></div></div>

<p>确认新的pci设备编译到QEMU中：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 编译完成之后通过qemu-system-aarch64查看设备信息</span>
songyj@songyj-HP:~/embedded<span class="nv">$ </span>qemu-system-aarch64 <span class="nt">-device</span> <span class="nb">help</span> | <span class="nb">grep </span>fake_gpu
name <span class="s2">"fake_gpu"</span>, bus PCI

<span class="c"># 在虚拟机中使用lspci也能看到相应的设备信息</span>
</code></pre></div></div>

<p><a href="https://www.linux-kvm.org/images/1/1b/02x04-Aspen-Gerd_Hoffmann-QEMU_and_OpenGL.pdf">QEMU display</a></p>

<p>QEMU添加显示支持：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="n">Fake_GPU_State</span>
<span class="p">{</span>
    <span class="n">PCIDevice</span> <span class="n">parent_obj</span><span class="p">;</span>

    <span class="n">QemuConsole</span><span class="o">*</span> <span class="n">console</span><span class="p">;</span>

    <span class="n">MemoryRegion</span> <span class="n">bar</span><span class="p">;</span>
    <span class="kt">uint32_t</span> <span class="n">regs</span><span class="p">[</span><span class="n">REG_NUM</span><span class="p">];</span>

    <span class="kt">void</span> <span class="o">*</span><span class="n">pixels</span><span class="p">;</span>
<span class="p">}</span><span class="n">Fake_GPU_State</span><span class="p">;</span>
</code></pre></div></div>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 显示测试函数</span>
<span class="k">static</span> <span class="kt">void</span> <span class="nf">fill_screen_with_yellow</span><span class="p">(</span><span class="kt">uint32_t</span> <span class="o">*</span><span class="n">buffer</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">uint32_t</span> <span class="o">*</span><span class="n">framebuffer</span> <span class="o">=</span> <span class="n">buffer</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">total_pixels</span> <span class="o">=</span> <span class="mi">1024</span> <span class="o">*</span> <span class="mi">768</span><span class="p">;</span>

    <span class="c1">// 黄色的 ARGB 格式</span>
    <span class="kt">uint32_t</span> <span class="n">yellow_color</span> <span class="o">=</span> <span class="mh">0x00FFFF00</span><span class="p">;</span>

    <span class="c1">// 使用 memset 直接填充内存</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">total_pixels</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">framebuffer</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">yellow_color</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="c1">// 初始化屏幕</span>
<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_realize</span><span class="p">(</span><span class="n">PCIDevice</span> <span class="o">*</span><span class="n">dev</span><span class="p">,</span> <span class="n">Error</span> <span class="o">**</span><span class="n">errp</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">Fake_GPU_State</span> <span class="o">*</span><span class="n">s</span> <span class="o">=</span> <span class="n">FAKE_GPU</span><span class="p">(</span><span class="n">dev</span><span class="p">);</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"realize start</span><span class="se">\r\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">memory_region_init_io</span><span class="p">(</span><span class="o">&amp;</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">bar</span><span class="p">,</span> <span class="n">OBJECT</span><span class="p">(</span><span class="n">s</span><span class="p">),</span> <span class="o">&amp;</span><span class="n">fake_gpu_ops</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="s">"fake_gpu"</span><span class="p">,</span> <span class="n">REG_NUM</span><span class="o">*</span><span class="n">REG_SIZE</span><span class="p">);</span>
    <span class="n">pci_register_bar</span><span class="p">(</span><span class="n">dev</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">PCI_BASE_ADDRESS_SPACE_MEMORY</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">bar</span><span class="p">);</span>

    <span class="n">s</span><span class="o">-&gt;</span><span class="n">pixels</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="n">PIXS_SIZE</span><span class="p">);</span> <span class="c1">// 为像素分配空间</span>
    <span class="n">memset</span><span class="p">(</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">pixels</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">PIXS_SIZE</span><span class="p">));</span>

    <span class="n">s</span><span class="o">-&gt;</span><span class="n">console</span> <span class="o">=</span> <span class="n">graphic_console_init</span><span class="p">(</span><span class="n">DEVICE</span><span class="p">(</span><span class="n">dev</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">fake_gpu_graphic_ops</span><span class="p">,</span> <span class="n">s</span><span class="p">);</span>

    <span class="n">DisplaySurface</span><span class="o">*</span> <span class="n">ds</span> <span class="o">=</span> <span class="n">qemu_create_displaysurface_from</span><span class="p">(</span><span class="mi">1024</span><span class="p">,</span> <span class="mi">768</span><span class="p">,</span> <span class="n">PIXMAN_a8r8g8b8</span><span class="p">,</span> <span class="mi">1024</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">s</span><span class="o">-&gt;</span><span class="n">pixels</span><span class="p">);</span>
    <span class="n">fill_screen_with_yellow</span><span class="p">(</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">pixels</span><span class="p">);</span> <span class="c1">// 初始化屏幕为黄色</span>
    <span class="n">dpy_gfx_replace_surface</span><span class="p">(</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">console</span><span class="p">,</span> <span class="n">ds</span><span class="p">);</span> <span class="c1">// 替换画布</span>
    <span class="n">dpy_gfx_update_full</span><span class="p">(</span><span class="n">s</span><span class="o">-&gt;</span><span class="n">console</span><span class="p">);</span> <span class="c1">// 显示</span>
<span class="p">}</span>
</code></pre></div></div>

<p>https://blog.davidv.dev/posts/pcie-driver-dma/</p>

<h2 id="0x03-linux---fake-gpu-device-driver">0x03 Linux - fake GPU device driver</h2>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;linux/module.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;linux/pci.h&gt;</span><span class="cp">
</span>
<span class="cp">#define DRV_NAME    "fake_gpu"
#define PCI_VENDOR_ID_FAKE_GPU    0x1234
#define PCI_DEVICE_ID_FAKE_GPU    0xbeef
</span>
<span class="k">volatile</span> <span class="kt">uint32_t</span> <span class="n">__iomem</span> <span class="o">*</span><span class="n">regs</span><span class="p">;</span>

<span class="k">static</span> <span class="k">const</span> <span class="k">struct</span> <span class="n">pci_device_id</span> <span class="n">pci_table</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">{</span> <span class="n">PCI_DEVICE</span><span class="p">(</span><span class="n">PCI_VENDOR_ID_FAKE_GPU</span><span class="p">,</span> <span class="n">PCI_DEVICE_ID_FAKE_GPU</span><span class="p">)},</span>
    <span class="p">{</span> <span class="p">},</span>
<span class="p">};</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_gpu_remove</span><span class="p">(</span><span class="k">struct</span> <span class="n">pci_dev</span> <span class="o">*</span><span class="n">pdev</span><span class="p">)</span>
<span class="p">{</span>

<span class="p">}</span>

<span class="k">static</span> <span class="kt">int</span> <span class="nf">fake_gpu_probe</span><span class="p">(</span><span class="k">struct</span> <span class="n">pci_dev</span> <span class="o">*</span><span class="n">pdev</span><span class="p">,</span> <span class="k">const</span> <span class="k">struct</span> <span class="n">pci_device_id</span> <span class="o">*</span><span class="n">ent</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">printk</span><span class="p">(</span><span class="s">"This is a pci driver"</span><span class="p">);</span>
 	<span class="kt">int</span> <span class="n">ret</span><span class="p">;</span>

	<span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">pdev</span><span class="p">)</span> 
	<span class="p">{</span>
		<span class="n">printk</span><span class="p">(</span><span class="s">"Invalid pci device pointer</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
		<span class="k">return</span> <span class="o">-</span><span class="n">EINVAL</span><span class="p">;</span>
	<span class="p">}</span>

	<span class="n">ret</span> <span class="o">=</span> <span class="n">pci_enable_device</span><span class="p">(</span><span class="n">pdev</span><span class="p">);</span>
	<span class="k">if</span><span class="p">(</span><span class="n">ret</span><span class="p">)</span>
	<span class="p">{</span>
		<span class="n">printk</span><span class="p">(</span><span class="s">"pci_enable_devices failed"</span><span class="p">);</span>
		<span class="k">return</span> <span class="n">ret</span><span class="p">;</span>
	<span class="p">}</span>
	<span class="k">else</span>
	<span class="p">{</span>
		<span class="n">printk</span><span class="p">(</span><span class="s">"nice try"</span><span class="p">);</span>
	<span class="p">}</span>

	<span class="n">ret</span> <span class="o">=</span> <span class="n">pci_request_regions</span><span class="p">(</span><span class="n">pdev</span><span class="p">,</span> <span class="n">DRV_NAME</span><span class="p">);</span>
	<span class="k">if</span><span class="p">(</span><span class="n">ret</span><span class="p">)</span>
	<span class="p">{</span>
		<span class="n">printk</span><span class="p">(</span><span class="s">"pci_request_regions failed"</span><span class="p">);</span>
	<span class="p">}</span>

	<span class="n">resource_size_t</span> <span class="n">pciaddr</span> <span class="o">=</span> <span class="n">pci_resource_start</span><span class="p">(</span><span class="n">pdev</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>

	<span class="n">regs</span> <span class="o">=</span> <span class="n">ioremap</span><span class="p">(</span><span class="n">pciaddr</span><span class="p">,</span> <span class="mi">32</span><span class="p">);</span>

	    <span class="n">u32</span> <span class="n">reg_value</span> <span class="o">=</span> <span class="n">readl</span><span class="p">(</span><span class="n">regs</span><span class="p">);</span>
    <span class="n">printk</span><span class="p">(</span><span class="s">"Register value: 0x%x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">reg_value</span><span class="p">);</span>

    <span class="c1">// 写入寄存器</span>
    <span class="n">writel</span><span class="p">(</span><span class="mh">0xDEADBEEF</span><span class="p">,</span> <span class="n">regs</span><span class="p">);</span>
    <span class="n">printk</span><span class="p">(</span><span class="s">"Written value: 0xDEADBEEF</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

	    <span class="n">reg_value</span> <span class="o">=</span> <span class="n">readl</span><span class="p">(</span><span class="n">regs</span><span class="p">);</span>
    <span class="n">printk</span><span class="p">(</span><span class="s">"Register value: 0x%x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">reg_value</span><span class="p">);</span>

	<span class="k">return</span> <span class="n">ret</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="k">struct</span> <span class="n">pci_driver</span> <span class="n">fake_gpu_driver</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">DRV_NAME</span><span class="p">,</span>
    <span class="p">.</span><span class="n">id_table</span> <span class="o">=</span> <span class="n">pci_table</span><span class="p">,</span>
    <span class="p">.</span><span class="n">probe</span> <span class="o">=</span> <span class="n">fake_gpu_probe</span><span class="p">,</span>
    <span class="p">.</span><span class="n">remove</span> <span class="o">=</span> <span class="n">fake_gpu_remove</span><span class="p">,</span>
<span class="p">};</span>

<span class="n">module_pci_driver</span><span class="p">(</span><span class="n">fake_gpu_driver</span><span class="p">);</span>
</code></pre></div></div>

<p>https://crab2313.github.io/post/drm-legacy-kms/</p>

<p>https://encelo.github.io/trip_through_graphics_pipeline_2011.html</p>

<p>https://adrian.geek.nz/graphics_docs/DRM.html</p>

<p>http://phd.mupuf.org/files/kr2014.pdf</p>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[[TOC]]]></summary></entry><entry><title type="html">Rk3588_baremetal</title><link href="https://songyj123.github.io/rk3588-baremetal.html" rel="alternate" type="text/html" title="Rk3588_baremetal" /><published>2023-11-08T00:00:00+08:00</published><updated>2023-11-08T00:00:00+08:00</updated><id>https://songyj123.github.io/RK3588_baremetal</id><content type="html" xml:base="https://songyj123.github.io/rk3588-baremetal.html"><![CDATA[<h1 id="rk系列裸机">rk系列裸机</h1>

<!--more-->

<h2 id="1-准备">1. 准备</h2>

<p>现在厂商都一套自己的打包软件，按照如hceng讲解的<a href="https://hceng.cn/2018/08/16/RK3399%E2%80%94%E2%80%94%E8%A3%B8%E6%9C%BA%E5%A4%A7%E5%85%A8/">rk3399</a>来做理论上应该可以，但是我在实验时发现不行，下载之后没有相应的串口显示。</p>

<p>查看这个<a href="https://wiki.radxa.com/Rock5/guide/build-u-boot-on-5b">radax build uboot</a>指南之后，我们可以知道：</p>

<p><img src="https://i.imgur.com/zROx0Jn.png" alt="guide" /></p>

<p>经过尝试之后，发现只需要idbloader就可以启动，我们继续看这个idbloader是怎么生成的：</p>

<pre><code class="language-bash=">./tools/mkimage -n rk3588 -T rksd -d ../rkbin/bin/rk35/rk3588_ddr_lp4_2112MHz_lp5_2736MHz_v1.08.bin:spl/u-boot-spl.bin idbloader.img
</code></pre>

<p>在官方提供的sdk中的<strong>mk-uboot.sh</strong>找到上述命令，上面的命令合成了ddr和spl两个文件，spl是用于载入uboot的，我们不需要，所以我们只需要将我们自己的bin文件替换spl文件就可以启动了。</p>

<p>根据文件我们可以看到其实3399的bootcode可以用<strong>mkimage</strong>来制作：</p>
<pre><code class="language-bash=">tools/mkimage -n rk3399 -T rksd -d ../rkbin/bin/rk33/rk3399_ddr_800MHz_v1.25.bin idbloader.img
</code></pre>

<h3 id="使用uboot启动程序">使用uboot启动程序</h3>

<p>直接使用裸机程序出现一个问题：从el3到el2不能成功，即使代码在qemu上面没有问题，只能直接从el3到el1。我感觉可能是没有使用atf的原因，所以想使用uboot来引导，编译的uboot已经包括atf了。</p>

<p>rk3588的uboot并没有支持网络驱动，所以没法nfs或者tftp加载应用程序，需要将编译的应用程序放在sd卡中，利用其他命令加载：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>//保证自己的sd卡是fat格式
//fatload &lt;interface&gt; &lt;device&gt; &lt;load_addr&gt; &lt;filename&gt;
//mmc 表示sd卡或者是emmc 我的机器上1是sd卡 0x8300000是加载到内核的地址
fatload mmc 1 0x8300000 rk3588.bin

//跳转到0x8300000运行
go 0x8300000
</code></pre></div></div>

<p>使用uboot引导之后可以正常下降到el1,但是出现了下降到el1之后，data段被清零了，最开始是在el1使用printf()不能打印全局变量会进入trap，但是在el2使用printf()一切正常，最后定位到data段在进入el1之后清零了。目前不清楚是什么原因导致的，可能el2到el1还有一些特殊操作。我最后用了一个trick：在el2的时候将程序重定位到新的位置，数据段的问题解决了但是暂时不清楚还有没有什么其他的bug。</p>

<p>X开头的是64bits的寄存器 W开头的是32bits寄存器一定要区分开 <strong>rk3588上的外设基本上都是32bit的 不能用X寄存器去写</strong></p>

<h3 id="qemu-virt开发板帮助验证程序">qemu virt开发板帮助验证程序</h3>
<pre><code class="language-cpp=">static const MemMapEntry base_memmap[] = {
    /* Space up to 0x8000000 is reserved for a boot ROM */
    [VIRT_FLASH] =              {          0, 0x08000000 },
    [VIRT_CPUPERIPHS] =         { 0x08000000, 0x00020000 },
    /* GIC distributor and CPU interfaces sit inside the CPU peripheral space */
    [VIRT_GIC_DIST] =           { 0x08000000, 0x00010000 },
    [VIRT_GIC_CPU] =            { 0x08010000, 0x00010000 },
    [VIRT_GIC_V2M] =            { 0x08020000, 0x00001000 },
    [VIRT_GIC_HYP] =            { 0x08030000, 0x00010000 },
    [VIRT_GIC_VCPU] =           { 0x08040000, 0x00010000 },
    /* The space in between here is reserved for GICv3 CPU/vCPU/HYP */
    [VIRT_GIC_ITS] =            { 0x08080000, 0x00020000 },
    /* This redistributor space allows up to 2*64kB*123 CPUs */
    [VIRT_GIC_REDIST] =         { 0x080A0000, 0x00F60000 },
    [VIRT_UART] =               { 0x09000000, 0x00001000 },
    [VIRT_RTC] =                { 0x09010000, 0x00001000 },
    [VIRT_FW_CFG] =             { 0x09020000, 0x00000018 },
    [VIRT_GPIO] =               { 0x09030000, 0x00001000 },
    [VIRT_SECURE_UART] =        { 0x09040000, 0x00001000 },
    [VIRT_SMMU] =               { 0x09050000, 0x00020000 },
    [VIRT_PCDIMM_ACPI] =        { 0x09070000, MEMORY_HOTPLUG_IO_LEN },
    [VIRT_ACPI_GED] =           { 0x09080000, ACPI_GED_EVT_SEL_LEN },
    [VIRT_NVDIMM_ACPI] =        { 0x09090000, NVDIMM_ACPI_IO_LEN},
    [VIRT_PVTIME] =             { 0x090a0000, 0x00010000 },
    [VIRT_SECURE_GPIO] =        { 0x090b0000, 0x00001000 },
    [VIRT_MMIO] =               { 0x0a000000, 0x00000200 },
    /* ...repeating for a total of NUM_VIRTIO_TRANSPORTS, each of that size */
    [VIRT_PLATFORM_BUS] =       { 0x0c000000, 0x02000000 },
    [VIRT_SECURE_MEM] =         { 0x0e000000, 0x01000000 },
    [VIRT_PCIE_MMIO] =          { 0x10000000, 0x2eff0000 },
    [VIRT_PCIE_PIO] =           { 0x3eff0000, 0x00010000 },
    [VIRT_PCIE_ECAM] =          { 0x3f000000, 0x01000000 },
    /* Actual RAM size depends on initial RAM and device memory settings */
    [VIRT_MEM] =                { GiB, LEGACY_RAMLIMIT_BYTES },
};
</code></pre>

<h3 id="rockpi5b-io口">ROCKPI5B IO口</h3>

<p><img src="https://i.imgur.com/LEsVopR.png" alt="rockpi5b io" /></p>

<h3 id="奇怪的错误">奇怪的错误</h3>

<ol>
  <li>freestanding的printf不知道什么时候开始，使用就报错，最后发现是在可变参函数的入栈出了错误<strong>esr=0x1fe00000</strong>，未知错误，检查了sp的对齐没有问题不知道怎么回事，也不是没有isb的问题。最后在<a href="https://forum.osdev.org/viewtopic.php?f=1&amp;t=56542">osdev</a>上发现了和我一样的问题</li>
</ol>

<pre><code class="language-Makefile=">CFLAGS += -mgeneral-regs-only
</code></pre>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />bootcode指南的代码尝试不对</li>
</ul>

<h3 id="注意">注意</h3>

<p>异常处理函数的参数不对会导致错误</p>

<h2 id="2-armv8">2. ARMv8</h2>

<h3 id="special-寄存器">SPECIAL 寄存器</h3>

<p><img src="https://i.imgur.com/f4UYBwC.png" alt="" /></p>

<p><img src="/home/songyj/pics/armv8_special_regs1.png" alt="" /></p>

<h4 id="spsr_elx-saved-process-status-register">SPSR_ELx (Saved Process Status Register)</h4>

<p><strong>SPSR_ELx</strong>寄存器在异常发生时会复制<strong>PSTATE</strong>值，以便在退出异常时恢复。<strong>PSTATE</strong>包括计算标志位（例如溢出，进位等），中断屏蔽位（DAIF），执行状态（aarch64 aarch32），异常出现时运行在异常等级（EL0 EL1 EL2 EL3），使用哪一个<strong>SP</strong>寄存器（sp_el0 sp_elx）。</p>

<h4 id="elr_elx-exception-link-register">ELR_ELx (Exception Link Register)</h4>

<p>异常处理完毕的返回地址。</p>

<h4 id="sp_elx-stack-pointer">SP_ELx (Stack pointer)</h4>

<p>每个异常等级都有对应的SP寄存器，高级别的寄存器都可以使用SP_EL0，需要用户在程序中选择，默认使用SP_ELx。</p>

<h3 id="armv8汇编">armv8汇编</h3>

<h4 id="1-栈操作">1. <a href="https://thinkingeek.com/2017/05/29/exploring-aarch64-assembler-chapter-8/">栈操作</a></h4>

<p>sp的值不会自己加减，需要手动维护，写c时汇编会汇编出sp操作</p>

<pre><code class="language-asm">// x8入栈
str x8, [sp, #-8]!  // *(sp - 8) ← x8
                    // sp ← sp - 8
                    
str x8, [sp, #-8]	//有无感叹号的区别就在于 入栈之后对SP进行操作与否                    
</code></pre>

<pre><code class="language-asm">// x8出栈
ldr x8, [sp], #8   // x8 ← *sp
                   // sp ← sp + 8
</code></pre>

<p>虽然上述通用指令可以完成SP的维护但是这还不够，Armv8的convention要求栈应该以16bytes对其（keep the stack aligned to a multiple of 16）</p>

<pre><code class="language-asm">// x8和x11入栈
stp x8, x11, [sp, #-16]!  // *(sp - 16) ← x8
                          // *(sp - 16 + 8) ← x11
                          // sp ← sp - 16
</code></pre>

<pre><code class="language-asm">// x8和x11出栈
ldp x8, x11, [sp], #16  // x8 ← *sp
                        // x11 ← *(sp + 8)
                        // sp ← sp + 16
</code></pre>

<h4 id="2-adr-adrp-ldr">2. adr adrp ldr</h4>

<p>ldr伪指令加载的是绝对地址(链接脚本中的地址)</p>

<p>adr/adrp加载的是当前程序实际运行的物理地址 adr+-4k adrp+-4G</p>

<p>adrp指令获取label所在的page的起始地址</p>

<h4 id="3-跳转">3. 跳转</h4>

<p>跳转以前没怎么注意，但是跳转是有范围的，在启用了虚拟地址之后如果还是使用<strong>bl b</strong>的话，他们都是<strong>pc-related</strong>所以跳转范围其实只有<strong>+-128m</strong>，需要使用绝对跳转<strong>br</strong></p>

<h2 id="3-异常处理">3. 异常处理</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: left">特权模式</th>
      <th style="text-align: left">Column 2</th>
      <th style="text-align: left">系统调用指令</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">EL0</td>
      <td style="text-align: left">应用程序</td>
      <td style="text-align: left">SVC</td>
    </tr>
    <tr>
      <td style="text-align: left">EL1</td>
      <td style="text-align: left">操作系统</td>
      <td style="text-align: left">HVC</td>
    </tr>
    <tr>
      <td style="text-align: left">EL2</td>
      <td style="text-align: left">hypervisor 虚拟化</td>
      <td style="text-align: left">SMC</td>
    </tr>
    <tr>
      <td style="text-align: left">EL3</td>
      <td style="text-align: left">secure monitor 安全世界</td>
      <td style="text-align: left"> </td>
    </tr>
  </tbody>
</table>

<p>读取current_el返回值对应exception level的关系：
<img src="" alt="" /></p>

<p>最开始一直搞不清armv8的sp sp_el0 sp_elx，sp_sel的bit0决定当前使用的是spel0还是spelx</p>

<p><img src="" alt="" /></p>

<p>在物理上sp寄存器有4g</p>

<p><img src="https://i.imgur.com/T6jpF88.png" alt="" /></p>

<p>通过ARM提供的异常处理流程图可以看出，在异常发生之后，从异常处理的<strong>entry</strong>代码开始就已经进入到同等级或者更高等级的exception level中了，所以在异常处理代码中使用的SP就是和之前不同的SP</p>

<h3 id="1-异常返回">1. 异常返回</h3>

<pre><code class="language-asm">/*执行eret完成异常的返回*/
eret
</code></pre>

<p>执行<strong>eret</strong>时会拷贝<strong>SPSR_ELn</strong>中的状态到<strong>PSTATE</strong>中去，之后程序就开始从<strong>ELR_ELn</strong>中装载的地址运行。</p>

<p>注意：异常的进入保存和退出恢复的顺序很重要 仔细检查 <strong>stp ldp指令基址的加减顺序</strong></p>

<h3 id="2-中断处理">2. 中断处理</h3>

<p>中断属于异步异常(<strong>asynchronous exception</strong>)，在Armv8架构中，进入异常之后会将<strong>中断屏蔽DAIF</strong>全部置0，如果要使能<strong>中断嵌套</strong>需要在读取<strong>intid</strong>之后手动清楚DAIF屏蔽位</p>

<p>进入Handler()之后：</p>

<ol>
  <li>读取<strong>ICC_IAR1_EL1</strong>寄存器 获取对应的中断号</li>
  <li>写中断号到<strong>ICC_EOIR1_EL1</strong> 清除相应的中断标志位</li>
  <li>重新打开DAIF</li>
</ol>

<p><strong>注意：</strong>不需要重新置位<strong>DAIF</strong>，重新置位会造成中断嵌套，退出中断处理时从spsr中自动读取之前保存的状态</p>

<h4 id="1-ppi定时器-spiuart">1. PPI(定时器) SPI(UART)</h4>

<p>问题：uart中断使能接收中断之后，使用键盘给rk3588发送字符导致程序暂停，最开始以为是进入到其他的中断处去了，最后发现是与UART的状态位冲突，卡死在uart_send_char()的while循环中，在发送标志位检测中添加超时机制，解决。</p>

<p>esr = 0x96000046 stp一般是是使用了空指针 或者是操作了没有mmu映射的内存。</p>

<p>esr = 0x96000047 ldp</p>

<h2 id="2-gic-500-gic-600">2. GIC-500 GIC-600</h2>

<p>rk3399使用gic500，rk3588使用gic600。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//rk3588s.dtsi</span>
<span class="n">gic</span><span class="o">:</span> <span class="n">interrupt</span><span class="o">-</span><span class="n">controller</span><span class="err">@</span><span class="n">fe600000</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"arm,gic-v3"</span><span class="p">;</span>
    <span class="cp">#interrupt-cells = &lt;3&gt;; 
</span>    <span class="cp">#address-cells = &lt;2&gt;; 
</span>    <span class="cp">#size-cells = &lt;2&gt;; 
</span>    <span class="n">ranges</span><span class="p">;</span>
    <span class="n">interrupt</span><span class="o">-</span><span class="n">controller</span><span class="p">;</span>

    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0xfe600000</span> <span class="mi">0</span> <span class="mh">0x10000</span><span class="o">&gt;</span><span class="p">,</span> <span class="cm">/* GICD */</span>
          <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0xfe680000</span> <span class="mi">0</span> <span class="mh">0x100000</span><span class="o">&gt;</span><span class="p">;</span> <span class="cm">/* GICR */</span>
    <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="n">GIC_PPI</span> <span class="mi">9</span> <span class="n">IRQ_TYPE_LEVEL_HIGH</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="nl">its0:</span> <span class="n">msi</span><span class="o">-</span><span class="n">controller</span><span class="err">@</span><span class="n">fe640000</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"arm,gic-v3-its"</span><span class="p">;</span>
        <span class="n">msi</span><span class="o">-</span><span class="n">controller</span><span class="p">;</span>
        <span class="cp">#msi-cells = &lt;1&gt;; 
</span>        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0xfe640000</span> <span class="mh">0x0</span> <span class="mh">0x20000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="p">};</span>
    <span class="nl">its1:</span> <span class="n">msi</span><span class="o">-</span><span class="n">controller</span><span class="err">@</span><span class="n">fe660000</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"arm,gic-v3-its"</span><span class="p">;</span>
        <span class="n">msi</span><span class="o">-</span><span class="n">controller</span><span class="p">;</span>
        <span class="cp">#msi-cells = &lt;1&gt;; 
</span>        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0xfe660000</span> <span class="mh">0x0</span> <span class="mh">0x20000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="p">};</span>
<span class="p">};</span>  
</code></pre></div></div>

<h3 id="1-中断类型">1. 中断类型</h3>

<ul>
  <li><strong>SPI (Shared Peripheral Interrupt)</strong></li>
</ul>

<p>共享的外设中断，每一个SPI都可以被route到一个核心，比如uart</p>

<ul>
  <li><strong>PPI (Private Peripheral Interrupt)</strong></li>
</ul>

<p>每个核心私有的外设中断，比如cortex-a53上的PS定时器等</p>

<ul>
  <li><strong>SGI (Software Generated Interrupt)</strong></li>
</ul>

<p>主要用于核间通信</p>

<ul>
  <li><strong>LPI (Locality-specific Peripheral Interrupt)</strong></li>
</ul>

<p>用于PCIe？</p>

<h3 id="2-gic结构">2. GIC结构</h3>

<p><img src="https://i.imgur.com/K5pcwv2.png" alt="GIC struct" /></p>

<ul>
  <li><strong>Distributor</strong></li>
</ul>

<p>主管<strong>SPI</strong>和<strong>secure</strong></p>

<p>一个<strong>SoC</strong>只有一个<strong>Distributor</strong>进行一些全局设置，主要和<strong>SPI</strong>相关，所有的核心共用的外设如果发出了中断请求，这个中断请求根据具体情况被发送到指定的核心，从这个结构图上来看也只有<strong>Distributor</strong>可以完成这个任务，因为是全局设置所以还有一些<strong>secure</strong>相关的设置也在这里。</p>

<p>设置个</p>

<ul>
  <li><strong>Redistributor</strong></li>
</ul>

<p>主管<strong>PPI</strong>和<strong>SGI</strong></p>

<p>每个核心都有对应的<strong>Redistributor</strong>，在这里设置私有中断应该是比较合适的</p>

<ul>
  <li><strong>CPU Interface</strong></li>
</ul>

<p>主管<strong>priority</strong></p>

<p>上面两块的设置都不涉及到中断优先级</p>

<h3 id="3-初始化gic">3. 初始化GIC</h3>

<p>mpidr_el1对应aff编号（read only）</p>

<ol>
  <li>
    <p>初始化<strong>Distributor</strong></p>
  </li>
  <li>
    <p>初始化<strong>Redistributor</strong></p>
  </li>
  <li>
    <p>初始化<strong>CPU</strong></p>
  </li>
</ol>

<h2 id="4-内核空间用户空间的映射">4. 内核空间用户空间的映射</h2>

<h3 id="1-mmu基本">1. MMU基本</h3>

<p>rk3588的trm中提到有两个MMU600实例，但是这并不是用来操作普通内存地址的，其中一个链接到pcie高速外设另一个php链接到sata usb3等。普通的操作地址使用cortex a核心内部的MMU。</p>

<ul>
  <li><strong>TCR_EL1</strong> 页表设置 4k 16k 64k 48位 39位等</li>
  <li><strong>MAIR_EL1</strong> 内存权限</li>
  <li><strong>TTBR0_EL1</strong> 存放pagetable的地址</li>
  <li><strong>TTBR1_EL1</strong> 存放pagetable的地址</li>
</ul>

<p>有两个页表基址寄存器那么转换的时候用那一个呢？</p>

<p>虚拟地址VA在<strong>(0x00, 0x0000FFFFFFFFFFFF)</strong>使用<strong>TTBR0_EL1</strong></p>

<p>虚拟地址VA在<strong>(0xFFFF000000000000, 0xFFFFFFFFFFFFFFFF)</strong>使用<strong>TTBR1_EL1</strong></p>

<p>需要注意的是：尽管有64位宽的地址和数据总线但是ARMv8要求在48位宽内完成映射</p>

<p><img src="https://i.imgur.com/ZiwsVVe.png" alt="" /></p>

<p>mmu使能之前至少需要完成3步设置：</p>

<ol>
  <li>设置<strong>tcr_el1</strong></li>
  <li>设置<strong>mair_el1</strong></li>
  <li>设置<strong>ttbr0_el1 ttbr1_el1</strong></li>
</ol>

<p>问题：使能MMU之后，多次进入串口中断会触发PC没有对齐的异常。后来检查发现内存映射没有做好，之前完整映射时一直有remap没有检查，只是少映射了数据段的长度，串口打印时会使用到没有映射地址的数据，导致异常。解决：检查映射后更改，不再触发。</p>

<h3 id="2-映射kernel代码到内核空间">2. 映射kernel代码到内核空间</h3>

<p>uboot引导内核之后，内核一直运行在低地址，这个地方是操作系统预留给用户程序的空间，我们需要尽快将内核映射到<strong>内核空间(0xffff000000000000)</strong>去。操作MMU完成两个页表的映射，<strong>_pagetable0</strong>用于完成<strong>1：1映射(identical mapping)</strong>以保证在开启MMU之后的下一条指令仍然能够正常运行不触发page fault。<strong>_pagetable1</strong>用于映射内核代码的物理地址到内核空间，映射完成之后使用绝对跳转到高地址执行内核。</p>

<pre><code class="language-asm">adr x0, before_kernel	/*完成映射并开启MMU*/
blr x0

ldr x0, =kernel_main	/*跳转到kernel_main链接地址去执行*/
br x0
</code></pre>

<p><strong>问题：</strong>页表切换之后不能访问，但是如果在原来的页表上增加映射又可以继续执行，没有设置data尚不清楚机制，排查发现是忘记更换堆栈指针到高地址。最开始使用的SP空间一直在低地址，切换了低地址页表之后SP的空间不能访问。在完成内核映射到高地址之后就应该重新设置SP到高地址了。</p>

<h3 id="3-配置页表">3. 配置页表</h3>

<p>和之前RISCV上完成的<strong>等比映射</strong>（虚拟地址=物理地址）相比，在RK上我使用了<strong>直接映射</strong>（虚拟地址=物理地址+固定偏移）的方式，这样需要对原有的<strong>map_page()</strong>函数改进。原因在于进程的<strong>页表基址</strong>和<strong>页表项中存储的地址</strong>都是<strong>物理地址</strong>，但是操作这些页表需要虚拟地址。</p>

<h2 id="5-系统调用框架">5. 系统调用框架</h2>

<h3 id="1-svc">1. SVC</h3>

<p>与<strong>RISCV</strong>不同的是，<strong>RISCV</strong>中<strong>ecall</strong>指令传递参数的方式是直接通过<strong>x0</strong>寄存器，<strong>Armv8</strong>中使用<strong>svc #imm</strong>直接将数字硬编码到指令中，参数的传递通过<strong>ESR</strong>寄存器完成，<strong>ESR寄存器在面对不同的异常类型时，字段的解码方式不同</strong>。</p>

<p><img src="/image/ESR.png" alt="ESR info" /></p>

<p>系统调用的具体参数就遵从<strong>armv8</strong>的<strong>call procedure ABI</strong>，x0到x7存放param，x8返回值。</p>

<h3 id="2函数调用标准procedure-call-standard">2.函数调用标准(Procedure Call Standard)</h3>

<table>
  <thead>
    <tr>
      <th style="text-align: center">寄存器</th>
      <th style="text-align: center">描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">x0~x7</td>
      <td style="text-align: center">传递参数和结果，如果参数多于8个，使用stack</td>
    </tr>
    <tr>
      <td style="text-align: center">x8</td>
      <td style="text-align: center">间接结果，例如返回结构体的指针</td>
    </tr>
    <tr>
      <td style="text-align: center">x30（LR）</td>
      <td style="text-align: center">函数返回地址</td>
    </tr>
  </tbody>
</table>

<h2 id="6-user-kernel-transition--context-switch">6. User-Kernel Transition &amp; Context Switch</h2>

<p><img src="/image/armv8切换进程过程.png" alt="" /></p>

<h3 id="1-struct-context">1. struct context</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">context</span>
<span class="p">{</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">reg</span><span class="p">[</span><span class="mi">11</span><span class="p">];</span> <span class="cm">/* x19~x28 x29(FP)*/</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">sp</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">lr</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>在<strong>context</strong>结构体的定义中没有PC寄存器的值，如何返回到另一个进程之前运行的地方？<strong>swtch</strong>函数最后使用<strong>ret</strong>指令返回到<strong>lr</strong>中装载的地址，而<strong>lr</strong>中的地址正好是上一次进入<strong>swtch</strong>函数时装载的，所以<strong>ret</strong>之后刚好是<strong>swtch</strong>执行之后。</p>

<h3 id="2-struct-trapframe">2. struct trapframe</h3>

<p><strong>trapframe</strong>是发生异常时的<strong>cpu</strong>状态快照，需要保存所有的寄存器。异常发生之后，在异常入口按照<strong>trapframe</strong>的结构将<strong>trapframe</strong>压栈或者存储在内存页中（xv6-riscv）。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">trap_frame</span>
<span class="p">{</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">general_reg</span><span class="p">[</span><span class="mi">30</span><span class="p">];</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">lr</span><span class="p">;</span>       <span class="c1">//x30</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">sp_el0</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">spsr_el1</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">elr_el1</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">esr_el1</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">trap_type</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<h3 id="3-trapframe和context有什么区别">3. <strong>trapframe</strong>和<strong>context</strong>有什么区别？</h3>

<p><strong>处理器上下文和进程上下文</strong></p>

<p>在<strong>xv6-riscv</strong>中<strong>trapframe</strong>是以一页的形式映射在用户空间的，每一个用户进程都有一页单独存放<strong>trapframe</strong>。<strong>trapframe</strong>顾名思义也就是在异常发生的那一刻对于<strong>cpu</strong>状态的保存，当异常处理完成之后，我们需要返回到<strong>cpu</strong>之前的状态继续执行被打断的程序。</p>

<p><img src="https://i.imgur.com/4jDqPWc.png" alt="" /></p>

<p>参考xv6在用户层异常进入和退出的代码就很容易明白trapframe的含义：</p>

<pre><code class="language-asm">/*uservec是用户空间中发生异常时的异常入口*/
uservec:
	csrw sscratch, a0
  
	li a0, 0x3fffffe000
  
    sd ra, 40(a0)
    sd sp, 48(a0)
    sd gp, 56(a0)
    ...
    sd t3, 256(a0)
    sd t4, 264(a0)
    sd t5, 272(a0)
    sd t6, 280(a0)
</code></pre>

<pre><code class="language-asm">userret:
    sfence.vma zero, zero	/*更换页表*/
    csrw satp, a0
    sfence.vma zero, zero

    li a0, 0x3fffffe000		/*加载trapframe地址到a0*/

    ld ra, 40(a0)			/*从trapframe中恢复寄存器的值*/
    ld sp, 48(a0)
    ld gp, 56(a0)
    ld tp, 64(a0)
	...
	ld t5, 272(a0)
    ld t6, 280(a0)
  
    ld a0, 112(a0)
    sret					/*sret返回*/
</code></pre>

<p>为什么专门有这样一个结构用特殊的一页来存储或者说为什么不直接存储在<strong>stack</strong>中？</p>

<p><strong>riscv</strong>在<strong>qemu virtborad</strong>的实现中只有一个<strong>SP</strong>寄存器，因此三个异常等级是共用这一个<strong>SP</strong>寄存器的，在user阶段的异常涉及到异常等级的上升下降，每个异常等级都会使用到不同的<strong>stack</strong>，也就是说会更换<strong>SP</strong>寄存器中的值。如果不涉及到异常等级的改变，例如在kernel的运行中出现异常，<strong>trapframe</strong>就是直接存储在<strong>stack</strong>当中的：</p>

<pre><code class="language-asm">/*kernelvec是内核空间中发生异常时的异常入口*/
kernelvec:    
    addi sp, sp, -256		/*make room to save registers*/
    
    sd ra, 0(sp)			/*save the registers*/
    sd sp, 8(sp)
    sd gp, 16(sp)
    sd tp, 24(sp)
    sd t0, 32(sp)    
    ...
    call kerneltrap			/*trap handler in c*/
    ...
    
    ld t4, 224(sp)			/*restore the registers*/
    ld t5, 232(sp)
    ld t6, 240(sp)

    addi sp, sp, 256
    sret        			/*return to whatever we were doing in the kernel*/
</code></pre>

<p>所以<strong>trapframe</strong>肯定是需要的，因为<strong>trapframe</strong>是用来恢复异常之前的<strong>CPU</strong>状态的，但是<strong>trapframe</strong>不一定要像<strong>RISCV</strong>当中一样存储在单独的一页中，他也有可能存储在<strong>stack</strong>中。</p>

<p>在ARMv8中，每个异常等级都有自己的<strong>stack</strong>，因此我选择将<strong>trapframe</strong>直接存储在内核栈当中。</p>

<p><strong>问题1：</strong>切换进程的定时器1s一次的时候，进程切换正常，但是提高频率之后trap，且有几率trap错位置（在el1却进入了el0的trap函数，甚至elr=0x01）？排查发现trapframe栈帧有问题设置短了。</p>

<p><strong>问题2：</strong>程序在内核和用户空间执行时定时器中断都有可能发生，在内核空间切换trap？内核的切换必须在第一次用户切换之后，不然会加载空的context，设置标志位暂时解决。</p>

<h3 id="4-用户内核切换">4. 用户内核切换</h3>

<p>用户空间内核空间的切换实际上就是依赖异常的进入和退出完成的，用户空间和内核空间的切换主要出现在两个时候：</p>

<ul>
  <li><strong>syscall</strong></li>
  <li><strong>interrupt</strong></li>
</ul>

<h3 id="5-上下文切换">5. 上下文切换</h3>

<p>上下文切换都发生在内核空间也是两个时候：</p>

<ul>
  <li><strong>进程的内核部分切换上下文回到scheduler进程的状态</strong></li>
  <li><strong>scheduler进程切换上下文去到新进程的内核状态</strong></li>
</ul>

<h2 id="7-文件系统">7. 文件系统</h2>

<p><img src="/image/文件系统框架.png" alt="" /></p>

<h3 id="1-硬件驱动">1. 硬件驱动</h3>

<h4 id="1-sdmmc">1. SDMMC</h4>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />sdmmc驱动</li>
</ul>

<h4 id="2-spi">2. SPI</h4>

<p>SPI驱动一定要拉低片选才能通信</p>

<p><strong>问题：发送cmd0之后一直返回0xff？</strong></p>

<p>发送完cmd的6个byte之后不能马上进行读取操作，<strong>需要再发送一次或者多次0xff等待sd卡响应</strong>。sd卡每次发送命令之前最好先发送一个<strong>0xff</strong></p>

<p><strong>问题：读写块不对，只有0正常其他块错误？</strong></p>

<p>读写命令SDHC卡只需要块号就行了 不需要转化成字节地址</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />使用DMA pl330</li>
</ul>

<h4 id="3-lru磁盘缓存">3. LRU磁盘缓存</h4>

<p>使用双向链表完成，头尾各有一个哨兵节点，哈希表暂时没有实现，缓存容量是10个想把几万个blk映射进来不好完成。采用遍历的方式寻找缓存中是否有需要的节点。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">node</span>
<span class="p">{</span>
    <span class="kt">int</span> <span class="n">blk_num</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">key</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">buf</span> <span class="n">data</span><span class="p">;</span>	<span class="c1">//存放块数据</span>

    <span class="k">struct</span> <span class="n">node</span> <span class="o">*</span><span class="n">nxt</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">node</span> <span class="o">*</span><span class="n">pre</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="n">LRU_cache</span>
<span class="p">{</span>
    <span class="kt">int</span> <span class="n">capacity</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">size_cnt</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">node</span> <span class="o">*</span><span class="n">head</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">node</span> <span class="o">*</span><span class="n">tail</span><span class="p">;</span>

    <span class="k">struct</span> <span class="n">node</span> <span class="o">**</span><span class="n">hush</span><span class="p">;</span> <span class="c1">//哈希表存储键值对</span>
<span class="p">};</span>
</code></pre></div></div>

<p><img src="https://i.imgur.com/2Kiwwwb.jpg" alt="例如" /></p>

<h3 id="2调试">2.调试</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#读取sd卡中的前5个blk 每个blk大小512byte 用hexdump打印 -C显示字符</span>
<span class="nb">sudo dd </span><span class="k">if</span><span class="o">=</span>/dev/sdb <span class="nv">bs</span><span class="o">=</span>512 <span class="nv">count</span><span class="o">=</span>5 | hexdump <span class="nt">-C</span>

<span class="c">#向sd卡写入文件mydisk.img 每个blk大小512byte seek=0跳过0个blk</span>
<span class="nb">sudo dd </span><span class="k">if</span><span class="o">=</span>mydisk.img <span class="nv">of</span><span class="o">=</span>/dev/sdb <span class="nv">bs</span><span class="o">=</span>512 <span class="nv">seek</span><span class="o">=</span>0
</code></pre></div></div>

<h2 id="8-具体系统调用实现">8. 具体系统调用实现</h2>

<h3 id="1-fork">1. fork()</h3>

<h3 id="2-exec">2. exec()</h3>

<p>Linux中的exec系统调用具体有几个，参数的传递有区别：</p>

<ul>
  <li><strong>l: list</strong></li>
  <li><strong>v: vector(array)</strong></li>
  <li><strong>p: path</strong></li>
  <li><strong>e: environment</strong></li>
</ul>

<h4 id="动态链接">动态链接</h4>

<p>加载静态链接的程序很简单，读取到程序内容之后复制并跳转到entry就可以了。动态链接的程序不一样，动态链接会首先加载<strong>.interp</strong>中指定的<strong>动态连接器</strong></p>

<h3 id="3-open">3. open()</h3>

<p>sys_open()只返回一个<strong>fd号</strong>，根据这个<strong>fd号</strong>可以找到<strong>struct file</strong>，<strong>struct file</strong>当中存有指向<strong>缓存在RAM中的inode</strong>的指针，这样就确定了打开的文件。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//用户使用open函数</span>
<span class="n">open</span><span class="p">(</span><span class="s">"./example.txt"</span><span class="p">,</span> <span class="n">O_RDONLY</span><span class="p">)</span>
</code></pre></div></div>

<p>open的系统调用涉及到用户空间往内核空间传参，如果是一个值比如第二个O_RDONLY比较好理解，直接在trapframe中按照顺序找到寄存器的值就行了。假如不是一个值而是类似字符串这样的，我们再去查找寄存器只能找到一个用户空间的地址。内核需要将这个用户空间的地址转换成实际的内核空间中的虚拟地址。</p>

<h4 id="1-copy_from_user">1. copy_from_user()</h4>

<p>copy_from_user寻找地址主要有三步：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//根据用户空间的地址va来寻找实际的物理地址pa所在的pte</span>
<span class="n">pte</span> <span class="o">=</span> <span class="n">walk</span><span class="p">(</span><span class="n">user_pagetable</span><span class="p">,</span> <span class="n">va</span><span class="p">);</span>
<span class="c1">//pte中有标志位需要去除</span>
<span class="n">pa</span> <span class="o">=</span> <span class="n">pte</span> <span class="o">&amp;</span> <span class="n">MASK</span><span class="p">;</span>
<span class="c1">//pte中存储的是绝对的物理地址，但是内核空间一般都在高地址，有一定偏移pa_to_va(pa)是加上偏移</span>
<span class="n">pa</span> <span class="o">=</span> <span class="n">pa_to_va</span><span class="p">(</span><span class="n">pa</span><span class="p">);</span>
</code></pre></div></div>

<p>实际上页表是最小4k对其的，我们查询的时候会将用户空间的va向下0x1000取整查询，查询到之后再找回去取整前的位置</p>

<h3 id="4-read">4. read()</h3>

<h3 id="5-write">5. write()</h3>

<h3 id="6-mmap">6. mmap()</h3>

<p>mmap()用于给用户空间添加新的映射</p>

<h2 id="9-用户程序">9. 用户程序</h2>

<p>在0号程序运行起来会呼叫init程序一般是一个shell存储在ROM上，如何编译并且运行这个用户程序？</p>

<h3 id="1-readelf">1. readelf</h3>

<h2 id="10-调度器scheduler">10. 调度器(Scheduler)</h2>

<p><strong>时间片轮转—&gt;O(n)—&gt;O(1)—&gt;CFS</strong></p>

<h2 id="11-多核">11. 多核</h2>

<h3 id="1-mpidr_el1">1. mpidr_el1</h3>

<h3 id="2-锁">2. 锁</h3>

<h4 id="1-mutex">1. MUTEX</h4>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span><span class="p">(</span><span class="o">!</span><span class="n">try_lock</span><span class="p">())</span>
<span class="p">{</span>
    <span class="n">sleep</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<p>互斥锁得不到会睡眠，比如wfe进入低功耗</p>

<h4 id="2-spinlock">2. SPINLOCK</h4>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span><span class="p">(</span><span class="o">!</span><span class="n">try_lock</span><span class="p">())</span>
<span class="p">{}</span>
</code></pre></div></div>

<p>spinlock会不断尝试及</p>

<h2 id="6-更多">6. 更多</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#使用find工具查找可能的文件</span>
find /home/user/documents <span class="nt">-type</span> f <span class="nt">-name</span> <span class="s2">"*.txt"</span>
</code></pre></div></div>

<p><strong>minicom</strong> 默认似乎是不开启line warp 这样超出终端也不会自动换行</p>

<h2 id="7-参考">7. 参考</h2>

<p><a href="https://github.com/s-matyukevich/raspberry-pi-os">s-matyukevich/rpios</a></p>

<p><a href="https://blog.csdn.net/qq_30095023/article/details/131591790">sd驱动</a></p>

<p><a href="https://www.0xaa55.com/thread-25692-1-1.html">sd/mmc</a></p>

<p><a href="https://linuxtools-rst.readthedocs.io/zh_CN/latest/index.html">Linux tool</a></p>

<p><a href="https://www.sobyte.net/post/2022-03/mmap/">mmap</a></p>

<p><a href="https://course.ccs.neu.edu/cs3650sp22/resources.html">neu cs3650</a></p>

<p><a href="https://intezer.com/blog/malware-analysis/executable-linkable-format-101-part-4-dynamic-linking/">dynamic linking</a></p>

<p><a href="https://www.intel.com/content/www/us/en/docs/programmable/683711/21-2/sd-mmc-controller-42838.html">sdmmc同款ip</a></p>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[rk系列裸机]]></summary></entry><entry><title type="html">Riscv_xv6</title><link href="https://songyj123.github.io/riscv-xv6.html" rel="alternate" type="text/html" title="Riscv_xv6" /><published>2023-05-01T00:00:00+08:00</published><updated>2023-05-01T00:00:00+08:00</updated><id>https://songyj123.github.io/RISCV_xv6</id><content type="html" xml:base="https://songyj123.github.io/riscv-xv6.html"><![CDATA[<h1 id="rvos">RVOS</h1>
<!--more-->

<p><a href="https://sourceware.org/binutils/docs/as/index.html">汇编器AS</a>
反汇编</p>

<pre><code class="language-bash=">riscv64-unknown-elf-objdump -D -b binary -m riscv test.bin &gt; disassembly_out
</code></pre>

<h1 id="1-基础">1. 基础</h1>

<h2 id="vim">vim</h2>

<p>替换</p>

<pre><code class="language-bash=">#全局替换整个文件中的 old 为 new
%s/old/new/g 
</code></pre>

<h2 id="11-gcc的使用">1.1 GCC的使用</h2>

<h2 id="12-gdb的使用">1.2 GDB的使用</h2>

<h3 id="1-gdbinit文件">1. gdbinit文件</h3>

<p>使用gdbinit文件作为默认配置文件，帮你自动输入一些命令：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">set </span>disassemble-next-line on    
target remote localhost:1234
layout regs
b _start
</code></pre></div></div>

<p>要使当前的gdbinit文件生效需要将~/.gdbinit配置文件中加上当前文件的路径，否则gdbinit文件不能生效：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>add-auto-load-safe-path /home/songyj/embedded_proj/xv6-riscv/.gdbinit                                                             add-auto-load-safe-path /home/songyj/embedded_proj/rk3399/.gdbinit
</code></pre></div></div>

<p>启动gdb并使用当前的配置文件：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gdb tui ./rk3399.elf <span class="nt">-x</span> ./.gdbinit
</code></pre></div></div>

<h3 id="2-具体命令">2. 具体命令</h3>

<p>examine: inspect memory contents</p>

<pre><code class="language-bash=">x/nfu addr
</code></pre>

<p>n: count
f: format
u: unit size</p>

<p>只观察thread 1到达断点</p>

<pre><code class="language-bash=">break _start thread 1
</code></pre>

<p><a href="https://doc.ecoscentric.com/gnutools/doc/gdb/Threads.html">Debug Multithread Program with GDB</a></p>

<p>在某个地址打断点</p>

<pre><code class="language-bash=">b *0x80000000
</code></pre>
<p><strong><em>后面程序会运行到用户空间，想debug用户空间的程序必须要重新加载符号表!</em></strong></p>
<pre><code class="language-c=">file ./user/first_test.out
</code></pre>

<p>调试用户空间的程序也很重要：<a href="https://www.cnblogs.com/KatyuMarisaBlog/p/13727565.html">debug用户程序</a></p>

<h3 id="3-更多">3. 更多</h3>

<p><a href="https://cs.brown.edu/courses/cs033/docs/guides/gdb.pdf">brown gdb cheatsheet</a></p>

<h2 id="13-makefile">1.3 Makefile</h2>

<p>自动化变量 <strong>Automatic varibles</strong></p>

<ul>
  <li>$@ 指代生成的目标文件 target filename</li>
  <li>$&lt; 指代第一材料文件 1st prerequisit filename</li>
  <li>$^ 指代所有的材料文件</li>
</ul>

<h2 id="14-linkerscript">1.4 Linkerscript</h2>

<p>链接的代码段最前面的一定要自己指定好
刚才重写了sbi的链接脚本 去掉了之前指定sbi_boot.s代码段在最前面的标号 直接所有用text表示结果gdb调试进不去 发现本来sbi_boot的代码段链接在sbi_main.c代码段后面去了</p>

<h3 id="1-align">1. ALIGN()</h3>

<h3 id="2-在源文件中使用链接脚本当中的变量">2. 在源文件中使用链接脚本当中的变量</h3>

<pre><code class="language-linkersciprt=">_s_text = 0x80000000;
</code></pre>

<p>在c源文件中访问 <strong>_s_text</strong></p>

<pre><code class="language-cpp=">extern unsigned char _s_text;

unsigned char *value_in_var;
value_in_var = &amp;_s_text;
</code></pre>

<p>可以将这个特殊的变量 <strong>_s_text</strong> 看成是一个空值的变量，他在symbol table上的对应的地址是0x80000000
我们访问一个变量的地址用&amp;
<a href="https://sourceware.org/binutils/docs/ld/Source-Code-Reference.html">参考</a></p>

<h2 id="15-汇编和c">1.5 汇编和C</h2>

<h3 id="1-汇编">1. 汇编</h3>

<h4 id="大端小端-big-endianlittle-endian-讲的是字节不是位">大端小端 big endian/little endian 讲的是字节不是位</h4>

<pre><code class="language-asm=">128 64 32 16 8 4 2 1 is big endian，big-end comes first
1 2 4 8 16 32 64 128 is little endian，little-end comes first 低位字节在前面
</code></pre>

<h4 id="字节对齐-align">字节对齐 align</h4>

<pre><code class="language-asm=">//字节对齐
.align size-log-2
</code></pre>

<h4 id="存储指令">存储指令</h4>

<pre><code class="language-asm=">    #存储指令
    #sd rs1, offset(rs2)
    #往0x80000000上写0
    li a0, 0x80000000
    sd zero, (a0)
</code></pre>

<h4 id="call-j-jr">call j jr</h4>

<p>call和j指令需要特别注意，call是一条伪指令相当于把pc+4放入ra</p>

<pre><code class="language-asm=">    #函数调用指令 call
    call add_ab
    #等价于
    #ra &lt;-- pc+4
    #j add_ab
  
    #函数返回指令 ret
    ret
    #等价于
    #pc &lt;-- ra
</code></pre>

<p><strong>jr</strong>用于跳转寄存器里面存的地址</p>

<h4 id="auipc-lui">AUIPC LUI</h4>

<p>Add Upper Immediate to Program Counter
Load Upper Immediate</p>

<pre><code class="language-asm=">    #将立即数带符号左移12bits加到PC
    auipc a5, 0x02
    #将立即数带符号左移12bits
    lui a5, 0x02
</code></pre>

<h3 id="2-c语法">2. C语法</h3>

<p>函数指针</p>

<pre><code class="language-cpp=">//func_ptr_name是一个指向函数的指针，这个函数有两个int参数，返回的值是int
int (*func_ptr_name)(int, int);

//cast
//some_ptr是一个函数指针
(int (*)(int, int))some_ptr;
</code></pre>

<pre><code class="language-cpp=">//函数指针数组
//类似[SYS_fork]是索引
static uint64 (*syscalls[])(void) = {
[SYS_fork]    sys_fork,
[SYS_exit]    sys_exit,
[SYS_wait]    sys_wait,
[SYS_pipe]    sys_pipe,
[SYS_read]    sys_read,
[SYS_kill]    sys_kill,
[SYS_exec]    sys_exec,
[SYS_fstat]   sys_fstat,
[SYS_chdir]   sys_chdir,
[SYS_dup]     sys_dup,
[SYS_getpid]  sys_getpid,
[SYS_sbrk]    sys_sbrk,
[SYS_sleep]   sys_sleep,
[SYS_uptime]  sys_uptime,
[SYS_open]    sys_open,
[SYS_write]   sys_write,
[SYS_mknod]   sys_mknod,
[SYS_unlink]  sys_unlink,
[SYS_link]    sys_link,
[SYS_mkdir]   sys_mkdir,
[SYS_close]   sys_close,
};

//调用
syscalls[num]();
</code></pre>

<h3 id="3-riscv的寄存器">3. riscv的寄存器</h3>

<h4 id="general-reg">General reg</h4>

<p>通用寄存器有别名和特殊用途</p>

<table>
  <thead>
    <tr>
      <th>通用寄存器</th>
      <th>别名</th>
      <th>特殊用途</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>x0</td>
      <td>zero</td>
      <td><strong>全零寄存器</strong></td>
    </tr>
    <tr>
      <td>x1</td>
      <td>ra</td>
      <td><strong>函数返回地址</strong></td>
    </tr>
    <tr>
      <td>x2</td>
      <td>sp</td>
      <td><strong>stack ptr</strong></td>
    </tr>
    <tr>
      <td>x3</td>
      <td>gp</td>
      <td><strong>globle ptr</strong></td>
    </tr>
    <tr>
      <td>x5~x7, x28~x31</td>
      <td>t0~t2, t3~t6</td>
      <td><strong>tempories</strong> Callee可能会使用不保证在函数调用过程中不变</td>
    </tr>
    <tr>
      <td>x8~x9, x18~x27</td>
      <td>s0~s1, s2~s11</td>
      <td><strong>saved</strong> Callee如果要使用必须在stack中备份，退出时恢复</td>
    </tr>
    <tr>
      <td>x10~x11</td>
      <td>a0~a1</td>
      <td><strong>argument</strong> 参数寄存器 函数返回值也在里面</td>
    </tr>
    <tr>
      <td>x12~x17</td>
      <td>a2~a7</td>
      <td><strong>argument</strong> 参数寄存器</td>
    </tr>
  </tbody>
</table>

<h4 id="csr">CSR</h4>

<table>
  <thead>
    <tr>
      <th>CSR</th>
      <th>用途</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>msratch</td>
      <td>m模式下上下文数据指针</td>
    </tr>
    <tr>
      <td>mcause</td>
      <td>m模式异常中断</td>
    </tr>
    <tr>
      <td>satp</td>
      <td>s模式页表指针</td>
    </tr>
    <tr>
      <td>scause</td>
      <td>s模式异常中断</td>
    </tr>
    <tr>
      <td>sepc</td>
      <td>s模式异常中断处理之后返回的地址</td>
    </tr>
    <tr>
      <td>stvec</td>
      <td>s模式异常中断进入的地址</td>
    </tr>
  </tbody>
</table>

<h4 id="stack-versus-heap">Stack versus Heap</h4>

<p>都存在于RAM当中</p>

<p>每一个线程都有自己的stack 所有的线程共享同一个heap</p>

<p>stack是确定的长度不能改变 heap的长度可以改变 如果需要heap存一个新的变量但是没有足够的空间 操作系统可以分配新的空间给到heap</p>

<pre><code class="language-cpp=">// 分配在数据段 如果等于0在bss 如果不等于0在data
int someGlobalVariable;

// 分配在数据段
static int someStaticVariable;

void MyFunction(int someArgument) {

    // 分配在数据段
    static int someLocalStaticVariable;

    // 分配在栈上
    int someLocalVariable;
    int* someDynamicVariable;

    // 分配在堆上
    someDynamicVariable = new int;

    delete someDynamicVariable;

    return;
}

// Note that someGlobalVariable, someStaticVariable and
// someLocalStaticVariable continue to exist, and are not
// deallocated until the program exits.

</code></pre>

<h5 id="stack-trace栈回溯">stack trace(栈回溯)</h5>

<p>当我们出现异常时，os通常可以打印出一系列函数调用的信息，这是怎么做到的？</p>

<p>编译器使能frame pointer之后(-fno-omit-frame-pointer)，每次进入函数会有如下汇编代码生成：</p>

<pre><code class="language-asm=">    #stack腾出16byte的空间
    #先放函数返回地址ra（8byte），再放fp（8byte）
    #此时的fp存储的是上一个函数分配的stack空间的起始地址
    #更新fp=当前函数被分配的stack空间起始地址
    addi sp, sp, -16
    sd ra, 8(sp)
    sd fp, 0(sp)
    addi fp, sp, 16
</code></pre>

<p>stack是从高地址向低地址生长，所以在用指针去访问存储在stack当中的结构体时要注意</p>

<pre><code class="language-cpp=">//将上述fp和ra看成一个结构体
//因为stack和地址是反的 从地址的角度来看低地址放的是fp高地址放的是ra
struct stack_frame
{
    unsigned long sf_fp;  
    unsigned long sf_ra;
};

void back_trace()
{
    //取得当前的fp值
    //__builtin_frame_address gcc自带特性
    unsigned long cur_fp = __builtin_frame_address(0);
    unsigned long pc;
  
    //通过结构体指针去访问stack frame
    struct stack_frame *sf_ptr = cur_fp - 16;
  
    //循环条件在代码段中
    while(sf_ptr-&gt;sf_fp &gt; &amp;_stext)
    {
        //返回地址减去4就是程序的调用地址
        pc = sf_ptr-&gt;sf_ra - 4;
        printf("pc: %p\n", pc);
        //指向下一个stack frame
        sf_ptr = sf_ptr-&gt;sf_fp - 16;
    }
}
</code></pre>

<p>思考：为什么计算机的pc每次都是+4而不是1？</p>

<p>指令长度4byte</p>

<h3 id="152-static-inline">1.5.2 static inline</h3>

<p>在异常处理或者中断的时候经常见到调用一个函数然后然后找到这个函数发现他是<strong>static inline …</strong>里面只有一条汇编指令 感觉有点看不懂</p>

<p><strong>inline关键字什么意思？</strong>
函数定义前面加inline表示这是一个Inline Function Inline Function are those function whose definitions are small and be substituted at the place where its function call is happened.
这种函数一般很短，转换成汇编指令可能只有一条指令。如果将它写成一个普通的函数跳转过去执行 可能跳转执行然后再返回的一串指令比这条指令本身长很多，这样执行效率会降低。</p>

<p><strong>inline</strong>提示编译器可以把这条内联函数直接编译到调用他的代码出处，这样无需跳转返回的一系列指令 但是这也只是一条hint 编译器不一定会inline这个函数</p>

<p><strong>为什么要前面加上static呢?</strong>
如果只有前面的inline对于GCC来说会报错</p>

<pre><code class="language-cpp=">#include &lt;stdio.h&gt;

inline int add_two(int a, int b)
{
	return a+b;
}

int main()
{
	int ret;

	ret = add_two(1, 3);
	printf("output : %d", ret);

	return 0;
}
</code></pre>

<pre><code class="language-bash=">/usr/bin/ld: /tmp/ccy8plLQ.o: in function `main':
try_inline.c:(.text+0x17): undefined reference to `add_two'
collect2: error: ld returned 1 exit status
</code></pre>

<p>前面说到inline只是一条建议 建议编译器可以内联 但是编译器不一定会采纳
所以这里实际的意思是告诉编译器<strong>This is an alternative function to use if you want to inline this function</strong></p>

<p>在rk3399中读取pc寄存器的值只使用static inline并没有内联还是跳转到了函数的位置：
<img src="https://hackmd.io/_uploads/rkm6WamSh.png" alt="" /></p>

<p>内联汇编之后之前的r_pc()函数就消失了，所以程序的地址也发生了改变：</p>

<p><img src="https://hackmd.io/_uploads/By0TbTmBn.png" alt="" /></p>

<p>加上static则可以避免这个错误，让编译器把这个函数当作internal linkage</p>

<blockquote>
  <p>If you don’t put <strong>static</strong> there, you’re defining an inline function with external linkage without instantiating an external definition.</p>
</blockquote>

<p><strong>如何强制inline一个函数呢？</strong></p>

<p>GCC提供了<strong>attribute((always_inline))</strong></p>
<pre><code class="language-c=">static inline __attribute__((always_inline)) unsigned long r_pc()
{
	unsigned long x;

	asm volatile(
			"adr %0, ."
			: "=r" (x)
			:
			:
				);

	return x;
}
</code></pre>

<p><a href="https://gcc.gnu.org/onlinedocs/gcc/Inline.html">gcc/Inline</a></p>

<h3 id="153-uart输出字符串的写法对比">1.5.3 UART输出字符串的写法对比</h3>

<pre><code class="language-cpp=">void uart_send_string(char *str)
{
  char *string_ptr = str;

  while(*string_ptr != '\0')
  {
    uart_send_char(*string_ptr);
    string_ptr++;
  }
}
</code></pre>

<pre><code class="language-cpp=">void uart_puts(char *s)                                       
{                                                             
  while (*s) {                                                
    uart_putc(*s++);                                          
  }                                                           
} 
</code></pre>

<p>涉及到 <strong>i++</strong> <strong>++i</strong> 的区别
i++先返回值后inc
++i先inc后返回值
<strong>++i</strong>是加一之后的值 <strong>i++</strong> 是加一之前的值</p>

<h3 id="154-printf的实现">1.5.4 printf的实现</h3>

<p>实现完串口的输出，发现rvos和xv6都还实现了printf函数，这个看起来好像比较简单先实现一下这个文件</p>

<h4 id="可变参数函数-variadic-function-in-c">可变参数函数 (variadic function in c)</h4>

<p>此处涉及到的宏定义暂不深究
va_list
va_start()
va_arg()
va_end()</p>

<pre><code class="language-cpp=">#include &lt;stdarg.h&gt;

//...表示数量不定的参数
void printf(char *format, ...)
{
    va_list args;
    va_start(args, format);    //va_start的第二个参数是...之前的参数，即最后一个确定的参数
  
    for(; *format!=0;)
    {
        //va_arg()执行一次会返回一个参数值，然后指到下一个参数
        //解析格式字符串的%标记以及后面的字符来确定打印类型
        if(*fmt == '%')
        {
            switch(*(++fmt))
            {
                case 'd':
                {
                    va_arg(args, 可变参数类型（int，char，float ...）);
                    //分别根据单独的数据类型输出
                    break;
                }
                case 'x':
                {
                    va_arg(args, 可变参数类型（int，char，float ...）);
                    break;
                }
                case 'p':
                {
                    va_arg(args, 可变参数类型（int，char，float ...）);
                    break;
                }
                default：
                {
                    //处理特殊情况
                    break;
                }
                
                //指向下一个字符
                format++;                    
            }
        }
        else
        {
            uart_send_char(*format++);
        }  
    }
  
    va_end(args);
}
</code></pre>

<h3 id="155-杂">1.5.5 杂</h3>

<h4 id="固定串口设备的串口号并设置权限">固定串口设备的串口号并设置权限</h4>

<p>每次sudo chmod太麻烦，用udev规则自动完成设置</p>

<pre><code class="language-bash=">lsusb 查看usb设备信息
sudo vim /etc/udev/rules.d/99-serial-ports.rules #设置udev规则表 默认两位数字开头 越小优先级越高
添加 SUBSYSTEM=="tty", ATTRS{idVendor}=="xxxx", ATTRS{idProduct}=="yyyy", MODE="0666"
sudo udevadm control --reload-rules
</code></pre>

<h1 id="2-trap异常的处理">2. Trap（异常的处理）</h1>

<p>调riscv板子的时候经常看到opensbi，但是又不知道opensbi干嘛的。只知道opensbi引导uboot，uboot再引导内核</p>

<h3 id="21-sbi服务">2.1 sbi服务</h3>

<p>为运行在S模式的操作系统提供特权服务接口，sbi固件是运行在M模式的权限最高。
为处于低级别的程序提供访问重要硬件的接口</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />架构图</li>
</ul>

<h3 id="22-sbi系统调用是异常的一种">2.2 sbi系统调用是异常的一种</h3>

<p>mcause寄存器有两个字段分别表示异常类型和中断类型</p>

<table>
  <thead>
    <tr>
      <th>Interrupt</th>
      <th>Exception Code</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1表示中断 0表示异常</td>
      <td>具体的中断或者异常类型</td>
    </tr>
  </tbody>
</table>

<p>其中9号异常表示来自S模式的系统调用，通俗来说就是运行在S模式的操作系统通过上边提到的sbi服务接口完成了一些只能在M模式下面做的事情。</p>

<p>我们只需要顺清楚这里面的一种服务就可以知道他整个机制是如何运行的。</p>

<h3 id="23-异常过程sbi系统调用为例">2.3 异常过程（sbi系统调用为例）</h3>

<p>sbi服务是软件产生的异常，那么如何产生呢？</p>

<h4 id="231-ecall指令">2.3.1 ECALL指令</h4>

<p>a0~a5存放形参 a7存放系统调用号，区分是哪个Syscall</p>

<p>ecall会提升特权模式 在s mode下使用会提升到m mode 并且PC跳转到mtvec指向的地址</p>

<pre><code class="language-cpp=">#define SBI_CALL(which, arg0, arg1, arg2) ({      \
  register unsigned long a0 asm ("a0") = (unsigned long)(arg0); \
  register unsigned long a1 asm ("a1") = (unsigned long)(arg1); \
  register unsigned long a2 asm ("a2") = (unsigned long)(arg2); \
  register unsigned long a7 asm ("a7") = (unsigned long)(which);  \
  asm volatile ("ecall"         \
          : "+r" (a0)       \
          : "r" (a1), "r" (a2), "r" (a7)    \
          : "memory");        \
  a0;             \
})
</code></pre>

<p>在调用之前首先需要在sbi程序中设置好跳转地址。</p>

<pre><code class="language-cpp=">void sbi_trap_init()
{
  w_mtvec(sbi_exception_vec);
  w_mie(0);
}
</code></pre>

<p>根据sbi_trap_init()代码，程序在执行ecall之后会跳转到sbi_exception_vec()这个函数这里，这是一个统一的入口函数，微机原理里面就学过，进入中断需要保存现场，这里ecall跳转的sbi_exception_vec()函数其实就算是中断处理函数。</p>

<ol>
  <li>保存上下文(context)</li>
  <li>切换sp</li>
  <li>根据具体的系统调用功能执行相应的函数</li>
  <li>恢复上下文</li>
  <li>切换sp</li>
  <li>根据mepc返回继续执行</li>
</ol>

<h4 id="232-user模式下系统调用流程示意图">2.3.2 USER模式下系统调用流程示意图</h4>

<p><img src="/image/xv6_syscall.vpd.png" alt="" /></p>

<h4 id="233-系统调用中传递参数">2.3.3 系统调用中传递参数</h4>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />kernel调用sbi的服务 kernel中使用ecall</li>
</ul>

<p>有的系统调用有参数，比如最简单的sleep(),sleep()的参数是休眠的时钟周期比如sleep(1000)，那在用户空间里面使用的参数怎么样传递到内核中去的呢？</p>

<p>根据riscv的函数调用标准，参数是存放在a0～a7当中的，这里sleep的参数休眠周期就会存放在a0中，回顾ecall指令发生时，第一步保存上下文将所有的寄存器全部放在trapframe当中，所以当执行到具体的syscall函数时：</p>

<pre><code class="language-c=">argraw(int n)
{
    struct proc *p = myproc();
    switch (n) 
    {
        case 0:
            return p-&gt;trapframe-&gt;a0;
        case 1:
            return p-&gt;trapframe-&gt;a1;
        case 2:
            return p-&gt;trapframe-&gt;a2;
        case 3:
            return p-&gt;trapframe-&gt;a3;
        case 4:
            return p-&gt;trapframe-&gt;a4;
        case 5:
            return p-&gt;trapframe-&gt;a5;
    }
    panic("argraw");
    return -1;
}


sys_sleep()
{
    int time_interval;
    ...
    time_interval = argraw(n);
    ...
}
</code></pre>

<p>虽然sys_sleep()本身没有参数，但是函数在一开始就通过读取trapframe的方式，获得了用户空间中的参数。</p>

<h3 id="24-注意">2.4 注意</h3>

<p>在调试s模式的异常时，写好了s模式下的中断处理函数但是没有trap到s模式的中断处理函数，直接到了m模式的中断处理函数。重新看书才发现：</p>

<blockquote>
  <p>委托异常与中断：
中断和异常触发之后默认M模式下进行处理，我们通常吧常用的异常和中断委托给S模式，这样才会跳转到S模式的异常向量表中去。</p>
</blockquote>

<h3 id="xv6中断过程">xv6中断过程</h3>

<p>timervec
1.在CMP填入新值
2.置位SSIP</p>

<h4 id="中断分类">中断分类</h4>

<p>软件中断 定时器中断 外部中断 调试中断</p>

<p>0x0d号中断 一般都是页表映射没设置好 导致找不到页面</p>

<h3 id="25-trapframe-trampoline">2.5 trapframe trampoline</h3>

<p>swtch保存上下文和uservec保存上下文为什么不一样？
swtch只保存了ra sp + s系列寄存器
uservec保存了所有</p>

<p><strong>trapframe</strong>用于保存用户进程的上下文，kernel进程的上下文则在自己的stack中保存，所以kernel进程是没有<strong>trapframe</strong>的。</p>

<h3 id="26-xv6-roadmap">2.6 xv6 roadmap</h3>

<p>软件中断还有问题？scheduler如何切换回去 scheduler如何到usertrap</p>

<p><img src="/image/xv6_roadmap.png" alt="xv6 roadmap" /></p>

<h3 id="27-sleep和wakeup">2.7 sleep()和wakeup()</h3>

<h3 id="28-问题记录">2.8 问题记录</h3>

<ol>
  <li>在测试串口中断时，串口中断标志位清除不了
最开始没有加入串口接收终端，但是但后期发现必须要有一个shell来执行命令才能运行更多的user程序测试多进程的一些东西。最后突然发现是没有读取接收数据缓存寄存器。。。</li>
</ol>

<h1 id="3-page-table">3. Page Table</h1>

<h2 id="31-为什么要有虚拟地址">3.1 为什么要有虚拟地址？</h2>

<p>试想有个一64K大小的程序需要运行在ram只有32K大小的电脑中，ram没有办法装入整个程序，我们应当如何让这个程序跑起来？
程序当中的内容不是所有的都对当前程序的正常运作有关系，我们可以将程序分成固定大小的page例如4K，那么64K程序可以分成16个page，分别标号0到15。同样的，我们将ram也分成4k大小的page并称为page frame，得到page frame 0～7。按照程序的需求将page和page frame对应起来，将需要用的page和page frame对应起来，即把需要的page里面的内容加载到对应的page frame处。</p>

<p>这样程序就跑起来了。但是64K大小的程序实际上只有32K的内容被加载到RAM当中，万一运行到没有加载的page的指令怎么办？此时这条指令的地址（虚拟地址）被传到MMU，MMU通过page号段查找到对应的PTE，发现根据PTE标志位段显示，这个地址所处的page没有被映射到page frame，触发page fault异常，异常处理会取消一对对应关系，重新将这个page映射一个page frame上，退出异常后再次寻址可以继续执行。</p>

<h2 id="32-虚拟地址组成">3.2 虚拟地址组成</h2>

<p><strong>sv39分页机制</strong></p>

<table>
  <thead>
    <tr>
      <th>最高25bits保留</th>
      <th>27bits</th>
      <th>12bits</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>保留</strong></td>
      <td>page号</td>
      <td>offset</td>
    </tr>
  </tbody>
</table>

<p><strong>12位的offset</strong>用于寻址<strong>4K</strong>的地址
<strong>27位page号n</strong>说明这个地址位于<strong>第n个page</strong>，同时page table的 <strong>第n条目(PTE)</strong> 存储了page n的映射关系</p>

<p><strong>PTE (page table entry)</strong></p>

<table>
  <thead>
    <tr>
      <th>PPN (physical page number) page frame号</th>
      <th>标志位 (eg.映射没映射)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>44bits</td>
      <td>10bits</td>
    </tr>
  </tbody>
</table>

<p>44bits的page frame和12bits的offset可以在RAM上寻址。</p>

<h2 id="33-多级页表">3.3 多级页表</h2>

<p>按照4k的分页方法，32位机器：</p>

<blockquote>
  <p>2^32 /4/1024=2^20=1048576</p>
</blockquote>

<p>我们需要有1048576条PTE，这么大的页表放在RAM中是不可接受的，64位更多。</p>

<h2 id="34-tlbtranslation-lookaside-buffers">3.4 TLB(Translation Lookaside Buffers)</h2>

<blockquote>
  <p>Most programs tend to make a large number of references to a small number of pages, and not the other way around.</p>
</blockquote>

<h2 id="35-页表创建流程">3.5 页表创建流程</h2>

<p><img src="/image/页表分配.png" alt="" /></p>

<ol>
  <li>分配4K空间，用于存放pagetable
4096byte/8byte=512 有512条PTE
在linker.ld当中给pagetable直接分配4k空间，放在数据段最后面。</li>
</ol>

<pre><code class="language-linker=">    ...
  
    _sdata = .;
    .data :
    {
        *(.data)
    }
    . = ALIGN(4096);
    _pgt_start = .;
    . += 4096;
    _edata = .;
  
    ...
</code></pre>

<pre><code class="language-cpp=">//定义pgt_t是指向unsigned int的指针
typedef unsigned long *pgt_t;
typedef unsigned long pte_t;
</code></pre>

<p>PTE是最末一级页表的一个条目，这些页表都可以看作是一个数组，每个pte都是一个元素：
<strong>unsigned long pagetable[512];</strong></p>

<pre><code class="language-cpp=">//walk函数返回指向具体页表项(PTE)的指针
//walk函数可以让我们走到最末尾的元素上去
pte_t *walk(pgt_t pagetable, unsigned long va, int alloc);
</code></pre>

<p>一开始我们只有虚拟地址假设是0x80200000以及一个根页表也就是上面linkerscript中分配的空间</p>

<pre><code class="language-clike=">extern unsigned long _pgt_start[];
pgt_t pagetable = _pgt_start[];
unsigned long va = 0x80200000;

pte_t *walk(pagetable, va, 1);
</code></pre>

<p><strong>虚拟地址示意:</strong></p>

<table>
  <thead>
    <tr>
      <th>最高25bits保留</th>
      <th>9bits</th>
      <th>9bits</th>
      <th>9bits</th>
      <th>12bits</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>保留</strong></td>
      <td>2级</td>
      <td>1级</td>
      <td>0级</td>
      <td>offset</td>
    </tr>
  </tbody>
</table>

<p>根据2级目录寻找1级目录的位置，根据1级目录寻找0级目录的位置，而0级目录里面存放的就是我们真正的PTE</p>

<pre><code class="language-cpp=">//伪代码
9bits = va&gt;&gt;12&gt;&gt;(9*level) &amp; 0x1FF;

//marco
#define MASK 0x1FF
#define OFFSET 12
#define GET_NINE_BITS(va, level) ((va&gt;&gt;OFFSET&gt;&gt;(9*level)) &amp; MASK)
</code></pre>

<p>若只考虑页表已经建立好，我们只来寻找最末PTE：</p>

<pre><code class="language-cpp=">pte_t *walk(pgt_t pagetable, unsigned long va, int alloc)
{
    pgt_t pgt_ptr;
    pte *pte_ptr;
  
    for(int level=2; level&gt;0; level--)
    {
        pgt_ptr = &amp;pagetable[GET_NINE_BITS(va, level)];
    
        //如果这个条目是valid
        if(*pgt_ptr | PG_V)
        {
            //取出存放的物理页号PPN 无符号右移10位 最末10位是flags
            //根据PPN得到下一个页表的首地址 左移12位
            pgt_ptr = ((unsigned long)(*pgt_ptr))&gt;&gt;10&lt;&lt;12;
        }
    } 
  
    pte_ptr = &amp;pagetable[GET_NINE_BITS(va, 0)]
  
    return pte_ptr;
}
</code></pre>

<p>加入页表没有建立好的情况</p>

<pre><code class="language-cpp=">pte_t *walk(pgt_t pagetable, unsigned long va, int alloc)
{
    pgt_t pgt_ptr;
  
    for(int level=2; level&gt;0; level--)
    {
        pgt_ptr = &amp;pagetable[GET_NINE_BITS(va, level)];
    
        //如果这个条目是valid
        if(*pgt_ptr | PTE_V)
        {
            //取出存放的物理页号PPN 无符号右移10位 最末10位是flags
            //根据PPN得到下一个页表的首地址 左移12位
            pagetable = ((unsigned long)(*pgt_ptr))&gt;&gt;10&lt;&lt;12;
        }
        //这个条目不存在
        else
        {
            //允许分配且有内存可分配
            if(alloc || (pagetable=kalloc())==1)
            {
                memset(pagetable, 0, 4096);
                //回填
                *pte_ptr = (((unsigned long)pagetable)&gt;&gt;12&lt;&lt;10) | PTE_V;
            }
            else
            {
                return 0;
            }
        }
    } 
    
    pte_ptr = &amp;pagetable[GET_NINE_BITS(va, 0)];
    return pte_ptr;
}
</code></pre>

<h2 id="36-进程的pagetable">3.6 进程的Pagetable</h2>

<p>xv6采取的方式是内核有一个kernel pagetable，每一个进程有自己的process pagetable</p>

<blockquote>
  <p>Having a separate page table for each process is necessary for <strong>process isolation</strong> as they should not be allowed to stomp on each others memory.</p>
</blockquote>

<p>每个进程都有自己的stack，如何检测到stcakoverflow呢？
我们注意到xv6每个stack之间都有guardian page，超出会访问guardian page造成page fault</p>

<blockquote>
  <p>不同的进程可以使用相同的虚拟地址，但经过各自page table映射到不同的物理地址，彼此不会干扰。</p>
</blockquote>

<h2 id="37-address-space">3.7 Address Space</h2>

<blockquote>
  <p>Like those other systems, upon a context switch, the user portion of the currently-running address space changes; the kernel portion is the same across processes.</p>
</blockquote>

<h2 id="38-问题">3.8 问题</h2>

<ol>
  <li>在加入新的文件之后，页表不能正确切换，有些已经建立的页表映射没有成功显示
最后定位到新加入文件bss段的一个变量，发现这个变量长度超过一个值就不能建立完整的页表映射，但是很说不通，最后发现linkerscript里面将根页表的起始地址和bss段重合了，在没加入这个新文件之前恰好没有影响到页表的建立。</li>
</ol>

<h1 id="4-进程-process">4. 进程 (Process)</h1>

<h2 id="41-启动第一个进程">4.1 启动第一个进程</h2>

<p>目前为止，内核的页表kernel pagetable已经设置好了，映射了整个DDR，即从0x80000000开始的128M地址空间。还映射了UART的位置0x10000000，大小为0x1000。trap的流程也走通了，m模式委托异常和中断给s模式，可以实现一些简单系统调用，例如打印等。但是我们其实现在仍然处于S模式，要知道用户程序肯定是在u模式当中运行的。接下来就应该从kernel启动一个init进程，而且这个init进程应该运行在u模式，并且这个用户程序可以通过系统调用或者中断的形式trap到s模式进行处理，处理之后还要返回到用户程序继续执行。</p>

<p>kernel pagetable配置的空间是kernel address space，用户进程需要的是process address space，进程不能够看到内核的代码，这需要设置一个新的进程页表。</p>

<p>而从kernel启动进程就需要我们提前设置user pagetable，随之而来的问题就是</p>

<pre><code class="language-asm=">    sfence.vma zero zero
    csrw satp a0
    sfence.vma zero zero
</code></pre>

<p>执行完写satp寄存器之后，页表就会立刻更改，kernel space和process space如果没有把这段代码映射在相同的地方，执行就会出错cannot access</p>

<p>这就是trampoline代码的位置，xv6巧妙的利用了这段代码，从内核启动第一个用户进程不久相当于用户trap到内核，处理完成后再从内核退到用户进程吗？这一过程需要做几件事情才能完成进程的顺利启动：</p>

<ol>
  <li>装载用户pagetable</li>
  <li>保存当前kernel的context</li>
  <li>装载用户程序的context</li>
</ol>

<p>完成上面的事情之后只需要一条</p>

<pre><code class="language-asm=">    sret
</code></pre>

<p>pc就会被硬件设置到sepc处，然后从pc开始运行。</p>

<p>接下来就完成一个init程序，这个程序只通过系统调用打印一个字符0。</p>

<p>参考xv6,发现有一段initcode[]是纯机器指令，这段initcode源码在xv6/user下面，调用了一个syscall exec，运行了xv6的shell。</p>

<pre><code class="language-bash=">#反汇编initcode.out得到initcode[]
#小段存放
riscv64-linux-gnu-objdump -d ./user/initcode.out
</code></pre>

<p>效仿xv6的做法：</p>

<pre><code class="language-asm=">.globl start
start:
	li a7, 0x20
	li a0, 0x30 
	ecall

loop:
	j loop
</code></pre>

<p>这里的系统调用号是0x20可能不符合规范，但是测试一下整个流程应该可以不用在意。</p>

<pre><code class="language-asm=">//U模式trap处理函数
void user_trap()
{
	int syscall_num = proc_list[0].trapframe-&gt;a7;

	if(r_scause() == 8)
	{
		//proc_list[0].trapframe-&gt;a0 = 8;
		proc_list[0].trapframe-&gt;a0 = syscall[syscall_num]();
	}
	else
	{
		printf("user_trap(): unexpected scause %p \n", r_scause());
		printf(fault_info[r_scause()-0x8000000000000000].fault_name);
		panic("panic/n");
	}

	proc_list[0].trapframe-&gt;epc = r_sepc() + 4; 
	user_trap_ret();
}
</code></pre>

<p>运行后可见打印了一个0
<img src="https://i.imgur.com/HeJ9s6n.png" alt="" /></p>

<h3 id="linux中的0号进程和1号进程">linux中的0号进程和1号进程</h3>

<p><strong>0号</strong>进程是管理进程调度的内核进程（即现在运行<strong>scheduler()</strong>的这个进程），<strong>1号</strong>进程是第一个用户进程，user_init()初始化的init进程。在linux中使用</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps <span class="nt">-p</span> 1
</code></pre></div></div>

<p>查看1号进程的信息显示是systemd，systemd是新一代的init，内核进程会寻找init程序去执行，没找到就会启动shell。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">try_to_run_init_process</span><span class="p">(</span><span class="s">"/sbin/init"</span><span class="p">)</span> <span class="o">||</span>
	    <span class="o">!</span><span class="n">try_to_run_init_process</span><span class="p">(</span><span class="s">"/etc/init"</span><span class="p">)</span> <span class="o">||</span>
	    <span class="o">!</span><span class="n">try_to_run_init_process</span><span class="p">(</span><span class="s">"/bin/init"</span><span class="p">)</span> <span class="o">||</span>
	    <span class="o">!</span><span class="n">try_to_run_init_process</span><span class="p">(</span><span class="s">"/bin/sh"</span><span class="p">))</span>
		<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>

<span class="n">panic</span><span class="p">(</span><span class="s">"No working init found.  Try passing init= option to kernel. "</span>
	  <span class="s">"See Linux Documentation/admin-guide/init.rst for guidance."</span><span class="p">);</span>
</code></pre></div></div>

<p><a href="http://213.254.12.151/~rubini/docs/init/">init</a></p>

<h2 id="42-进程之间的切换-scheduling">4.2 进程之间的切换 scheduling</h2>

<p>xv6当中进程之间的切换不是从一个进程切换到新的进程，而是从一个进程切换到内核再从内核切换到新的进程。</p>

<p><img src="https://i.imgur.com/NhynPMJ.png" alt="" /></p>

<p>xv6设置了定时器中断，当定时器中断到来时触发<strong>yield()</strong>，随后调用<strong>sched()</strong>，在<strong>sched()</strong>之中调用<strong>swtch()</strong>，<strong>swtch</strong>中的操作会使得cpu重新回到内核进程，而内核进程之前已经执行到：</p>

<pre><code class="language-cpp=">void scheduler(void)
{ 
  struct proc *p; 
  struct cpu *c = mycpu();
  
  c-&gt;proc = 0;
  for(;;){
    // Avoid deadlock by ensuring that devices can interrupt.
    intr_on();
  
    for(p = proc; p &lt; &amp;proc[NPROC]; p++) {
      acquire(&amp;p-&gt;lock);
      if(p-&gt;state == RUNNABLE) {
        // Switch to chosen process.  It is the process's job
        // to release its lock and then reacquire it
        // before jumping back to us.
        p-&gt;state = RUNNING;
        c-&gt;proc = p;
       swtch(&amp;c-&gt;context, &amp;p-&gt;context);

        // Process is done running for now.
      // It should have changed its p-&gt;state before coming back.
        c-&gt;proc = 0;
      }
      release(&amp;p-&gt;lock);
    }
  }
}
</code></pre>

<p><strong>scheduler()</strong> 使用最简单的轮询算法完成新进程的选择，利用<strong>swtch()</strong> 进入新的进程。</p>

<p>第一个进程（打印0的那个进程）已经跑起来了，利用第一个进程把上述旧用户进程切换到内核scheduler进程，然后再切换到新内核进程的框架搭起来。相当于始终只调度一个用户进程：</p>

<pre><code class="language-cpp=">//切换到内核进程
void back_to_kernel()
{
	swtch(&amp;proc_ptr-&gt;context, &amp;kernel_ctxt.context);
}

//U模式trap处理函数
void user_trap()
{
	int syscall_num = proc_ptr-&gt;trapframe-&gt;a7;
	unsigned long trap_cause = r_scause();

	if(trap_cause == 8)
	{
		proc_ptr-&gt;trapframe-&gt;a0 = syscall[syscall_num]();
		proc_ptr-&gt;trapframe-&gt;epc = r_sepc() + 4; 
	}
	//S模式软件中断
	else if(trap_cause == 0x8000000000000001)
	{
		printf("soft interrupt from s mode %p \n", trap_cause);
		w_sip(r_sip() &amp; ~2);
		back_to_kernel();
		proc_ptr-&gt;trapframe-&gt;epc = r_sepc();
	}
	else
	{
		printf("user_trap(): unexpected scause %p \n", trap_cause);
		printf(fault_info[r_scause()].fault_name);
		panic("panic/n");
	}

	user_trap_ret();
}
</code></pre>

<p>指针<strong>proc_ptr</strong>始终指向当前运行的用户进程，<strong>kernel_ctxt</strong>是存储内核进程上下文的变量</p>

<pre><code class="language-cpp=">//内核进程一直在调度函数中循环
void scheduler()
{
	struct proc *nxt_proc_ptr = proc_ptr;
	struct proc *kernel_ptr = &amp;kernel_ctxt;

	while(1)
	{
		proc_ptr = &amp;proc_list[0];
		for(int n=0; n&lt;MAX_PROC; n++)
		{
			if(proc_ptr-&gt;state == RUNNABLE)
			{
				swtch(&amp;kernel_ptr-&gt;context, &amp;proc_ptr-&gt;context);
			}

			proc_ptr++;
		}
	}
}
</code></pre>

<p><strong>scheduler()</strong> 遍历<strong>proc_list[MAX_PROC]</strong>，找到<strong>RUNNABLE</strong>的程序执行，目前我们只有<strong>initcode</strong>程序是<strong>RUNNABLE</strong>，存放在<strong>proc_list[0]</strong>。</p>

<p>编译后运行反复打印：
<img src="https://i.imgur.com/wl3cr8p.png" alt="" /></p>

<h3 id="2-进程之间的切换的一些细节">2 进程之间的切换的一些细节</h3>

<ul>
  <li>
    <p><strong>每个进程其实都有两个栈</strong>
一个内核栈一个用户栈，进程并不是所有的内容都在用户空间完成的：比如<strong>用户空间执行时遇到一个切换进程目的的定时器中断</strong>，此时程序会陷入到内核中，但是这时候还并没有发生进程的切换，程序仍然是当前的程序，只是现在在内核空间执行。</p>
  </li>
  <li><strong>在user_trap_ret()一开始我们显式地关闭了中断</strong>
    <pre><code class="language-c=">intr_off();
</code></pre>
    <p>但是在这之后没有调用以下指令显示地打开中断</p>
    <pre><code class="language-c=">intr_on();
</code></pre>
    <p>没有开启吗？如果没有开启的话依赖于定时中断的任务切换函数没有办法进入。但是实际上，任务切换依旧可以进行。其实中断是被隐式的打开了：</p>
    <pre><code class="language-c=">unsigned long x = r_sstatus();
x &amp;= ~SSTATUS_SPP;
//这里允许了中断！！！                                 
x |= SSTATUS_SPIE;
w_sstatus(x);
</code></pre>
  </li>
  <li><strong>两个进程同时调用sys_print打印数字</strong></li>
</ul>

<p>父进程循环打印1，子进程循环打印2。如果两条指令之间不加延时，其中一个进程会一直打印，没有机会切换到schedule(),切换到另一个进程，但是在两条打印指令之间加入一段延时，又可以正常运行。为什么？</p>

<p>操作系统中有两个trap入口地址<strong>kerneltrap</strong>和<strong>usertrap</strong>，我只在usertrap中判断定时器中断并进行任务切换但是在kerneltrap中只是置位软中断，不进行任务切换。当一个进程循环调用<strong>sys_print</strong>进行字符打印时，他会不断trap到kernel即S模式，S模式使用kerneltrap，这样以来除非刚刚好在用户模式时触发定时器中断，不然即使定时器时间到达都不会进行任务切换。</p>

<p>解决：在<strong>kerneltrap</strong>中也加入任务切换。在<strong>kerneltrap</strong>中加入切换之后，发现最开始系统初始化的顺序也需要更改，pagetable的建立和映射必须要在<strong>trap_vector_install()</strong> 之前，不然在最开始就会pagefault，两个进程指针一个指向内核线程（<strong>kernel_ptr</strong>）一个指向当前运行进程(<strong>proc_ptr</strong>)。需要特别注意的是：在第一个用户进程起来之前也极有可能发生定时器中断，一定要初始化<strong>proc_ptr=kernel_ptr;</strong>，否则出现pagefault。至此，可以不加延时两个线程循环打印字符。</p>

<h2 id="43-创建新进程-fork">4.3 创建新进程 fork()</h2>

<p><strong>fork()</strong> 系统调用是接下来完成的函数，<strong>fork()</strong> 会将当前进程的全部内容复制一份，包括<strong>trapframe</strong>和代码</p>

<p>具体会做一些什么事情呢?</p>

<ol>
  <li>查看进程数组，可分配的进程是有限的，是否还有可以分配的空位</li>
  <li>分配<strong>pid</strong> 设置状态为<strong>USED</strong></li>
  <li>分配<strong>pagetable</strong> <strong>trapframe</strong>的空间并映射</li>
  <li>设置上下文如堆栈的位置</li>
  <li>根据程序的长度来分配新的物理空间并用<strong>walk</strong>映射这些物理地址</li>
  <li>设置状态为<strong>RUNNABLE</strong> 并返回<strong>PID</strong></li>
</ol>

<p>在看xv6源码时发现：</p>

<pre><code class="language-cpp=">// copy saved user registers.
*(np-&gt;trapframe) = *(p-&gt;trapframe);
</code></pre>

<p><del>在fork()函数当中并没有为child process复制parent process的trapfram仅仅把child process的trapframe指针指向了parent的trapframe。这样不就会把parent的trapframe内容给修改了吗？</del></p>

<p>更改initcode代码如下：</p>

<pre><code class="language-asm=">.globl start
start:
	li a7, 0x20 #系统调用号0x20 uart打印
	li a0, 0x30 #0x30 ‘0’
	ecall

	li a7, 0x20
	li a0, 0x31 #0x30 ‘1’
	ecall

	li a7, 0x01 #系统调用号0x01 fork()
	ecall

	li a7, 0x20
	li a0, 0x32 #0x30 ‘2’
	ecall

	li a7, 0x20
	li a0, 0x33 #0x30 ‘3’
	ecall
loop:
	j loop
</code></pre>

<p>如果代码运行正确：先通过系统调用打印出<strong>01</strong>，接着程序使用<strong>fork()</strong> 产生子进程，随后两个进程都会从14行开始执行，打印出<strong>2323</strong>或者<strong>2233</strong>。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">fork</span><span class="p">()</span>
<span class="p">{</span>
    <span class="k">struct</span> <span class="n">proc</span> <span class="o">*</span><span class="n">new_proc_ptr</span><span class="p">;</span>
    <span class="n">pte_t</span> <span class="o">*</span><span class="n">pte</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="o">*</span><span class="n">proc_mem</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">pa</span><span class="p">;</span>

    <span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">n</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span> <span class="n">n</span><span class="o">&lt;</span><span class="n">MAX_PROC</span><span class="p">;</span> <span class="n">n</span><span class="o">++</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">if</span><span class="p">(</span><span class="n">proc_list</span><span class="p">[</span><span class="n">n</span><span class="p">].</span><span class="n">state</span> <span class="o">==</span> <span class="n">UNUSED</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">new_proc_ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">proc_list</span><span class="p">[</span><span class="n">n</span><span class="p">];</span>
            <span class="k">goto</span> <span class="n">found</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="k">return</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>

    <span class="nl">found:</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">pid</span> <span class="o">=</span> <span class="o">++</span><span class="n">pid</span><span class="p">;</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">state</span> <span class="o">=</span> <span class="n">USED</span><span class="p">;</span>

    <span class="c1">//分配pgt和trapframe的空间 并映射</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">proc_pagetable</span> <span class="o">=</span> <span class="n">proc_pagetable</span><span class="p">();</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">trapframe</span> <span class="o">=</span> <span class="n">kalloc</span><span class="p">();</span>
    <span class="n">map_page</span><span class="p">(</span><span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">proc_pagetable</span><span class="p">,</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">long</span><span class="p">)</span><span class="n">_trampoline</span><span class="p">,</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">long</span><span class="p">)</span><span class="n">TRAMPOLINE</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">PTE_R</span> <span class="o">|</span> <span class="n">PTE_W</span> <span class="o">|</span> <span class="n">PTE_X</span><span class="p">);</span>
    <span class="n">map_page</span><span class="p">(</span><span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">proc_pagetable</span><span class="p">,</span> <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">trapframe</span><span class="p">,</span> <span class="mh">0x3fffffe000</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">PTE_R</span><span class="o">|</span><span class="n">PTE_W</span><span class="p">);</span>

    <span class="c1">//设置返回的地址 第一次</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">context</span><span class="p">.</span><span class="n">ra</span> <span class="o">=</span> <span class="n">fork_ret</span><span class="p">;</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">context</span><span class="p">.</span><span class="n">sp</span> <span class="o">=</span> <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">kstack</span><span class="p">;</span>

    <span class="c1">//复制trapframe</span>
        <span class="o">*</span><span class="p">(</span><span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">trapframe</span><span class="p">)</span> <span class="o">=</span> <span class="o">*</span><span class="p">(</span><span class="n">proc_ptr</span><span class="o">-&gt;</span><span class="n">trapframe</span><span class="p">);</span>

    <span class="c1">//设置子进程返回0</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">trapframe</span><span class="o">-&gt;</span><span class="n">a0</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">prog_size</span> <span class="o">=</span> <span class="n">proc_ptr</span><span class="o">-&gt;</span><span class="n">prog_size</span><span class="p">;</span>

    <span class="c1">//根据程序的长度复制并映射</span>
    <span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">n</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span> <span class="n">n</span><span class="o">&lt;</span><span class="n">proc_ptr</span><span class="o">-&gt;</span><span class="n">prog_size</span><span class="p">;</span> <span class="n">n</span><span class="o">+=</span><span class="mi">4096</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="n">pte</span> <span class="o">=</span> <span class="n">walk</span><span class="p">(</span><span class="n">proc_ptr</span><span class="o">-&gt;</span><span class="n">proc_pagetable</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>
        <span class="k">if</span><span class="p">(</span><span class="o">*</span><span class="n">pte</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">panic</span><span class="p">(</span><span class="s">"pte == 0</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
        <span class="p">}</span>
        <span class="n">pa</span> <span class="o">=</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">long</span><span class="p">)(</span><span class="o">*</span><span class="n">pte</span><span class="p">)</span><span class="o">&gt;&gt;</span><span class="mi">10</span><span class="o">&lt;&lt;</span><span class="mi">12</span><span class="p">;</span>
        <span class="n">proc_mem</span> <span class="o">=</span> <span class="n">kalloc</span><span class="p">();</span>
        <span class="n">memmove</span><span class="p">(</span><span class="n">proc_mem</span><span class="p">,</span> <span class="n">pa</span><span class="p">,</span> <span class="mi">4096</span><span class="p">);</span>
        <span class="n">map_page</span><span class="p">(</span><span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">proc_pagetable</span><span class="p">,</span> <span class="n">proc_mem</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">PTE_R</span><span class="o">|</span><span class="n">PTE_W</span><span class="o">|</span><span class="n">PTE_U</span><span class="o">|</span><span class="n">PTE_X</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="c1">//设置程序RUNNABLE</span>
    <span class="n">new_proc_ptr</span><span class="o">-&gt;</span><span class="n">state</span> <span class="o">=</span> <span class="n">RUNNABLE</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">pid</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>这里部分内容有几个坑：
<strong>1. undefined declaration of memcpy</strong></p>

<pre><code class="language-cpp=">*(new_proc_ptr-&gt;trapframe) = *(proc_ptr-&gt;trapframe);
</code></pre>

<p>涉及到通过指针来复制结构体，尽管没有在代码中使用memcpy但是编译器会优化代码使用memcpy来完成复制，如果没有memcpy的声明就会出现undefined错误。</p>

<p><strong>2. 一直fork()直到用光proc_list[MAX_PROC]数组</strong>
<img src="https://i.imgur.com/melYd6o.png" alt="" />
进入<strong>usertrap</strong>中断处理函数之后一定要先保存<strong>sepc</strong>寄存器的值，不然可能会被修改导致返回的地址不正确，出现多次fork()。</p>

<pre><code class="language-c{linenos=true}">//U模式trap处理函数
void user_trap()
{
	int syscall_num = proc_ptr-&gt;trapframe-&gt;a7;
	unsigned long trap_cause = r_scause();

	if(trap_cause == 8)
	{
		proc_ptr-&gt;trapframe-&gt;epc = r_sepc() + 4; 
		//proc_list[0].trapframe-&gt;a0 = 8;
		proc_ptr-&gt;trapframe-&gt;a0 = syscall[syscall_num]();
//		proc_ptr-&gt;trapframe-&gt;epc = r_sepc() + 4; 
	}
	//S模式软件中断
	else if(trap_cause == 0x8000000000000001)
	{
               proc_ptr-&gt;trapframe-&gt;epc = r_sepc();
		//printf("soft interrupt from s mode %p \n", trap_cause);
		w_sip(r_sip() &amp; ~2);
		back_to_kernel();
//		proc_ptr-&gt;trapframe-&gt;epc = r_sepc();
	}
	else
	{
		printf("user_trap(): unexpected scause %p \n", trap_cause);
		printf(fault_info[r_scause()].fault_name);
		panic("panic/n");
	}

	user_trap_ret();
}
</code></pre>

<p><strong>3. pid不对</strong>
pid是一个全局变量初始化成0，但是后来发现变成了一个地址。查看map文件，问题最后定位到pid所在的位置和空闲内存的位置重叠，而空闲内存的管理使用的是最简单的链表，pid的位置正好是空闲内存存指针的地方。原本以为所有初始化之后的全局变量都在data段中存储，查看map文件后发现还有sbss段，里面存放的第一个变量就是pid。修改ld文件考虑sbss段。</p>

<p>什么时候在sbss段什么时候在data段，可能是要看编译器？</p>

<p><strong>4. c编写的用户程序使用fork()函数</strong>
判断父子进程时，直接使用fork()的返回值不会出问题，而赋值给局部变量之后再判断pid，子进程出现page fault。debug后发现只分配并映射了stack空间，但是并没有将父进程的stack内容复制给子进程。</p>

<h2 id="44-进程睡眠-sleep">4.4 进程睡眠 sleep()</h2>

<p>进程会sleep，将当前进程状态设置为SLEEPING然后调用sched(),让当前进程主动放弃cpu</p>

<blockquote>
  <p>The basic idea is to have sleep mark the current process as SLEEPING and then call sched to release the CPU;</p>
</blockquote>

<p>其实就是设置当前的进程是SLEEPING状态，SLEEPING状态的程序自然不能运行，所以我们切换到scheduler进程去重新选择一个进程执行。sched函数其实就是之前使用的backtokernel函数。</p>

<pre><code class="language-cpp=">void sleep()
{
    proc_ptr-&gt;state = SLEEPING;
    sched();
}
</code></pre>

<h2 id="41-进程的并发和同步">4.1 进程的并发和同步</h2>

<p>以多线程计数器为例：</p>

<pre><code class="language-cpp=">unsigned int share_var;

void *routine(void *arg)
{
    for(int i=0; i&lt;1000; i++)
    {
        share_var++;
    }
    return NULL;
}

int main()
{
    pthread_t thread[2];
  
    pthread_create(&amp;thread[0], NULL, routine, 0);
    pthread_create(&amp;thread[1], NULL, routine, 0);
  
    pthread_join(&amp;thread[0], NULL);
    pthread_join(&amp;thread[1], NULL);
  
    printf("%d", share_var);
  
    return 0;
}
</code></pre>

<p>打印的结果大概率小于2000,在线程1没有将share_var+1的值写回到share_var时，可能线程2就被调度执行，此时线程2读取到的share_var仍然是0，加1后回写，share_var变成1,这时又轮到线程1执行，回写线程1的结果，最后share_var加了两次但是实际结果只加了一次，出现了数据更新的丢失。</p>

<p>需要注意的是即使将这条share_ptr++换成一条汇编的指令执行，仍然不能保证这个自加操作的原子性。即使这个程序在单个单核单线程CPU运行也不能保证原子性，因为CPU现在都是流水线执行，<strong>取指 译码 执行 访存 回写</strong>这五个阶段都可能会被打断，除非是单个单线程CPU在一个时钟周期可以完成五个阶段才能保证自加操作的原子性。</p>

<h2 id="42-spin-lock-自旋锁">4.2 spin lock (自旋锁)</h2>

<blockquote>
  <p>We cannot guarantee a thread execution order.</p>
</blockquote>

<p>就算有的线程新创建有的线程后创建，后创建的线程仍然有可能比先创建的线程先执行</p>

<p>竞争的产生 有一个共享变量，进程A去操作他的动作还没完成，进程B也去操作这个变量</p>

<p><strong>1. 关闭中断？</strong>
进程的切换是通过定时器中断来完成的，在操作共享变量的时候关闭所有的中断可以避免进程的切换，这样就不会产生竞争
但是user space的程序可以有关闭中断的权利危险</p>

<p><strong>2. 使用一个lock变量？</strong>
试想设置一个lock变量 如果lock=0 表示没进程在访问 反之则有其他进程正在访问 进程A在访问共享变量之前 先测试lock的值
这样一来好像可以避免竞争 但是lock变量自己也是共享的 如果A进程得到lock=0 在把lock赋值1之前 CPU切换到进程B 进程B也访问了lock 由于A还来得及上锁 B也认为可以操作共享内存
竞争再次发生</p>

<p><strong>3. spin lock</strong></p>

<p><strong>atomic operation</strong> (原子操作)
不可被打断的一串命令</p>

<p>TSL Test &amp; Set Lock</p>

<pre><code class="language-asm=">#将LOCK的值存入RX 并且设置LOCK=1
TSL RX,LOCK
</code></pre>

<h2 id="杂">杂</h2>

<p>trampoline.S又映射到了进程空间，又映射了内核空间，通过他的userret我们从kernel的S模式返回到了进程的U模式，当我们在进程的U模式时，有异常（系统调用）或者中断，我们通过uservec进入到内核S模式</p>

<h1 id="5-文件系统fs">5. 文件系统（FS）</h1>

<h2 id="51-virtio-disk">5.1 virtio disk</h2>

<p>运行在QEMU虚拟机上的xv6，其文件系统的读写是建立在virtio disk上面的，先完成virtio disk的驱动才能进行后面的操作。</p>

<h3 id="511-修改makefile">5.1.1 修改Makefile</h3>

<pre><code class="language-makefile=">#关闭legacy模式 legacy是之前的旧设备协议
QEMU_OPTS = -global virtio-mmio.force-legacy=false
#磁盘镜像还没好先不指定
QEMU_OPTS += -drive file=/dev/null,if=none,format=raw,id=x0
QEMU_OPTS += -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0
</code></pre>

<pre><code class="language-makefile=">#加入刚才得设置
run:
    qemu-system-riscv64 $(QEMU_FLAGS) $(QEMU_BIOS) $(QEMU_OPTS) -kernel benos.elf
debug:
    qemu-system-riscv64 $(QEMU_FLAGS) $(QEMU_BIOS) $(QEMU_OPTS) -kernel benos.elf -S -s
</code></pre>

<h3 id="512-生成测试img文件">5.1.2 生成测试img文件</h3>

<p>上面的配置只能测试磁盘得初始化不能测试读写，因为没有磁盘镜像。用dd工具生成一个随机得磁盘镜像来测试读写</p>

<pre><code class="language-bash=">#block size = 1024k
dd if=/dev/urandom of=mydisk.img bs=1M count=10
</code></pre>

<h3 id="513-virtio驱动">5.1.3 virtio驱动</h3>

<h4 id="1-使能plic管理的磁盘中断">1. 使能PLIC管理的磁盘中断</h4>

<p>virtio disk的中断是由PLIC来控制的，PLIC由将这个中断传递给CPU，PLIC的中断信号对于CPU来说是外部中断，因此要使能S模式的外部中断。</p>

<pre><code class="language-cpp=">w_sie(r_sie() | (0x01&lt;&lt;9));
</code></pre>

<h4 id="2-具体读写操作">2. 具体读写操作</h4>

<p>virtio disk读写都要通过struct buf结构体来完成，buf结构体的dev成员设置0x01</p>

<pre><code class="language-cpp=">struct buf {
  int valid;   // has data been read from disk?
  int disk;    // does disk "own" buf?
  uint dev;
  uint blockno;
  //struct sleeplock lock;
  uint refcnt;
  struct buf *prev; // LRU cache list
  struct buf *next;
  uchar data[1024];
};
</code></pre>

<p>测试virtio_disk_rw，向第0个block写1024个byte，内容都是0</p>

<pre><code class="language-cpp=">struct buf *test_buf = (struct buf *)kalloc();

memset(test_buf, 0, 4096);
test_buf-&gt;dev = 0x1;
test_buf-&gt;blockno = 0;
test_buf-&gt;valid = 0;
test_buf-&gt;refcnt = 1;

virtio_disk_rw(test_buf, 1);
</code></pre>

<p>在内核当中直接测试的时候一定要主要这个<strong>test_buf</strong>分配空间别放在<strong>free_mem</strong>之前了，不然后面的设置就全都没用了。</p>

<p>用<strong>hexdump</strong>查看写测试之前的磁盘内容，一行是16bytes，从0x0000开始：</p>

<p><img src="https://i.imgur.com/WeIaEUv.png" alt="" /></p>

<p>运行程序写一个block之后,第一个block已经成功变成0</p>

<p><img src="https://i.imgur.com/zHz5zAD.png" alt="" /></p>

<p>读写失败的情况：在gdb中可以查看test_buf具体的值，dev成员的值不对，block的值也不对，可能是由于test_buf有些成员没有设置正确，返回的data数组的值也是任意的。</p>

<pre><code class="language-cpp=">(gdb) p /x *test_buf 
$1 = {valid = 0x0, disk = 0x0, dev = 0x1000, blockno = 0x3, refcnt = 0x1, pre = 0x0, nxt = 0x0, data = {0x0 &lt;repeats 129 times&gt;, 0xf0,
    0xff, 0xff, 0x2, 0x0 &lt;repeats 156 times&gt;, 0xd0, 0xff, 0xff, 0x2, 0x0 &lt;repeats 156 times&gt;, 0xb0, 0xff, 0xff, 0x2,
    0x0 &lt;repeats 156 times&gt;, 0x90, 0xff, 0xff, 0x2, 0x0 &lt;repeats 156 times&gt;, 0x70, 0xff, 0xff, 0x2, 0x0 &lt;repeats 156 times&gt;, 0x50, 0xff,
    0xff, 0x2, 0x0 &lt;repeats 91 times&gt;}}
</code></pre>

<h2 id="fatfile-allocation-table">FAT（File Allocation Table）</h2>

<p>用链表把一个一个block链接起来，链表是集中存放并且有多个备份</p>

<h2 id="52-基于inode的文件系统">5.2 基于Inode的文件系统</h2>

<p>inode会在磁盘和RAM当中各一份，RAM当中我们直接写作inode，磁盘当中我们写作dinode作为区分。</p>

<pre><code class="language-cpp=">#define NDIRECT 12
#define NINDIRECT 1

//总大小64bytes
struct dinode
{
    //用major来指定是哪种设备 minor来指定是这种设备的哪一个
    short major;
    short minor;
    //三种type：file device directory
    short type;
    short nlink;
    unsigned int size;
    //12个直接块 1个间接块
    unsigned int block_addr[NDIRECT+NINDIRECT];  
};
</code></pre>

<p>xv6当中定义NINDIRECT</p>

<pre><code class="language-cpp=">#define NINDIRECT BSIZE/(sizeof(unsigned int))
</code></pre>

<p>虽然dinode只用了一个指针来指向<strong>indirect block</strong>，但是一个间接块当中其实含有<strong>Block_Size/sizeof(指针长度)</strong> 个指向block的指针。</p>

<p><img src="https://i.imgur.com/DCh5WeF.png" alt="dinode 结构体" /></p>

<p>inode是从dinode读取填入的。</p>

<pre><code class="language-cpp=">struct inode
{
    unsigned int dev_num;
    unsigned int inode_num;
    int valid;
    int ref;
  
    struct dinode inode_copy;  
};
</code></pre>

<p><strong>Directory Entry 目录项</strong>
如果没有目录，那么所有的文件都是平行的。目录是一种特殊的文件，一级目录就可以理解成一个文件夹。我们每创建一个文件就会分配一个inode，每创建一个文件夹也会分配inode，文件的数据块存放的是文件的内容，目录的数据块存放的是当前文件夹下面文件和子文件夹的dirtectory entry用struct dirent表示。所以每新添加一个文件需要朝他所在的文件夹的数据块添加他的dirent</p>

<pre><code class="language-cpp=">//大小16bytes
struct dirent {
  ushort inum;
  char name[DIRSIZ];
};
</code></pre>

<h3 id="521-创建文件系统到磁盘主要api">5.2.1 创建文件系统到磁盘主要API</h3>

<pre><code class="language-cpp=">//小端转换
unsigned short xshort(unsigned short val);
unsigned short xint(unsigned int val);

//根据block号 读写缓冲buf内容来写
wr_block(unsigned int n_block, void *buf);
rd_block(unsigned int n_block, void *buf);

//读写磁盘里面的inode
wr_inode(unsigned int inum, struct dinode *new_dinode);
rd_inode(unsigned int inum, struct dinode *new_dinode);

//分配新的inode 并写入inode blk
//注意！！！ inode结构体在使用之前一定要先将里面的东西全部清零
inode_alloc(unsigned short file_type);

//inode增加内容
inode_append(unsigned int inum, void *buf, int n);
</code></pre>

<p>大小端转换如果本来就是小端了那么使用xint函数输入等于输出。xv6使用了这个转换应该主要是考虑到会用在大端的磁盘上面。</p>

<p>实现一个最基本的文件系统结构，先不要log块</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">分区</th>
      <th style="text-align: center">boot</th>
      <th style="text-align: center">superblock</th>
      <th style="text-align: center">inode block</th>
      <th style="text-align: center">bitmap block</th>
      <th style="text-align: center">data blocks</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">块号</td>
      <td style="text-align: center">0</td>
      <td style="text-align: center">1</td>
      <td style="text-align: center">2</td>
      <td style="text-align: center">3</td>
      <td style="text-align: center">4及之后</td>
    </tr>
    <tr>
      <td style="text-align: center">起始地址</td>
      <td style="text-align: center">0x00</td>
      <td style="text-align: center">0x400</td>
      <td style="text-align: center">0x800</td>
      <td style="text-align: center">0xc00</td>
      <td style="text-align: center">0x1000</td>
    </tr>
  </tbody>
</table>

<p>里面比较费劲的实现应该就是<strong>inode_append</strong></p>

<pre><code class="language-c=">//往对应inode文件添加内容
//*xinput对应待添加数据的首地址
//n对应数据长度
void inode_append(unsigned int inum, void *input, int len)
{
	char *p = (char *)input;
	struct dinode dinode_buf;
	unsigned int offset, fbn, actual_wr_len;
	char indirect[BSIZE / sizeof(unsigned int)];
	unsigned int blk_num;
	char data_buf[BSIZE];

	//读取inode到buf里面
	rd_inode(inum, &amp;dinode_buf);

	offset = xint(dinode_buf.size);

	printf("offset = 0x%x\n", offset);
	//循环条件 如果添加的内容还没有添加完
	while(len &gt; 0)
	{
		printf("length = 0x%x\n", len);
		//完整块的数量
		fbn = offset / BSIZE;
		printf("fbn = %u\n", len);

		//如果这个文件数据块的数量小于直接块的数量
		if(fbn &lt; NDIRECT)
		{
			//对应还没有分配的情况 或者刚刚好文件大小是BSIZE的整数倍
			if(xint(dinode_buf.data_address[fbn] == 0))
			{
				dinode_buf.data_address[fbn] = xint(freeblock++);
			}

			blk_num = xint(dinode_buf.data_address[fbn]);
		}
		//如果超过了直接快
		else
		{
			//看看间接块的地址有没有分配
			if(xint(dinode_buf.data_address[NDIRECT]) == 0)
			{
				dinode_buf.data_address[NDIRECT] = freeblock++;
			}
			//读取间接块
			rd_sec(dinode_buf.data_address[NDIRECT], (char *)indirect);
			//查看有没有分配
			if(indirect[fbn-NDIRECT] == 0)
			{
				indirect[fbn-NDIRECT] = freeblock++;
                //分配之后将数据写回去
				wr_sec(dinode_buf.data_address[NDIRECT], (char *)indirect);
			}
			blk_num = indirect[fbn-NDIRECT];
		}

		//读取这一块的内容到缓存
		rd_sec(blk_num, data_buf);

		//等待添加的内容可能比当前块剩余的空间少或者多
		//我们只写入最少的
		//如果直接按照剩余空间长度来写 可能会造成segment fault
		actual_wr_len = min(n, (fbn+1)*BSIZE-offset);
		printf("actual_wr_len = %d\n", actual_wr_len);
		memcpy(data_buf+offset-(fbn*BSIZE), p, actual_wr_len);

		//回写
		wr_sec(blk_num, data_buf);

		len -= actual_wr_len;
		offset += actual_wr_len;
		p += actual_wr_len;
	}

	dinode_buf.size = offset;
	wr_inode(inum, &amp;dinode_buf);
}
</code></pre>

<p>如图所示
第0块 <strong>[0x3ff:0x00]</strong> 是boot 没有内容
第1块 <strong>[0x7ff:0x400]</strong> 是sb 起始内容是设置的magic number
第2块 <strong>[0xaff:0x800]</strong> 是inode块 每条inode长64byte对应四行的长度 从第12byte开始就是对应的direct block的块号
第3块 <strong>[0xfff:0xc00]</strong> 是bitmap 没有填写
第4块 <strong>[0x13ff:0x1000]</strong> 是数据块 创建rootdir时将这块分配给他作为datablock 用于存放rootdir文件夹下的子文件和子文件夹</p>

<p>rootdir是第一个创建的文件类型是文件夹 分配的数据块号是4对应 <strong>[0x13ff:0x1000]</strong> 后来又添加了两个文件 这两个文件属于rootdir 因此会创建相应的directory entry到4号数据块</p>

<p><img src="https://i.imgur.com/L88wFbv.png" alt="测试图" />
<img src="https://i.imgur.com/oPT8bHO.png" alt="测试图" /></p>

<h2 id="问题">问题</h2>

<p>在使用read_sb读取superblock的内容时，最开始偷懒直接用函数当中的临时变量struct buf结构体来存储virtio返回的数据块，在初始化的时候没问题，但是放在fork_ret当中去执行总是读取的不对，data里面全部是空。换成heap上分配的内存来存储就没问题，栈空间不够？但是gdb查看时发现是够用的，暂时还不知道为什么。</p>

<h2 id="53-exec系统调用">5.3 exec()系统调用</h2>

<p>exec()和文件系统的实现密切相关，本来在fork()系统调用之后接着就实现exec()系统调用，但是在查看了xv6的源码之后发现没有文件系统还真写不出来。exec()的任务很简单：</p>

<blockquote>
  <p>exec is a system call that replaces a process’s user address space with data read from a file, called a binary or executable file.</p>
</blockquote>

<p>exec()执行新的程序，他的方式就是把当前进程的一切替换成将要执行的程序的内容。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">elf文件组成</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>elf header</strong></td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>program header</strong></td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center">.init</td>
    </tr>
    <tr>
      <td style="text-align: center">.plt</td>
    </tr>
    <tr>
      <td style="text-align: center">.text</td>
    </tr>
    <tr>
      <td style="text-align: center">.rodata</td>
    </tr>
    <tr>
      <td style="text-align: center">.data</td>
    </tr>
    <tr>
      <td style="text-align: center">.bss</td>
    </tr>
    <tr>
      <td style="text-align: center">.comment</td>
    </tr>
    <tr>
      <td style="text-align: center">.symtab</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>section header</strong></td>
    </tr>
  </tbody>
</table>

<ol>
  <li>通过path字符串找到这个文件的inode，inode包含了这个文件的所有信息</li>
</ol>

<pre><code class="language-c=">char *get_nxt_name(char *path, char *name);
int get_inode(char *path, struct inode *ino);
</code></pre>

<ol>
  <li>根据inode提供的信息解析elf header的内容</li>
</ol>

<pre><code class="language-c=">struct elfhdr {
  uint magic;        //必须是0x7f+ELF
  uchar elf[12];     //
  ushort type;       //文件类型 
                     //可重定位文件（Relocatable File）
                     //可执行文件（Executable File）
                     //共享目标文件（Shared Object File）等几种类型
  ushort machine;    //架构 riscv对应243
  uint version;  
  uint64 entry;      //程序入口
  uint64 phoff;      //Program Header Table 在文件中的偏移量
  uint64 shoff;      //Section Header Table 在文件中的偏移量
  uint flags;        //标志位，表示与文件相关的处理器标志
  ushort ehsize;     //ELF Header大小 通常是 52 字节。
  ushort phentsize;  //Program Header Table 中每个表项的大小。  
  ushort phnum;      //Program Header Table 中表项的数量
  ushort shentsize;  //Section Header Table 中每个表项的大小。  
  ushort shnum;      //Section Header Table 中表项的数量
  ushort shstrndx;   //包含节名称的字符串表在 Section Header Table 中的索引
};

//program header
struct proghdr
{
    unsigned int type;
    unsigned int flags;
    unsigned long off;
    unsigned long vaddr;
    unsigned long paddr;
    unsigned long filesz;
    unsigned long memsz;
    unsigned long align;
};
</code></pre>

<p>使用<strong>readelf</strong>工具可以帮助我们分析写入的elf文件</p>

<p>注意：program header可能会有多个！我们需要根据elf header的信息来写：
最开始只使用汇编来编写用户代码的时候没有这些问题，但是在使用c之后，使用int main()的方式来编写用户程序，在生成elf文件时，会按照user.ld指定的方式来链接代码段和数据段</p>

<p><img src="https://hackmd.io/_uploads/r15s2zfI3.png" alt="" /></p>

<p><img src="https://hackmd.io/_uploads/ry2EnzzIn.png" alt="" /></p>

<pre><code class="language-c=">//从磁盘加载程序到内存的api

</code></pre>

<ol>
  <li>根据elf header分配pgt 填充程序各段的位置</li>
</ol>

<p>在完成elf文件程序装载时又发现：因为之前都是很短的程序所以我们分配的都是一个页就行了</p>

<pre><code class="language-c=">int exec(char *path, char **argv)
{
    ...
    //分配新的页表
    pgt_t user_pagetable = proc_pagetable();
  
    //映射trampoline
    //映射trapframe
    //映射stack
    map_page(user_pagetable, (unsigned long)_trampoline, (unsigned long)TRAMPOLINE, 1, PTE_R | PTE_X);
    map_page(user_pagetable, (unsigned long)proc_ptr-&gt;trapframe, (unsigned long)TRAMPOLINE-0x1000, 1, PTE_R | PTE_W);
    map_page(user_pagetable, (unsigned long)stack, (unsigned long)4096, 1, PTE_R | PTE_W);
  
    //读取elfhdr proghdr
    //分配装载程序内容的物理地址并映射
    //装载程序内容到刚才分配的物理地址
    read_elfhr(&amp;proc_elfhdr, &amp;proc_proghdr, &amp;proc_inode);
    uvmalloc(user_pagetable, 0, proc_proghdr.memsz, 0);
    loadsec(user_pagetable, &amp;proc_inode, proc_proghdr.vaddr, proc_proghdr.off, proc_proghdr.filesz);
  
    //填写程序结构体
    proc_ptr-&gt;proc_pagetable = user_pagetable;
    proc_ptr-&gt;prog_size = 4096;
    proc_ptr-&gt;trapframe-&gt;epc = proc_elfhdr.entry;
    proc_ptr-&gt;trapframe-&gt;sp = 4096+4096;
  
    return 0;
}
</code></pre>

<h3 id="测试exec系统调用">测试exec()系统调用</h3>

<p>在第一个用户程序initcode当中调用exec</p>

<h2 id="54-file-descriptor-layer">5.4 File Descriptor Layer</h2>

<p>在完成inode层之后本想紧接着实现一系列和文件相关的系统调用如<strong>pipe()</strong>，却发现在inode之上都还有一层。先来看看在完成inode之后我们得到了什么？我们可以以文件路径的方式访问存储在img文件中的任何一个文件或者文件夹，但是这离把所有的资源都抽象成文件还不够。</p>

<p>我们还需要一层<strong>File Descriptor Layer</strong>：</p>

<p><img src="https://hackmd.io/_uploads/ryTx1cXV2.png" alt="" /></p>

<p>xv6用<strong>struct file</strong>结构体实现了所有资源都是文件的统一,<strong>struct file</strong>就是对<strong>inode</strong>和<strong>pipe</strong>的一个新的封装</p>

<pre><code class="language-c=">struct file {
  enum { FD_NONE, FD_PIPE, FD_INODE, FD_DEVICE } type;
  int ref; // reference count
  char readable;
  char writable;
  struct pipe *pipe; // FD_PIPE
  struct inode *ip;  // FD_INODE and FD_DEVICE
  uint off;          // FD_INODE I/O offset
  short major;       // FD_DEVICE
};
</code></pre>

<p>使用路径名并通过file descriptor的方式打开读写磁盘上的文件示意图如下：</p>

<p><img src="/image/文件系统框架.png" alt="fs_all" /></p>

<p>在图中可以清晰的知道通过<strong>open</strong>函数返回的<strong>fd</strong>就是进程控制块(PCB)中<strong>ofile</strong>数组中的元素号码，内核中通过<strong>ofile[fd]</strong>寻址到其对应的<strong>struct file</strong>，<strong>struct file</strong>中对应的不同的文件类型分别有不同的指针指向对应的结构体，例如如果这个struct file的类型是FD_FILE那么就设置</p>

<h3 id="541-copy_from_user-copy_to_user">5.4.1 copy_from_user() copy_to_user()</h3>

<h3 id="542-pipe">5.4.2 pipe()</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//用户程序使用pipe的声明</span>
<span class="kt">int</span> <span class="nf">pipe</span><span class="p">(</span><span class="kt">int</span> <span class="n">pipefd</span><span class="p">[</span><span class="mi">2</span><span class="p">]);</span>
</code></pre></div></div>

<p>pipe从他的结构体定义中可知就是一块RAM中的内存，有一些读写参数加一把锁(写完之前不能读，读完之前不能写)。新建一个pipe和新建一个文件不同的是pipe有读写两个端口，这两个端口要分别对应不同的fd，fd[2]就对应两个struct file，而一般的文件只需要一个是struct file</p>

<p><img src="https://i.imgur.com/kBN0QAv.png" alt="pipe1" /></p>

<p><img src="https://i.imgur.com/zuuHw0g.png" alt="Imgur" /></p>

<p><img src="https://i.imgur.com/i2bbH9q.png" alt="Imgur" /></p>

<p><a href="https://academy.cba.mit.edu/classes/networking_communications/SD/FAT.pdf">FAT</a></p>

<p><a href="https://www.cs.williams.edu/~cs432/labs/07-files.pdf">07-files.pdf</a></p>

<p><a href="https://toroid.org/unix-pipe-implementation">pipe implementation</a></p>

<h1 id="6-开启多核">6. 开启多核</h1>

<p>走到这里发现很多的系统调用和函数都涉及到锁，还是开启多核把最基本的并发同步实验做了吧。</p>

<p>QEMU开启多核添加</p>

<pre><code class="language-Makefile=">
</code></pre>

<h3 id="为printf加锁">为printf()加锁</h3>

<pre><code class="language-c=">//xv6使用的是gcc编译器生成的命令
void acquire(struct spinlock *lk) 
{ 
    while(__sync_lock_test_and_set(&amp;lk-&gt;locked, 1) != 0) 
    { 
        ; 
    }   
  
    __sync_synchronize(); 
}
</code></pre>

<p>反汇编之后可以看到：</p>

<pre><code class="language-asm=">0000000080201fa4 &lt;acquire&gt;:
    80201fa4:   fe010113            addi    sp,sp,-32     80201fa8:   00813c23            sd  s0,24(sp)
    80201fac:   02010413            addi    s0,sp,32
    80201fb0:   fea43423            sd  a0,-24(s0)
    80201fb4:   00000013            nop
    80201fb8:   fe843783            ld  a5,-24(s0)
    80201fbc:   00100713            li  a4,1
    80201fc0:   0ce7a72f            amoswap.w.aq    a4,a4,(a5)
    80201fc4:   0007079b            sext.w  a5,a4
    80201fc8:   fe0798e3            bnez    a5,80201fb8 &lt;acquire+0x14&gt;
    80201fcc:   0ff0000f            fence
    80201fd0:   00000013            nop
    80201fd4:   01813403            ld  s0,24(sp)
    80201fd8:   02010113            addi    sp,sp,32
    80201fdc:   00008067            ret
</code></pre>

<p>栈帧基本上是16byte的倍数 前2个byte是ra和s0(fp),后面放参数</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />[] semaphore信号量</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />sleep wakeup实现等待队列</li>
</ul>

<h2 id="sleep-wakeup同步机制">sleep() wakeup()同步机制</h2>

<pre><code class="language-c=">void sleep(unsigned long *chan)
{
    proc_ptr-&gt;state = SLEEPING;
    proc_ptr-&gt;sleep_chan = (unsigned long)chan;
    sched();
}

void wakeup(unsigned long *chan)
{
    for(int n=0; n&lt;MAX_PROC; n++)
   {
        if((proc_list[n].state == SLEEPING) &amp;&amp; (proc_list[n].sleep_chan == (unsigned long)chan))
        {
           proc_list[n].state = RUNNABLE;
        }
    }
}          

int sys_sleep()
{
    int interval = proc_ptr-&gt;trapframe-&gt;a0;
    unsigned long tick_st = systicks;
    while(systicks-tick_st &lt; interval)
    {
        sleep(&amp;systicks);                                          
    }   
}       
</code></pre>

<p>注意：sleep()时需要将原本持有的锁释放 否则造成死锁</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />todo</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />对比linux</li>
</ul>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
</code></pre></div></div>

<p>uboot启动传参数</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
 *r2 DTB表的位置
 */</span>
<span class="n">kernel_entry</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">machid</span><span class="p">,</span> <span class="n">r2</span><span class="p">);</span> 
</code></pre></div></div>

<p>uboot为什么从0x20000开始：DOS时代使用1M一下的内存</p>

<p>uboot跳到head.S</p>

<h1 id="7-内存管理">7. 内存管理</h1>

<h2 id="1-操作系统为什么需要能够连续的物理页">1. 操作系统为什么需要能够连续的物理页？</h2>

<p>有了虚拟空间之后似乎好像不再依赖连续的地址空间了，但是从linux使用的buddy allocator来看，操作系统仍然需要经常分配连续的物理地址。</p>
<ol>
  <li>某些硬件设备（如DMA控制器）可能要求连续的物理页来进行数据传输。磁盘大文件传输。</li>
  <li>尽管虚拟内存可以将物理内存映射到不连续的虚拟地址空间，但在某些情况下，连续的物理页可以带来性能上的优势。例如，连续的物理页可以提高缓存的命中率，减少内存访问的延迟。</li>
</ol>

<p>之前实现的物理内存分配中只有一个单位–<strong>页</strong>，但是很明显我们还需要一种小内存的分配管理办法，不然这个内存分配的内部碎片就太大了，哪怕我们只需要malloc(1)也会分配4K的空间。</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />完善内存管理</li>
</ul>

<h1 id="参考">参考</h1>

<p><a href="https://github.com/qemu/qemu/blob/master/hw/riscv/virt.c">virt board</a></p>

<p><a href="https://github.com/mit-pdos/xv6-riscv">xv6_riscv</a></p>

<p><a href="https://github.com/runninglinuxkernel/riscv_programming_practice">RV体系结构编程与实践</a></p>

<p><a href="https://github.com/plctlab/riscv-operating-system-mooc">riscv-operating-system-mooc</a></p>

<p><a href="https://ipads.se.sjtu.edu.cn/ospi/">操作系统：原理与实现</a></p>

<p><a href="https://libgen.is/book/index.php?md5=F6FE9D77E7CC5647FE35B0446F34B5AC">Operating Systems Design and Implementation</a></p>

<p><a href="https://www.youtube.com/playlist?list=PLbtzT1TYeoMhTPzyTZboW_j7TPAnjv9XB">hhp3</a></p>

<p><a href="http://web.cs.ucla.edu/classes/winter12/cs111/scribe/15e/#:~:text=Having%20a%20separate%20page%20table,will%20work%20for%20every%20process.">Virtual Memory</a></p>

<p><a href="https://jyywiki.cn/OS/2022/">南大操作系统课</a></p>

<p><a href="https://www.youtube.com/playlist?list=PL2Yggtk_pK6-R9ehjj0AoTnWrNOLChuld">CS134</a></p>

<p><a href="https://notnik.cc/posts/async/">coroutine</a></p>

<p><a href="https://brennan.io/2020/03/22/sos-block-device/">virtio driver</a></p>

<p><a href="https://users.cs.cf.ac.uk/dave/C/">C</a></p>

<p><a href="https://courses.cs.washington.edu/courses/csep551/19au/projects/">更多的系统编程的idea</a></p>]]></content><author><name>Songyj</name></author><summary type="html"><![CDATA[RVOS]]></summary></entry></feed>